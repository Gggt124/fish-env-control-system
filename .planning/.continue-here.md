---
context: default
phase: ad-hoc UX fixes
task: 0
total_tasks: 0
status: in_progress
last_updated: 2026-06-09T17:15:00Z
---

<current_state>
We are responding to ad-hoc user UI/UX feedback for the web interface, specifically on the Wi-Fi setup page.
</current_state>

<completed_work>

Completed Tasks:
- Task 1: Implemented 1-second debounce and dirty-checking for the `toggleAutoProfile` checkbox in `app.js` to prevent excessive NVS writes.
- Task 2: Increased small typography scale in `style.css` (`--text-xs` to 0.75rem, `--text-sm` to 0.875rem) to improve Thai text readability (e.g., "-34 dBm", "เชื่อมต่ออยู่", "Refresh").
</completed_work>

<remaining_work>

- Task 3: Await user feedback on the new font sizes.
</remaining_work>

<decisions_made>

- Decided to globally adjust the CSS variables (`--text-xs` and `--text-sm`) instead of manually overriding `.btn-sm` or `.sig-label` classes so that the entire design scales proportionally.
</decisions_made>

<blockers>
- None.
</blockers>

## Required Reading (in order)
1. `main/static/style.css` — See lines 68-75 for the updated typography scale.
2. `main/static/app.js` — See `toggleAutoProfile` for the new debounce logic.

<context>
The user provided screenshots showing that the text in the buttons and Wi-Fi signal strength was too small on mobile. We increased the base CSS typography scale slightly and flashed the board. We are now pausing work to wait for the user to review it.
</context>

<next_action>
Start with: Ask the user if the new text sizes are comfortable to read or if they need further adjustments.
</next_action>
