---
quick_id: 260520-s4a
status: planned
date: 2026-05-20
---

# Quick Task 260520-s4a: Smooth pump countdown UI and update safe defaults

## Goal

Make the dashboard countdown feel smoother during ON/OFF phase changes and float switch transitions, then update product defaults requested after hardware validation.

## Tasks

1. Update `main/static/app.js` so countdown rendering is based on a local deadline instead of integer decrement drift.
2. Predict local ON/OFF phase rollover from the loaded pump config while continuing to reconcile with authoritative `/api/pump/status`.
3. Poll pump status frequently enough to reflect external float switch changes without relying on internet or frontend dependencies.
4. Change defaults in `components/app_config/app_config.h`: password `admin123`, session max age disabled by default, pump auto-start off by default.
5. Update `components/session/session.c` so `SESSION_MAX_AGE_SEC == 0` means no automatic timeout.
6. Keep README/AGENTS/component docs and `.planning/STATE.md` consistent with the new defaults.
7. Validate with `node --check main/static/app.js` and the project ESP-IDF build wrapper.

