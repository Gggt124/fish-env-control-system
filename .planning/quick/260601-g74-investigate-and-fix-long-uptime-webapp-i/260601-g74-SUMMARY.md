---
status: complete
quick_id: 260601-g74
date: 2026-06-01
---

# Quick Task 260601-g74 Summary

## Outcome

The attached 13-hour soak log no longer shows a reboot or monotonic heap loss, but weak-link refresh bursts still produce failed HTTP sends and each STA fallback restore starts DHCP twice.

Implemented the remaining scoped cleanup:

- Propagate JSON and embedded static response send failures so ESP-IDF closes the failed HTTP session.
- Configure the SoftAP before `esp_wifi_start()` and reuse that config during STA-to-APSTA fallback restore.
- Fail Wi-Fi manager initialization when the initial SoftAP start cannot be confirmed.
- Make `wifi_manager` own asynchronous scan state, reject scan overlap, cancel late callbacks before request cleanup, and recover STA reconnect after scan completion or cancellation.
- Reject a new scan until the canceled scan's trailing `WIFI_EVENT_SCAN_DONE` cleanup event has been consumed.
- Propagate redirect send failures as well as JSON/static failures so ESP-IDF closes all broken response sessions consistently.

## Verification

- `.\scripts\build.ps1` passed.
- `git diff --check` passed.
- Generated `build\fish_pump_relay_timer_control.bin` size: `0x10bb00`.
- `.\scripts\flash.ps1 -Port COM5` flashed the final supplemental build successfully and verified all written image hashes.
- A post-flash RTS reset reached `System Ready`, started SoftAP and DHCP once, and reported `Heap=195896`, `HeapMin=195004`, `Largest=110592`, `MainStackHWM=1972`.

## Closure Validation

- Final whole-device soak ran for at least `13:38:10`.
- STA requested connect passed `3/3`; explicit forget/reconnect passed `2/2`;
  Wi-Fi scans passed `9/9`.
- No spontaneous STA loss, reboot, panic, brownout, watchdog trip, assertion,
  heap corruption, stack overflow, or monotonic heap loss occurred.
- HTTP completed `8176` requests. Seven bounded stale-client send failures
  drained cleanly without a socket leak or static-response deadline abort.
