---
quick_id: 260601-g74
status: complete
description: Investigate and fix long-uptime webapp instability after repeated STA connect disconnect cycles
---

# Quick Task 260601-g74 Plan

## Context

The attached 13-hour soak log shows stable heap recovery and no reboot, but embedded static responses still fail with socket errors 11 and 104 around weak-link refresh bursts. STA fallback restores APSTA mode, but DHCP startup is logged twice during each restore.

## Tasks

1. Propagate failed JSON and static response sends back to ESP-IDF so the HTTP server closes failed sessions.
2. Configure the SoftAP before the Wi-Fi radio starts and avoid reapplying AP config during fallback restore.
3. Run `.\scripts\build.ps1` and inspect the scoped diff.
4. Record the remaining board-level soak validation requirement.

## Verification

- `.\scripts\build.ps1`
- Confirm send failures return the original ESP-IDF error.
- Confirm AP config is applied once before `esp_wifi_start()` and preserved across STA-to-APSTA restore.
