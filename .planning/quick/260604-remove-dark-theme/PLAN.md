# Quick Task: Remove Dark Theme and Theme Switcher

Remove the dark theme CSS styles, theme toggle buttons from dashboard/hardware/status/wifi/login pages, and `/api/theme` GET/POST web server handlers along with NVS persistence functions.
