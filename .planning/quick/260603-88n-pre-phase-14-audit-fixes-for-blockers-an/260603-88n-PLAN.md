# Quick Task 260603-88n: Pre Phase 14 Audit Fixes

## Goal

Audit the firmware and embedded UI before Phase 14, then fix blockers and clearly unusable UI defects found during the audit.

## Scope

- Fix high-risk firmware bugs found by code review.
- Fix mobile/browser UI failures found by the in-app browser and UI audit.
- Keep changes narrow and preserve ESP-IDF/offline frontend constraints.

## Tasks

1. Fix captive DNS response bounds handling and DNS task status cleanup.
2. Align float switch to timer/relay mapping with the project contract.
3. Keep SoftAP fallback enabled by default.
4. Harden NVS commit checks, session init failure handling, request body reads, and HTTP route registration failure behavior.
5. Fix mobile navigation, Wi-Fi scan loading feedback, Wi-Fi form labels, mobile network row layout, and toast click interception.
6. Validate with `.\scripts\build.ps1` and in-app browser checks.
