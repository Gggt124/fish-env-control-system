---
quick_id: 260601-jk6
status: complete
description: Add pump cooling board runtime CPU workload and temperature diagnostics to active 12 hour soak logger
---

# Quick Task 260601-jk6 Plan

## Context

Expand the active network soak into a whole-device soak without changing pump or cooling control
behavior. Capture board runtime, CPU workload, watchdog subscription, heap fragmentation, logical
pump hardware state, and DS18B20 cooling temperature so the next log supports point-by-point fixes.

Classic ESP32 internal die temperature is intentionally excluded: the safe field measurement is
the existing external DS18B20 channel. CPU workload uses 30-second deltas from FreeRTOS idle runtime
counters so the 32-bit ESP timer source wrapping after roughly 4290 seconds does not invalidate the
12-hour soak.

## Tasks

1. Enable FreeRTOS runtime stats with the ESP timer source in reproducible SDK defaults.
2. Add `[BOARD_DIAG]`, `[PUMP_DIAG]`, and `[COOLING_DIAG]` snapshots to the 30-second status loop.
3. Build, flash COM5, restart the hidden serial logger, and confirm snapshots are appended.
4. Record the new soak log handoff.

## Verification

- `.\scripts\build.ps1 -FullClean`
- `git diff --check`
- `.\scripts\flash.ps1 -Port COM5`
- Confirm `[BOARD_DIAG]`, `[PUMP_DIAG]`, and `[COOLING_DIAG]` appear in the timestamped soak log.
