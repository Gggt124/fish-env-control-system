---
phase: 09-authenticated-hardware-and-cooling-apis
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - main/web_server.c
autonomous: true
requirements:
  - UI-07
user_setup: []
must_haves:
  truths:
    - "Plan 02 depends on Plan 01 route-capacity changes and existing `GET /api/cooling/status` from Phase 8."
    - "Cooling config POST must validate threshold, hysteresis, auto-enable, mode, test timeout, compressor min-off, and cooling relay polarity before saving."
    - "Persist Auto and Force OFF as safe modes; Test ON is runtime-only and must not persist across reboot."
    - "Cooling config apply must use the active hardware map, not pending GPIO values."
    - "If saved settings cannot be applied to runtime, the cooling relay must remain OFF and the JSON response must make the apply failure visible."
    - "All cooling mutation routes must require `require_auth(req)` and `is_same_origin(req, false)`."
  artifacts:
    - path: "main/web_server.c"
      provides: "Authenticated cooling config and mode mutation APIs"
      contains: "/api/cooling/config"
    - path: "main/web_server.c"
      provides: "Runtime-only cooling mode route for Auto, Force OFF, and Test ON"
      contains: "/api/cooling/mode"
  key_links:
    - from: "main/web_server.c"
      to: "components/nvs_store/nvs_store.h"
      via: "loads and saves validated cooling settings"
      pattern: "nvs_store_save_cooling_settings"
    - from: "main/web_server.c"
      to: "components/cooling_control/cooling_control.h"
      via: "applies saved config and runtime mode changes"
      pattern: "cooling_control_set_mode"
---

<objective>
Add authenticated cooling configuration and runtime mode APIs on top of the Phase 8 cooling status/runtime contract.

Purpose: Let the future owner dashboard configure cooling threshold, hysteresis, boot auto-enable, cooling relay polarity, and bounded Test ON behavior without bypassing compressor protection or persisting unsafe test state.
Output: `GET /api/cooling/config`, `POST /api/cooling/config`, and `POST /api/cooling/mode` validate inputs, save/apply safe settings, and return current cooling status.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-authenticated-hardware-and-cooling-apis/09-RESEARCH.md
@.planning/phases/09-authenticated-hardware-and-cooling-apis/09-01-PLAN.md
@.planning/phases/08-ds18b20-cooling-runtime/08-01-SUMMARY.md
@.planning/phases/08-ds18b20-cooling-runtime/08-02-SUMMARY.md
@.planning/phases/08-ds18b20-cooling-runtime/08-03-SUMMARY.md
@components/cooling_control/cooling_control.h
@components/nvs_store/nvs_store.h
@components/hardware_map/hardware_map.h
@main/app_main.c
@main/web_server.c
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cooling config serialization, enum parsing, and range validators</name>
  <files>main/web_server.c</files>
  <read_first>main/web_server.c, components/nvs_store/nvs_store.h, components/cooling_control/cooling_control.h, components/hardware_map/hardware_map.h</read_first>
  <action>Add helper functions to serialize `nvs_store_cooling_settings_t` with stable fields: `threshold_c`, `threshold_c_x10`, `hysteresis_c`, `hysteresis_c_x10`, `auto_enable`, `mode`, `test_timeout_sec`, `compressor_min_off_sec`, and `cooling_relay_polarity`. Add parser helpers for cooling mode strings (`auto`, `force_off`, `test_on`) and relay polarity strings (`active_low`, `active_high`). Add numeric validators matching `nvs_store` and `cooling_control`: threshold `-550..1250` x10, hysteresis `1..500` x10, test timeout `1..3600`, compressor min-off `0..86400`. Prefer accepting tenths integers and decimal Celsius fields only if conversion is explicit and bounded; do not accept ambiguous strings.</action>
  <verify>`rg "cooling.*config|threshold_c_x10|hysteresis_c_x10|test_timeout_sec|compressor_min_off_sec|cooling_relay_polarity|parse.*cooling" main/web_server.c`</verify>
  <acceptance_criteria>
    - JSON field names are stable and machine-friendly.
    - Validation limits match storage/runtime validation.
    - Invalid type, missing required field, out-of-range value, invalid mode, and invalid polarity return JSON errors.
    - Test ON is parsed as a valid runtime mode but not allowed as a persisted config mode.
    - Existing `/api/cooling/status` enum strings stay unchanged.
  </acceptance_criteria>
  <done>Cooling config parser/serializer helpers are available.</done>
</task>

<task type="auto">
  <name>Task 2: Add authenticated GET /api/cooling/config</name>
  <files>main/web_server.c</files>
  <read_first>main/web_server.c, components/nvs_store/nvs_store.h, components/cooling_control/cooling_control.h</read_first>
  <action>Register `GET /api/cooling/config` and implement an authenticated handler. Load cooling settings with `nvs_store_load_cooling_settings()`, include the settings status string, include the serialized config, include limits/enums for frontend inputs, and include current cooling status by reusing `api_cooling_add_status_fields()` or a nested status helper. Defaults-missing should return defaults with a status string; invalid/error load should return a clear JSON error consistent with existing API patterns.</action>
  <verify>`rg "/api/cooling/config|handle_api_cooling_config_get|nvs_store_load_cooling_settings|api_cooling_add_status_fields|cooling.*limits" main/web_server.c`</verify>
  <acceptance_criteria>
    - The route is authenticated.
    - Saved config and current runtime status are both available for UI-07.
    - Limits and enum values are available for the frontend without hardcoding.
    - Defaults-missing behavior is non-fatal and explicit.
    - The handler does not mutate settings or runtime state.
  </acceptance_criteria>
  <done>`GET /api/cooling/config` exposes persisted cooling configuration and runtime status.</done>
</task>

<task type="auto">
  <name>Task 3: Add authenticated POST /api/cooling/config</name>
  <files>main/web_server.c</files>
  <read_first>main/web_server.c, main/app_main.c, components/nvs_store/nvs_store.h, components/hardware_map/hardware_map.h, components/cooling_control/cooling_control.h</read_first>
  <action>Register `POST /api/cooling/config` and implement a protected mutation handler. Require `require_auth(req)` and `is_same_origin(req, false)`. Parse the payload into `nvs_store_cooling_settings_t`, preserving existing values for optional fields only if this matches the local full-replacement pattern. Reject `mode:"test_on"` from config persistence with an error pointing callers to `/api/cooling/mode`. Save through `nvs_store_save_cooling_settings()`. After saving, build a `cooling_control_config_t` from the active hardware map plus saved settings and reapply with `cooling_control_init()` or a small local helper mirroring the boot mapping. If runtime apply fails, keep the cooling relay safe OFF through `cooling_control_stop()` when appropriate and return a JSON error that includes saved config/status. Never use pending hardware GPIO values for runtime apply.</action>
  <verify>`rg "handle_api_cooling_config_post|nvs_store_save_cooling_settings|cooling_control_init|cooling_control_stop|nvs_store_load_hardware_map|is_same_origin\\(req, false\\)" main/web_server.c`</verify>
  <acceptance_criteria>
    - Threshold, hysteresis, auto-enable, mode, test timeout, compressor min-off, and cooling relay polarity are validated before save.
    - Test ON cannot be persisted across reboot.
    - Saved config is applied to the current runtime using active GPIOs only.
    - Apply failure is visible and fail-safe.
    - Same-origin POST protection remains intact.
  </acceptance_criteria>
  <done>`POST /api/cooling/config` persists and applies safe cooling settings.</done>
</task>

<task type="auto">
  <name>Task 4: Add authenticated POST /api/cooling/mode for runtime mode and Test ON</name>
  <files>main/web_server.c</files>
  <read_first>main/web_server.c, components/cooling_control/cooling_control.h, components/nvs_store/nvs_store.h</read_first>
  <action>Register `POST /api/cooling/mode`. Require auth and same-origin. Parse `mode`. For `auto` and `force_off`, call `cooling_control_set_mode()` and save the non-test mode to cooling settings if the project wants mode persistence; do not change `auto_enable` unless the payload explicitly includes it and validation accepts it. For `test_on`, call `cooling_control_start_test()` and do not save `test_on` to NVS. Return `ok:true`, current config, and current status, including `test_remaining_sec`, `lockout_active`, and `blocked_reason` so compressor lockout is visible when Test ON is blocked.</action>
  <verify>`rg "/api/cooling/mode|handle_api_cooling_mode_post|cooling_control_set_mode|cooling_control_start_test|test_remaining_sec|blocked_reason" main/web_server.c`</verify>
  <acceptance_criteria>
    - Auto and Force OFF mode changes are valid authenticated mutations.
    - Test ON is runtime-only and bounded by the existing runtime timeout.
    - Test ON response exposes timeout or lockout status.
    - Invalid modes return JSON errors.
    - No cooling mode route bypasses compressor protection.
  </acceptance_criteria>
  <done>`POST /api/cooling/mode` controls runtime cooling mode safely.</done>
</task>

</tasks>

<verification>
- [ ] `rg "/api/cooling/config|handle_api_cooling_config_get|handle_api_cooling_config_post|nvs_store_save_cooling_settings" main/web_server.c`
- [ ] `rg "/api/cooling/mode|cooling_control_set_mode|cooling_control_start_test|test_remaining_sec|blocked_reason" main/web_server.c`
- [ ] `rg "threshold_c_x10|hysteresis_c_x10|auto_enable|test_timeout_sec|compressor_min_off_sec|cooling_relay_polarity" main/web_server.c`
- [ ] `rg "test_on" main/web_server.c` and inspect that Test ON is not persisted through config POST.
- [ ] `rg "is_same_origin\\(req, false\\)" main/web_server.c`
</verification>

<success_criteria>
- Cooling config APIs validate threshold, hysteresis, auto-enable, mode, test timeout, compressor min-off, and relay polarity.
- Cooling config changes are saved and applied against active hardware only.
- Test ON is exposed as runtime mode/action without reboot persistence.
- Cooling status remains stable and machine-readable.
- UI-07 cooling API coverage is ready for Phase 10 UI consumption.
</success_criteria>

<output>
After completion, create `.planning/phases/09-authenticated-hardware-and-cooling-apis/09-02-SUMMARY.md`.
</output>
