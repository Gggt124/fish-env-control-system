---
phase: 09-authenticated-hardware-and-cooling-apis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/app_config/app_config.h
  - main/web_server.c
autonomous: true
requirements:
  - UI-03
  - UI-04
  - UI-05
  - UI-06
  - UI-07
user_setup: []
must_haves:
  truths:
    - "Phase 9 has no CONTEXT.md; use roadmap success criteria, REQUIREMENTS.md UI-03 through UI-07, and 09-RESEARCH.md as the planning contract."
    - "Hardware GPIO mutation must save pending values only; do not write the active hardware map or reinitialize pump/cooling runtime from pending GPIO pins."
    - "Every hardware map value accepted by POST must come from the role-specific `hardware_map_options_for_role()` safe enum set."
    - "The hardware map POST must require `confirm_reboot_required:true` before saving pending wiring changes."
    - "If the submitted hardware map equals the active map, clear pending hardware and report `reboot_required:false`."
    - "New mutation routes must require `require_auth(req)` and `is_same_origin(req, false)`."
    - "Raise `APP_TEMPLATE_HTTP_MAX_URI_HANDLERS` before registering new routes; the current route table has only one spare handler."
  artifacts:
    - path: "main/web_server.c"
      provides: "Authenticated hardware map read/update API with safe GPIO options and pending/reboot status"
      contains: "/api/hardware/map"
    - path: "components/app_config/app_config.h"
      provides: "HTTP handler capacity for Phase 9 API routes"
      contains: "APP_TEMPLATE_HTTP_MAX_URI_HANDLERS"
  key_links:
    - from: "main/web_server.c"
      to: "components/hardware_map/hardware_map.h"
      via: "serializes safe role options and validates submitted GPIO values"
      pattern: "hardware_map_options_for_role"
    - from: "main/web_server.c"
      to: "components/nvs_store/nvs_store.h"
      via: "loads active/pending maps and saves pending values only"
      pattern: "nvs_store_save_pending_hardware_map"
---

<objective>
Add authenticated hardware map APIs for active/pending GPIO state, safe GPIO enum options, reboot-required status, and confirmation-guarded pending saves.

Purpose: Give Phase 10's Hardware/Install UI a safe API surface for dropdown-only GPIO mapping without allowing runtime pin changes or freeform GPIO input.
Output: `GET /api/hardware/map` returns active map, pending map, safe role options, statuses, and reboot-required state; `POST /api/hardware/map` saves validated pending maps only when explicit confirmation is present.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-authenticated-hardware-and-cooling-apis/09-RESEARCH.md
@.planning/phases/06-hardware-contract-and-persistent-map-foundation/06-01-SUMMARY.md
@.planning/phases/06-hardware-contract-and-persistent-map-foundation/06-02-SUMMARY.md
@.planning/phases/06-hardware-contract-and-persistent-map-foundation/06-03-SUMMARY.md
@components/app_config/app_config.h
@components/hardware_map/hardware_map.h
@components/hardware_map/hardware_map.c
@components/nvs_store/nvs_store.h
@components/nvs_store/nvs_store.c
@main/web_server.c
</context>

<tasks>

<task type="auto">
  <name>Task 1: Increase HTTP route capacity and add hardware-map serialization helpers</name>
  <files>components/app_config/app_config.h, main/web_server.c</files>
  <read_first>components/app_config/app_config.h, main/web_server.c, components/hardware_map/hardware_map.h, components/nvs_store/nvs_store.h</read_first>
  <action>Increase `APP_TEMPLATE_HTTP_MAX_URI_HANDLERS` to a conservative value that covers existing routes plus all Phase 9 API routes with small headroom, for example 28 or 32. In `web_server.c`, add helper functions to serialize a `hardware_map_t` object with stable JSON fields `float_input_gpio`, `pump_relay1_gpio`, `pump_relay2_gpio`, `ds18b20_gpio`, and `cooling_relay_gpio`. Add pending-status string helpers for `nvs_store_pending_hardware_map_load_status_t` and reuse existing active hardware map status naming where practical.</action>
  <verify>`rg "APP_TEMPLATE_HTTP_MAX_URI_HANDLERS|hardware_map.*json|pending_hardware|float_input_gpio|ds18b20_gpio" components/app_config/app_config.h main/web_server.c`</verify>
  <acceptance_criteria>
    - HTTP route capacity can register all Phase 9 routes without `ESP_ERR_HTTPD_HANDLERS_FULL`.
    - Active and pending maps serialize with stable field names.
    - Pending load status is visible as a stable string.
    - Existing pump/cooling status helpers continue to compile.
    - No static frontend or runtime control behavior changes are made in this task.
  </acceptance_criteria>
  <done>Route capacity and hardware map JSON helper foundations exist.</done>
</task>

<task type="auto">
  <name>Task 2: Serialize safe GPIO options grouped by role</name>
  <files>main/web_server.c</files>
  <read_first>main/web_server.c, components/hardware_map/hardware_map.h, components/hardware_map/hardware_map.c</read_first>
  <action>Add cJSON helpers that call `hardware_map_options_for_role()` for each hardware role and serialize safe dropdown options grouped by field name. Each option should include at least `gpio`, `label`, `role`, `input_capable`, `output_capable`, `internal_pull_capable`, and `is_default`. Use the firmware-defined option arrays only; do not generate arbitrary GPIO ranges. Keep labels from `hardware_gpio_option_t` so the frontend can render owner-friendly options without hardcoding pin names.</action>
  <verify>`rg "hardware_map_options_for_role|input_capable|output_capable|internal_pull_capable|is_default|options" main/web_server.c`</verify>
  <acceptance_criteria>
    - UI-03 is supported by API-returned safe enum options.
    - Options are grouped for all five roles: float input, pump Relay 1, pump Relay 2, DS18B20 data, and cooling relay.
    - No freeform numeric GPIO list is generated.
    - Memory failures return existing JSON memory errors rather than partial malformed JSON.
    - Role names are stable strings suitable for frontend use.
  </acceptance_criteria>
  <done>Safe hardware GPIO option metadata is available to the hardware API.</done>
</task>

<task type="auto">
  <name>Task 3: Add authenticated GET /api/hardware/map</name>
  <files>main/web_server.c</files>
  <read_first>main/web_server.c, components/nvs_store/nvs_store.h, components/hardware_map/hardware_map.h</read_first>
  <action>Register `GET /api/hardware/map` and implement a protected handler. The response must require `require_auth(req)`, load active hardware map, load pending hardware map, call `nvs_store_hardware_reboot_required()`, and return `ok:true`, `active`, `pending`, `pending_valid`, `reboot_required`, `active_status`, `pending_status`, and `options`. If active map load falls back to defaults, return those defaults with the status string rather than failing the whole route. If pending is not found, report `pending_valid:false` and `reboot_required:false` unless an error/invalid pending state is detected.</action>
  <verify>`rg "/api/hardware/map|handle_api_hardware_map|nvs_store_load_hardware_map|nvs_store_load_pending_hardware_map|nvs_store_hardware_reboot_required" main/web_server.c`</verify>
  <acceptance_criteria>
    - The route is authenticated.
    - Active values, pending values, and reboot-required state are visible for UI-05.
    - Safe options are included in the same response for UI-03.
    - Pending-not-found is handled as normal, not an error.
    - The handler does not mutate NVS or runtime state.
  </acceptance_criteria>
  <done>`GET /api/hardware/map` exposes hardware map state and safe options.</done>
</task>

<task type="auto">
  <name>Task 4: Add confirmation-guarded POST /api/hardware/map</name>
  <files>main/web_server.c</files>
  <read_first>main/web_server.c, components/hardware_map/hardware_map.h, components/nvs_store/nvs_store.h</read_first>
  <action>Register `POST /api/hardware/map` and implement a protected mutation handler. Require `require_auth(req)` and `is_same_origin(req, false)`. Parse JSON object `map` plus `confirm_reboot_required`. Reject missing or false confirmation before saving. Parse the five GPIO fields as integers, validate each through `hardware_map_gpio_allowed_for_role()` for its role, then validate the complete map with `hardware_map_validate()`. Save only through `nvs_store_save_pending_hardware_map()`. If the submitted map equals the active map, call `nvs_store_clear_pending_hardware_map()` instead. Return the same shape as GET after the save/clear operation. Do not call `nvs_store_save_hardware_map()`, `pump_control_init()`, or `cooling_control_init()` from this handler.</action>
  <verify>`rg "confirm_reboot_required|hardware_map_gpio_allowed_for_role|hardware_map_validate|nvs_store_save_pending_hardware_map|nvs_store_clear_pending_hardware_map|is_same_origin\\(req, false\\)" main/web_server.c`</verify>
  <acceptance_criteria>
    - UI-04 saves GPIO changes as pending persistent values only.
    - UI-06 confirmation is mandatory and enforced server-side.
    - Unsafe or role-invalid GPIO values are rejected.
    - Pending equal to active clears pending state and reports no reboot required.
    - Existing auth and same-origin POST protection remain intact.
  </acceptance_criteria>
  <done>`POST /api/hardware/map` safely stores pending GPIO changes.</done>
</task>

</tasks>

<verification>
- [ ] `rg "APP_TEMPLATE_HTTP_MAX_URI_HANDLERS" components/app_config/app_config.h`
- [ ] `rg "/api/hardware/map|handle_api_hardware_map|hardware_map_options_for_role|nvs_store_load_pending_hardware_map" main/web_server.c`
- [ ] `rg "confirm_reboot_required|hardware_map_gpio_allowed_for_role|hardware_map_validate|nvs_store_save_pending_hardware_map|nvs_store_clear_pending_hardware_map" main/web_server.c`
- [ ] `rg "nvs_store_save_hardware_map|pump_control_init|cooling_control_init" main/web_server.c` and inspect that the hardware map POST does not call these from pending-save flow.
- [ ] `rg "is_same_origin\\(req, false\\)" main/web_server.c`
</verification>

<success_criteria>
- UI-03 safe GPIO enum options are API-visible.
- UI-04 pending GPIO saves are implemented without changing active runtime pins.
- UI-05 active map, pending map, and reboot-required state are visible.
- UI-06 server-side confirmation is required before saving pending GPIO changes.
- UI-07 hardware map state is exposed through authenticated local APIs.
</success_criteria>

<output>
After completion, create `.planning/phases/09-authenticated-hardware-and-cooling-apis/09-01-SUMMARY.md`.
</output>
