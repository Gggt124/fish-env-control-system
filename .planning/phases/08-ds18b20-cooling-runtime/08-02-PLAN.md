---
phase: 08-ds18b20-cooling-runtime
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - components/cooling_control/cooling_control.h
  - components/cooling_control/cooling_control.c
autonomous: true
requirements:
  - COOL-01
  - COOL-02
  - COOL-03
  - COOL-05
  - COOL-06
  - COOL-07
user_setup:
  - "Manual relay validation after flashing should connect the cooling relay module to the active cooling relay GPIO, default GPIO25, with polarity matching saved/default settings."
must_haves:
  truths:
    - "Plan 02 depends on Plan 01's `cooling_control` config/status contract and DS18B20 sensor state."
    - "D-06: `mode` is the main runtime control: Auto, Force OFF, and Test ON."
    - "D-07: `auto_enable` is boot preference only; boot maps true to Auto and false to Force OFF."
    - "D-08: Boot Auto with unknown temperature keeps mode Auto but relay OFF until a successful reading."
    - "D-09: Test ON may be allowed when sensor is unknown or fault for wiring verification, but must be timeout-limited and clearly reported as test override."
    - "D-10: Test ON is runtime-only, does not persist across reboot, and returns to previous mode after timeout."
    - "D-11: Boot or runtime reinit treats the cooling relay as just OFF and enforces compressor min-off before ON."
    - "D-12: Auto demand during lockout keeps relay OFF and exposes lockout status/countdown."
    - "D-13: Test ON obeys compressor min-off; blocked tests do not energize relay."
    - "D-14: Restart compressor min-off on every cooling relay ON-to-OFF transition."
    - "Do not add HTTP mutation routes, hardware-map mutation, dashboard redesign, or multi-sensor selection in this plan."
  artifacts:
    - path: "components/cooling_control/cooling_control.c"
      provides: "Cooling relay state machine with threshold/hysteresis, modes, Test ON timeout, and compressor lockout"
      contains: "compressor_lockout"
    - path: "components/cooling_control/cooling_control.h"
      provides: "Mode and blocked-reason status fields consumed by boot/API integration"
      contains: "cooling_control_blocked_reason_t"
  key_links:
    - from: "components/cooling_control/cooling_control.c"
      to: ".planning/phases/08-ds18b20-cooling-runtime/08-CONTEXT.md"
      via: "implements D-06 through D-14 cooling mode and compressor decisions"
      pattern: "test_remaining_sec"
---

<objective>
Implement cooling relay control, runtime modes, threshold/hysteresis, Test ON timeout, and compressor minimum off-time inside `cooling_control`.

Purpose: Convert the sensor/status foundation from Plan 01 into a safe cooling runtime that can drive the dedicated cooling relay without short-cycling compressor loads or confusing Test ON with automatic cooling.
Output: `cooling_control` enforces Auto / Force OFF / Test ON semantics, threshold/hysteresis relay behavior, lockout countdowns, and all relay fail-safe transitions required by COOL-02, COOL-03, COOL-05, COOL-06, and COOL-07.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-ds18b20-cooling-runtime/08-CONTEXT.md
@.planning/phases/08-ds18b20-cooling-runtime/08-RESEARCH.md
@.planning/phases/08-ds18b20-cooling-runtime/08-01-PLAN.md
@components/cooling_control/cooling_control.h
@components/cooling_control/cooling_control.c
@components/app_config/app_config.h
@components/hardware_map/hardware_map.h
@components/nvs_store/nvs_store.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cooling relay GPIO setup and inactive/fault-safe helpers</name>
  <files>components/cooling_control/cooling_control.h, components/cooling_control/cooling_control.c</files>
  <read_first>components/cooling_control/cooling_control.h, components/cooling_control/cooling_control.c, components/pump_control/pump_control.c, .planning/phases/08-ds18b20-cooling-runtime/08-CONTEXT.md</read_first>
  <action>Implement cooling relay polarity handling and GPIO setup. Configure the cooling relay output inactive before and after `gpio_config()`, and provide helpers to write active/inactive levels according to configured polarity. Force relay OFF on init failure, invalid config, sensor fault, Force OFF, compressor lockout, Test ON timeout, stop/reinit, and any GPIO write failure. Status `relay_energized` must reflect actual intended output state and never remain true after a failed write.</action>
  <verify>`rg "cooling_relay|relay_energized|gpio_config|gpio_set_level|inactive|force.*off|config_invalid" components/cooling_control`</verify>
  <acceptance_criteria>
    - Relay output cannot energize before config validation completes.
    - Invalid config and GPIO write failure fail safe with relay OFF and visible fault/block reason.
    - Relay polarity is independent and not tied to pump relay polarity.
    - Stop/reinit paths force relay inactive.
    - Relay status does not lie after a write failure.
  </acceptance_criteria>
  <done>Cooling relay GPIO output is fail-safe controlled.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Auto mode threshold and hysteresis control</name>
  <files>components/cooling_control/cooling_control.c</files>
  <read_first>components/cooling_control/cooling_control.c, components/cooling_control/cooling_control.h, .planning/REQUIREMENTS.md, .planning/phases/08-ds18b20-cooling-runtime/08-CONTEXT.md</read_first>
  <action>In Auto mode, use valid DS18B20 temperature to compute cooling demand. Energize the cooling relay only when temperature is at or above threshold and compressor lockout is clear. Once energized, keep it ON until temperature falls below `threshold - hysteresis`, then turn relay OFF. With defaults, this means ON at or above 30.0 C and OFF below 29.0 C. Unknown/fault/config-invalid sensor state must keep automatic relay output OFF and set an appropriate `blocked_reason`.</action>
  <verify>`rg "threshold|hysteresis|cooling_demand|temperature_valid|COOLING_CONTROL_MODE_AUTO|blocked_reason" components/cooling_control/cooling_control.c`</verify>
  <acceptance_criteria>
    - COOL-02 defaults are used by runtime config.
    - COOL-03 ON/OFF threshold/hysteresis rules are implemented.
    - Cooling demand can be true while relay remains OFF due to protection.
    - Sensor unknown/fault prevents automatic relay ON.
    - Hysteresis prevents rapid relay toggling around threshold.
  </acceptance_criteria>
  <done>Auto mode controls relay from threshold and hysteresis.</done>
</task>

<task type="auto">
  <name>Task 3: Implement mode semantics and Test ON timeout</name>
  <files>components/cooling_control/cooling_control.h, components/cooling_control/cooling_control.c</files>
  <read_first>components/cooling_control/cooling_control.h, components/cooling_control/cooling_control.c, .planning/phases/08-ds18b20-cooling-runtime/08-CONTEXT.md</read_first>
  <action>Represent runtime mode as Auto, Force OFF, or Test ON. At init, derive starting mode from config: `auto_enable=true` starts Auto, `auto_enable=false` starts Force OFF. If a persisted/configured mode of Test ON appears at boot, do not persist that behavior across reboot; start from a safe non-test mode consistent with auto_enable or Force OFF. Add internal APIs such as `cooling_control_set_mode()` / `cooling_control_start_test()` only if helpful for later Phase 9 integration, but do not expose HTTP mutation routes. Test ON should be runtime-only, record the previous mode, set `test_remaining_sec`, return to previous mode after timeout, and be visibly distinguishable from Auto in status. Test ON may be requested with sensor unknown/fault, but cannot bypass compressor lockout.</action>
  <verify>`rg "COOLING_CONTROL_MODE_AUTO|COOLING_CONTROL_MODE_FORCE_OFF|COOLING_CONTROL_MODE_TEST_ON|auto_enable|previous_mode|test_remaining_sec|set_mode|start_test" components/cooling_control`</verify>
  <acceptance_criteria>
    - COOL-05 auto-enable boot preference is implemented.
    - COOL-06 mode semantics are implemented at runtime.
    - Test ON does not persist across reboot/reinit.
    - Test ON returns to previous mode after timeout.
    - No HTTP POST/config route is added in Phase 8.
  </acceptance_criteria>
  <done>Cooling runtime modes and Test ON timeout behavior are implemented.</done>
</task>

<task type="auto">
  <name>Task 4: Enforce compressor minimum off-time for Auto and Test ON</name>
  <files>components/cooling_control/cooling_control.c, components/cooling_control/cooling_control.h</files>
  <read_first>components/cooling_control/cooling_control.c, components/cooling_control/cooling_control.h, .planning/phases/08-ds18b20-cooling-runtime/08-CONTEXT.md</read_first>
  <action>Implement compressor minimum off-time. At boot/reinit, set the last-off time to now so the relay is locked out for `compressor_min_off_sec` before first ON. If Auto or Test ON demands relay ON during lockout, keep relay OFF, set `lockout_active=true`, compute `lockout_remaining_sec`, keep `cooling_demand` accurate, and set `blocked_reason=compressor_lockout`. Restart min-off every time relay transitions ON to OFF, including hysteresis shutoff, Force OFF, stop/reinit, Test ON timeout, and sensor fault. Ensure `compressor_min_off_sec=0` is supported only if existing validation allows it; default remains 300 seconds.</action>
  <verify>`rg "compressor|min_off|lockout_active|lockout_remaining_sec|cooling_demand|blocked_reason|ON.*OFF|transition" components/cooling_control`</verify>
  <acceptance_criteria>
    - COOL-07 is enforced by default.
    - Boot/reinit cannot immediately energize relay before lockout expires.
    - Auto and Test ON use the same lockout rules.
    - Status explains lockout and remaining time.
    - Every ON-to-OFF transition restarts protection timing.
  </acceptance_criteria>
  <done>Compressor protection is enforced for automatic and test cooling.</done>
</task>

</tasks>

<verification>
- [ ] `rg "cooling_relay|gpio_set_level|force.*off|relay_energized" components/cooling_control`
- [ ] `rg "threshold|hysteresis|cooling_demand|temperature_valid" components/cooling_control`
- [ ] `rg "MODE_AUTO|MODE_FORCE_OFF|MODE_TEST_ON|auto_enable|previous_mode|test_remaining_sec" components/cooling_control`
- [ ] `rg "compressor|min_off|lockout_active|lockout_remaining_sec|blocked_reason" components/cooling_control`
- [ ] Inspect relay write paths to confirm unknown/fault/config-invalid/lockout states cannot energize relay.
</verification>

<success_criteria>
- COOL-02, COOL-03, COOL-05, COOL-06, and COOL-07 are implemented inside the runtime.
- Cooling relay behavior is separate from pump relays.
- Test ON is bounded, runtime-only, and compressor-protected.
- Auto mode safely handles unknown/fault sensor state and lockout.
- Status fields needed by Plan 03 are populated by the component.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ds18b20-cooling-runtime/08-02-SUMMARY.md`.
</output>
