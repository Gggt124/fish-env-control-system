---
phase: 08-ds18b20-cooling-runtime
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/cooling_control/CMakeLists.txt
  - components/cooling_control/idf_component.yml
  - components/cooling_control/cooling_control.h
  - components/cooling_control/cooling_control.c
  - dependencies.lock
autonomous: true
requirements:
  - COOL-01
  - COOL-04
user_setup:
  - "For manual validation after flashing, wire one powered-mode DS18B20 on the active DS18B20 GPIO, default GPIO33, with external 4.7 kOhm pull-up to 3.3 V."
must_haves:
  truths:
    - "D-01: Declare DS18B20 sensor fault only after 3 consecutive failed reads, not after a single missed read."
    - "D-02: Startup temperature unknown keeps cooling relay OFF and is not immediate fault; 3 startup read failures enter sensor fault."
    - "D-03: Impossible, uninitialized, or out-of-range DS18B20 values are invalid sensor inputs, not valid temperatures."
    - "D-04: Clear sensor fault only after 2 consecutive successful DS18B20 readings."
    - "D-05: In Auto mode, unknown or fault sensor state forces automatic cooling relay OFF; Test ON exception is handled in Plan 02."
    - "Use Espressif ESP-IDF-native `espressif/onewire_bus` and `espressif/ds18b20` components, not Arduino/PlatformIO libraries."
    - "Do not add cooling mutation APIs, hardware GPIO mutation APIs, dashboard redesign, or pump API changes in this plan."
  artifacts:
    - path: "components/cooling_control/cooling_control.h"
      provides: "Cooling runtime public config/status contract"
      contains: "cooling_control_status_t"
    - path: "components/cooling_control/cooling_control.c"
      provides: "DS18B20 initialization, reading, sensor state, fault threshold, and status snapshots"
      contains: "COOLING_CONTROL_SENSOR_FAULT"
    - path: "components/cooling_control/idf_component.yml"
      provides: "ESP-IDF managed dependencies for DS18B20 and 1-Wire bus"
      contains: "espressif/ds18b20"
  key_links:
    - from: "components/cooling_control/cooling_control.c"
      to: ".planning/phases/08-ds18b20-cooling-runtime/08-CONTEXT.md"
      via: "implements D-01 through D-05 sensor fault decisions"
      pattern: "consecutive"
    - from: "components/cooling_control/idf_component.yml"
      to: "REFERENCE.md"
      via: "uses official Espressif registry components recorded for provenance"
      pattern: "espressif/ds18b20"
---

<objective>
Create the cooling runtime component and implement DS18B20 sensor state handling.

Purpose: Establish the ESP-IDF-native component boundary, managed DS18B20 dependencies, public config/status types, and fail-safe temperature acquisition behavior before any relay control logic depends on sensor data.
Output: `components/cooling_control` can initialize the active DS18B20 bus, report `unknown` / `ok` / `fault` sensor state, expose stable fault codes, and maintain temperature validity with the locked 3-failure / 2-success thresholds.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-ds18b20-cooling-runtime/08-CONTEXT.md
@.planning/phases/08-ds18b20-cooling-runtime/08-RESEARCH.md
@.planning/phases/06-hardware-contract-and-persistent-map-foundation/06-01-SUMMARY.md
@.planning/phases/06-hardware-contract-and-persistent-map-foundation/06-02-SUMMARY.md
@components/app_config/app_config.h
@components/hardware_map/hardware_map.h
@components/nvs_store/nvs_store.h
@components/pump_control/pump_control.h
@components/pump_control/pump_control.c
@REFERENCE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cooling_control component shell and managed DS18B20 dependencies</name>
  <files>components/cooling_control/CMakeLists.txt, components/cooling_control/idf_component.yml, components/cooling_control/cooling_control.h, components/cooling_control/cooling_control.c, dependencies.lock</files>
  <read_first>components/pump_control/CMakeLists.txt, components/pump_control/pump_control.h, components/hardware_map/hardware_map.h, main/idf_component.yml, .planning/phases/08-ds18b20-cooling-runtime/08-RESEARCH.md</read_first>
  <action>Create `components/cooling_control/` with CMake, header, implementation, and component manifest. Declare dependencies on `espressif/onewire_bus^1.1.1` and `espressif/ds18b20^0.3.1` in the component manifest, and add CMake `REQUIRES` for `app_config`, `hardware_map`, `esp_driver_gpio`, `esp_timer`, `freertos`, `onewire_bus`, and `ds18b20` as needed by actual includes. Keep the component independent from `web_server.c`. Define public prefix-scoped types for relay polarity, mode, sensor state, fault code, blocked reason, config, and status. Do not register the component in `main` yet; that happens in Plan 03 after runtime behavior exists.</action>
  <verify>`rg "idf_component_register|cooling_control|espressif/ds18b20|espressif/onewire_bus|cooling_control_status_t|COOLING_CONTROL_SENSOR" components/cooling_control dependencies.lock`</verify>
  <acceptance_criteria>
    - New component follows existing component layout and prefix-scoped C style.
    - Managed dependencies are ESP-IDF-native and version-bounded.
    - Public header exposes config/status types without leaking web-server details.
    - Component compiles independently once linked by main in Plan 03.
    - No Arduino, PlatformIO, SPIFFS/LittleFS, WebSocket, or cloud dependency is added.
  </acceptance_criteria>
  <done>`cooling_control` component shell and dependency manifest exist.</done>
</task>

<task type="auto">
  <name>Task 2: Implement default config, validation, mutex, and status snapshot</name>
  <files>components/cooling_control/cooling_control.h, components/cooling_control/cooling_control.c</files>
  <read_first>components/cooling_control/cooling_control.h, components/cooling_control/cooling_control.c, components/app_config/app_config.h, components/hardware_map/hardware_map.h, components/nvs_store/nvs_store.h, components/pump_control/pump_control.c</read_first>
  <action>Implement `cooling_control_default_config()`, `cooling_control_init()`, `cooling_control_stop()` or equivalent safe shutdown API, and `cooling_control_get_status()`. Config must include DS18B20 GPIO, cooling relay GPIO and polarity for later tasks, threshold/hysteresis in x10 or converted float form, auto-enable, mode, test timeout, and compressor min-off seconds. Validate DS18B20 GPIO with `GPIO_IS_VALID_GPIO()`, cooling relay GPIO with `GPIO_IS_VALID_OUTPUT_GPIO()`, distinct GPIOs, valid mode/polarity, threshold/hysteresis ranges, nonzero test timeout, and bounded min-off. Use a FreeRTOS mutex for shared state and initialize status to `sensor_state=unknown`, `temperature_valid=false`, `fault=false`, `fault_code=none`, `relay_energized=false`, and `blocked_reason=none`.</action>
  <verify>`rg "cooling_control_default_config|cooling_control_init|cooling_control_get_status|sensor_state|fault_code|temperature_valid|xSemaphore" components/cooling_control`</verify>
  <acceptance_criteria>
    - Defaults match `APP_TEMPLATE_COOLING_*` and Phase 6 hardware defaults.
    - Invalid config is visible as status `fault_code=config_invalid` and cannot energize relay.
    - Status snapshots are safe to call from HTTP handlers.
    - Relay status fields exist but relay energizing remains for Plan 02.
    - Component state can be reset/reinitialized without stale temperature being treated as valid.
  </acceptance_criteria>
  <done>Cooling runtime has a validated public config/status contract.</done>
</task>

<task type="auto">
  <name>Task 3: Add DS18B20 bus initialization and single-sensor discovery</name>
  <files>components/cooling_control/cooling_control.c</files>
  <read_first>components/cooling_control/cooling_control.c, .planning/phases/08-ds18b20-cooling-runtime/08-RESEARCH.md, docs/hardware.md</read_first>
  <action>Use the Espressif `onewire_bus` RMT backend and `ds18b20` driver to initialize a 1-Wire bus on the configured DS18B20 GPIO, enumerate devices, and bind the first DS18B20 sensor handle. The project hardware contract expects one DS18B20 channel; if no DS18B20 is found, keep `sensor_state=unknown` until failed reads reach the locked fault threshold. If multiple DS18B20 devices are found, use the first discovered sensor for Phase 8 and expose a non-blocking status/debug indicator if practical; do not add multi-sensor selection. Ensure deinit/reinit cleans old bus/device handles if the selected APIs require it.</action>
  <verify>`rg "onewire_new_bus_rmt|onewire_new_device_iter|ds18b20_new_device|ds18b20_get_device_address|ds18b20" components/cooling_control/cooling_control.c`</verify>
  <acceptance_criteria>
    - DS18B20 bus uses the active configured GPIO, default GPIO33.
    - Missing sensor does not crash boot or block Wi-Fi/web startup.
    - Startup without a reading remains `unknown` and relay OFF.
    - No multi-sensor UI/API scope is introduced.
    - Any Espressif component API usage is isolated inside `cooling_control.c`.
  </acceptance_criteria>
  <done>Cooling runtime can discover the DS18B20 sensor without driving the relay.</done>
</task>

<task type="auto">
  <name>Task 4: Implement periodic temperature reads and fault/recovery thresholds</name>
  <files>components/cooling_control/cooling_control.c, components/cooling_control/cooling_control.h</files>
  <read_first>components/cooling_control/cooling_control.c, components/cooling_control/cooling_control.h, .planning/phases/08-ds18b20-cooling-runtime/08-CONTEXT.md</read_first>
  <action>Add a component-owned periodic `esp_timer` or equivalent FreeRTOS-safe scheduling pattern for DS18B20 conversion/read cycles. Trigger conversion, read temperature, and update status. Count consecutive failures and declare fault after 3 failed reads. Count consecutive successful reads after a fault and clear fault after 2 successes. Treat invalid driver returns, uninitialized 85.0 C state, NaN-like values if encountered, and values outside DS18B20's supported range as invalid sensor inputs. Do not save temperature to NVS. Keep relay OFF during `unknown` or sensor fault; Plan 02 will own relay state transitions.</action>
  <verify>`rg "esp_timer|trigger_temperature|ds18b20_get_temperature|failed_reads|successful_reads|out_of_range|read_failed|85" components/cooling_control`</verify>
  <acceptance_criteria>
    - D-01 through D-05 are implemented in source.
    - `temperature_valid` becomes true only after a successful read.
    - Fault state is stable against single transient read failures.
    - Fault recovery requires 2 consecutive successes.
    - Automatic cooling cannot use stale or invalid temperature.
  </acceptance_criteria>
  <done>DS18B20 temperature acquisition and sensor fault recovery are implemented.</done>
</task>

</tasks>

<verification>
- [ ] `rg "espressif/ds18b20|espressif/onewire_bus" components/cooling_control/idf_component.yml`
- [ ] `rg "cooling_control_default_config|cooling_control_init|cooling_control_get_status|cooling_control_status_t" components/cooling_control`
- [ ] `rg "onewire_new_bus_rmt|ds18b20_get_temperature|trigger_temperature|read_failed|out_of_range|config_invalid" components/cooling_control`
- [ ] `rg "failed_reads|successful_reads|sensor_state|temperature_valid|fault_code" components/cooling_control`
- [ ] `rg "DS18B20|onewire_bus|espressif/ds18b20" REFERENCE.md .planning/phases/08-ds18b20-cooling-runtime/08-RESEARCH.md`
</verification>

<success_criteria>
- COOL-01 has a component-level DS18B20 runtime foundation.
- COOL-04 sensor missing/unreadable behavior is represented and fail-safe.
- Startup unknown, runtime fault, and recovery thresholds match CONTEXT.md D-01 through D-05.
- The cooling relay cannot be automatically energized from invalid temperature data.
- The component is ready for relay/mode/compressor logic in Plan 02.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ds18b20-cooling-runtime/08-01-SUMMARY.md`.
</output>
