---
phase: 03-authenticated-pump-control-api
plan: 03
type: execute
wave: 3
depends_on:
  - "03-02"
files_modified:
  - main/web_server.c
  - components/app_config/app_config.h
  - build/
autonomous: true
requirements:
  - RUN-01
  - RUN-02
  - API-01
  - API-02
  - API-03
  - API-04
  - API-05
  - API-06
user_setup: []
must_haves:
  truths:
    - "All five Phase 3 routes are registered: GET/POST `/api/pump/config`, GET `/api/pump/status`, POST `/api/pump/start`, and POST `/api/pump/stop`."
    - "Route count remains within `APP_TEMPLATE_HTTP_MAX_URI_HANDLERS`; update the constant only if the final route count exceeds the current capacity."
    - "API-06 is validated across all pump routes: GET routes require session auth and POST routes require session auth plus same-origin checks."
    - "The existing `/api/status` system endpoint remains unchanged in Phase 3."
    - "No Phase 3 work adds static UI controls, frontend pump pages, editable GPIO/debounce settings, event logs, WebSocket updates, or hardware flashing."
    - "Phase 3 remains ESP-IDF build-valid and generates `build/fish_pump_relay_timer_control.bin`."
  artifacts:
    - path: "main/web_server.c"
      provides: "Registered authenticated pump API routes"
      contains: "/api/pump/start"
    - path: "build/fish_pump_relay_timer_control.bin"
      provides: "ESP-IDF build output"
      contains: "firmware binary"
    - path: ".planning/phases/03-authenticated-pump-control-api/03-03-SUMMARY.md"
      provides: "Execution summary and validation evidence"
      contains: "idf.py build"
  key_links:
    - from: "main/web_server.c"
      to: "components/app_config/app_config.h"
      via: "HTTP route capacity must fit existing plus pump routes"
      pattern: "APP_TEMPLATE_HTTP_MAX_URI_HANDLERS"
---

<objective>
Register, validate, and build-check the Phase 3 authenticated pump API.

Purpose: Ensure all pump API handlers are reachable through the ESP-IDF HTTP server, protected consistently, and compile into firmware without expanding into UI or hardware validation scope.
Output: All Phase 3 pump routes are registered, static contract checks pass, `idf.py build` succeeds, and the plan summary records coverage evidence for RUN-01, RUN-02, and API-01 through API-06.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-authenticated-pump-control-api/03-CONTEXT.md
@.planning/phases/03-authenticated-pump-control-api/03-RESEARCH.md
@.planning/phases/03-authenticated-pump-control-api/03-01-SUMMARY.md
@.planning/phases/03-authenticated-pump-control-api/03-02-SUMMARY.md
@AGENTS.md
@scripts/build.ps1
@main/web_server.c
@main/CMakeLists.txt
@components/app_config/app_config.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register pump API routes</name>
  <files>main/web_server.c, components/app_config/app_config.h</files>
  <read_first>main/web_server.c, components/app_config/app_config.h, .planning/phases/03-authenticated-pump-control-api/03-CONTEXT.md</read_first>
  <action>Add the five pump API route entries to the `routes[]` array in `web_server_start()`: `GET /api/pump/config`, `POST /api/pump/config`, `GET /api/pump/status`, `POST /api/pump/start`, and `POST /api/pump/stop`. Count the total registered routes. The current capacity is expected to be exactly enough at 18 handlers; only update `APP_TEMPLATE_HTTP_MAX_URI_HANDLERS` if the final route count is greater than the constant. Preserve existing route order and do not remove or rename existing routes.</action>
  <verify>Run `rg "/api/pump/(config|status|start|stop)|APP_TEMPLATE_HTTP_MAX_URI_HANDLERS" main/web_server.c components/app_config/app_config.h`.</verify>
  <acceptance_criteria>
    - All Phase 3 routes are registered with the correct HTTP methods.
    - Existing login, dashboard, Wi-Fi, static asset, and `/api/status` routes remain registered.
    - HTTP max URI handler capacity is not exceeded.
    - No static UI files are added or edited.
  </acceptance_criteria>
  <done>The HTTP server can dispatch all Phase 3 pump API routes.</done>
</task>

<task type="auto">
  <name>Task 2: Run API contract static checks</name>
  <files>main/web_server.c, components/app_config/app_config.h, main/static/</files>
  <read_first>.planning/phases/03-authenticated-pump-control-api/03-CONTEXT.md, .planning/REQUIREMENTS.md, main/web_server.c</read_first>
  <action>Run focused `rg` checks to confirm auth, same-origin protection, route registration, required JSON fields, idempotency flags, stable enum strings, error style, NVS save-before-apply, and no Phase 3 static UI changes. Also check that `/api/status` is not extended with pump runtime fields.</action>
  <verify>Run the commands listed in this plan's verification block.</verify>
  <acceptance_criteria>
    - Every pump route has the required auth/same-origin gate for its method.
    - Config API exposes and saves the fields required by API-01 and API-02.
    - Start/stop APIs expose idempotency fields and status snapshots.
    - Pump status exposes API-05 machine-friendly fields.
    - Existing frontend/static files remain untouched for Phase 3 unless execution documents a build-only reason.
  </acceptance_criteria>
  <done>Phase 3 API behavior is traceable before build validation.</done>
</task>

<task type="auto">
  <name>Task 3: Build validate firmware</name>
  <files>build/</files>
  <read_first>AGENTS.md, scripts/build.ps1, sdkconfig.defaults</read_first>
  <action>Run the documented ESP-IDF build workflow. Prefer `.\scripts\build.ps1` because it sets code page 65001, `PYTHONUTF8=1`, exports ESP-IDF, prints `idf.py --version`, and runs `idf.py build`. If the script is unavailable, run `chcp 65001 > $null`, export ESP-IDF from `$env:IDF_PATH` or `C:\esp-idf`, then run `idf.py build`. Do not flash hardware.</action>
  <verify>`idf.py build` succeeds and `build/fish_pump_relay_timer_control.bin` exists.</verify>
  <acceptance_criteria>
    - Build completes without C compiler, linker, or CMake errors.
    - `build/fish_pump_relay_timer_control.bin` is generated.
    - No hardware flashing or monitor session is required.
  </acceptance_criteria>
  <done>Phase 3 automated validation gate passes.</done>
</task>

<task type="auto">
  <name>Task 4: Summarize Phase 3 requirement coverage</name>
  <files>.planning/phases/03-authenticated-pump-control-api/03-03-SUMMARY.md</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/03-authenticated-pump-control-api/03-CONTEXT.md, previous Phase 3 summaries</read_first>
  <action>Create the plan summary using the established GSD summary format. Include route list, static check results, build result, and explicit requirement coverage for RUN-01, RUN-02, API-01, API-02, API-03, API-04, API-05, and API-06. Note that Phase 4 owns web UI controls and Phase 5 owns hardware validation.</action>
  <verify>Summary file exists and names all Phase 3 requirement IDs.</verify>
  <acceptance_criteria>
    - Summary records build command and result.
    - Summary records route registration and auth/same-origin evidence.
    - Summary records any deviations and compatibility fixes.
    - Summary explicitly notes that no web UI or hardware flash validation was added in Phase 3.
  </acceptance_criteria>
  <done>Phase 3 execution evidence is ready for verification.</done>
</task>

</tasks>

<verification>
- [ ] `rg "/api/pump/config|/api/pump/status|/api/pump/start|/api/pump/stop" main/web_server.c`
- [ ] `rg "require_auth\\(req\\)" main/web_server.c`
- [ ] `rg "is_same_origin\\(req\\)" main/web_server.c`
- [ ] `rg "timer1_on_sec|timer1_off_sec|timer2_on_sec|timer2_off_sec|auto_start|relay_polarity" main/web_server.c`
- [ ] `rg "float_state|active_timer|phase|countdown_sec|relay_energized|settings_status" main/web_server.c`
- [ ] `rg "already_running|already_stopped" main/web_server.c`
- [ ] `rg "/api/pump|timer1_on_sec|relay_polarity" main/static` returns no matches unless explicitly justified.
- [ ] `.\scripts\build.ps1` or documented `idf.py build` succeeds.
- [ ] `Test-Path build\fish_pump_relay_timer_control.bin` returns `True`.
</verification>

<success_criteria>
- Firmware builds successfully after Phase 3 API additions.
- Authenticated clients can read and save pump configuration.
- Authenticated clients can start and stop pump control.
- Pump status reports running state, float state, active timer, phase, countdown, relay state, and auto-start.
- Unauthenticated pump API access is rejected consistently with existing protected APIs.
</success_criteria>

<output>
After completion, create `.planning/phases/03-authenticated-pump-control-api/03-03-SUMMARY.md`.
</output>

