---
phase: 03-authenticated-pump-control-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - main/web_server.c
autonomous: true
requirements:
  - API-01
  - API-02
  - API-06
user_setup: []
must_haves:
  truths:
    - "D-01 pump APIs use the dedicated `/api/pump/*` namespace."
    - "D-02 pump config uses `GET /api/pump/config` and `POST /api/pump/config` with no separate save route."
    - "D-05 all new pump routes use protected API auth, and POST routes use same-origin checks."
    - "D-06 config read returns Timer 1/2 ON/OFF durations, auto_start, relay_polarity, and read-only GPIO/debounce defaults."
    - "D-07 timer durations in JSON use seconds-only fields."
    - "D-08 editable save fields are four timer duration fields, auto_start, and relay_polarity."
    - "D-09 hardware/debounce fields are read-only in Phase 3 and rejected if included in save requests."
    - "D-10 config save is full replacement, not partial update."
    - "D-11 save validates the complete payload, saves to NVS successfully, then applies runtime changes."
    - "D-12 save while running stops relay inactive, reinitializes, and restarts only because it was running before save."
    - "D-13 auto_start remains boot-only and changing it does not start or stop the current runtime by itself."
    - "D-18 config validation errors use HTTP 400 with ok:false."
    - "D-20 error bodies include stable error code and message."
    - "D-21 validation returns the first error only."
  artifacts:
    - path: "main/web_server.c"
      provides: "Authenticated pump config read/save handlers and shared config helpers"
      contains: "handle_api_pump_config"
  key_links:
    - from: "main/web_server.c"
      to: "components/nvs_store/nvs_store.h"
      via: "loads and saves persisted pump settings"
      pattern: "nvs_store_save_pump_settings"
    - from: "main/web_server.c"
      to: "components/pump_control/pump_control.h"
      via: "safe runtime reinitialization after saved config changes"
      pattern: "pump_control_init"
---

<objective>
Add the authenticated pump configuration API contract.

Purpose: Let authenticated local clients read and save pump settings through JSON while preserving Phase 2 save-before-apply safety and keeping GPIO/debounce fields read-only.
Output: `GET /api/pump/config` and `POST /api/pump/config` handlers exist in `main/web_server.c`, with shared helpers for pump settings JSON, validation, polarity mapping, and safe runtime apply.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-authenticated-pump-control-api/03-CONTEXT.md
@.planning/phases/03-authenticated-pump-control-api/03-RESEARCH.md
@.planning/phases/01-hardware-safe-pump-control-core/01-VERIFICATION.md
@.planning/phases/02-pump-settings-persistence-and-boot-behavior/02-VERIFICATION.md
@main/web_server.c
@components/app_config/app_config.h
@components/nvs_store/nvs_store.h
@components/pump_control/pump_control.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pump web-server helper layer</name>
  <files>main/web_server.c</files>
  <read_first>main/web_server.c, components/app_config/app_config.h, components/nvs_store/nvs_store.h, components/pump_control/pump_control.h</read_first>
  <action>Include `nvs_store.h` and `pump_control.h` if they are not already available in `web_server.c`. Add file-local helpers to map `nvs_store_pump_settings_load_status_t` to JSON strings `loaded`, `defaults_missing`, `defaults_invalid`, and `defaults_error`; map relay polarity to/from stable JSON strings `active_low` and `active_high`; copy `nvs_store_pump_settings_t` into `pump_control_config_t` starting from `pump_control_default_config()` so GPIO pins, float polarity, and debounce stay source defaults. Keep helpers file-local and prefix them with `pump_` or `api_pump_` consistently.</action>
  <verify>Run `rg "nvs_store.h|pump_control.h|settings_status|active_low|active_high|pump_control_default_config" main/web_server.c`.</verify>
  <acceptance_criteria>
    - Web server can access the pump storage and runtime APIs through existing component dependencies.
    - Settings load status strings are stable and use underscores.
    - Relay polarity JSON is a stable string, not an implementation enum value.
    - Runtime config merge starts from `pump_control_default_config()` and does not persist or edit GPIO/debounce fields.
  </acceptance_criteria>
  <done>The web layer has reusable conversion helpers for pump config responses and runtime apply.</done>
</task>

<task type="auto">
  <name>Task 2: Implement GET /api/pump/config</name>
  <files>main/web_server.c</files>
  <read_first>main/web_server.c, .planning/phases/03-authenticated-pump-control-api/03-CONTEXT.md</read_first>
  <action>Add a protected GET handler for `/api/pump/config`. It must call `require_auth(req)` first and return `401 Unauthorized` with `{"ok":false,"error":"unauthorized"}` if authentication fails. Load settings through `nvs_store_load_pump_settings()`, then return `ok:true` with seconds-only timer fields, `auto_start`, `relay_polarity`, read-only `float_gpio`, `relay_gpio`, `debounce_ms`, and `settings_status`. Use cJSON and `send_json()`; return an internal error status only if JSON allocation or settings loading cannot produce a response.</action>
  <verify>Run `rg "handle_api_pump_config|timer1_on_sec|timer2_off_sec|float_gpio|debounce_ms|settings_status" main/web_server.c`.</verify>
  <acceptance_criteria>
    - Unauthenticated config read is rejected consistently with existing protected APIs.
    - Response includes all API-01 fields.
    - Timer durations are represented only as seconds.
    - GPIO/debounce defaults are visible but clearly read-only by contract.
    - Existing `/api/status` is unchanged.
  </acceptance_criteria>
  <done>Authenticated clients can read the full Phase 3 pump config contract.</done>
</task>

<task type="auto">
  <name>Task 3: Implement POST /api/pump/config</name>
  <files>main/web_server.c</files>
  <read_first>main/web_server.c, components/nvs_store/nvs_store.c, components/pump_control/pump_control.c, .planning/phases/03-authenticated-pump-control-api/03-CONTEXT.md</read_first>
  <action>Add a protected POST handler for `/api/pump/config`. It must call `require_auth(req)` and `is_same_origin(req)` before mutation. Parse JSON as a full replacement payload requiring `timer1_on_sec`, `timer1_off_sec`, `timer2_on_sec`, `timer2_off_sec`, `auto_start`, and `relay_polarity`. If `float_gpio`, `relay_gpio`, or `debounce_ms` appears, reject with HTTP 400 and a read-only-field error. Validate timer durations against `APP_TEMPLATE_PUMP_TIMER_MIN_SEC` and `APP_TEMPLATE_PUMP_TIMER_MAX_SEC`, validate booleans and relay polarity, and return only the first validation error. Save with `nvs_store_save_pump_settings()` before changing runtime state. After save succeeds, read whether pump control is currently running, stop to force relay inactive, initialize the new runtime config, and restart only if it was running before the save. Return `ok:true` with the saved config and a pump status snapshot if available. If NVS save fails, return HTTP 500 and leave runtime untouched. If runtime apply fails after save, fail safe with relay inactive where possible and return `ok:false` with a stable error code.</action>
  <verify>Run `rg "readonly|read_only|duration_out_of_range|nvs_store_save_pump_settings|pump_control_init|pump_control_start|pump_control_stop" main/web_server.c`.</verify>
  <acceptance_criteria>
    - Save is full replacement and rejects missing editable fields.
    - Read-only GPIO/debounce fields are rejected instead of ignored.
    - Invalid durations or polarity return HTTP 400 with `ok:false`.
    - NVS save failure returns HTTP 500 and does not mutate runtime state.
    - Runtime apply happens only after save succeeds.
    - Changing `auto_start` alone does not start or stop the pump unless the pump was already running and must be restarted after reinit.
  </acceptance_criteria>
  <done>Authenticated clients can safely save validated pump configuration.</done>
</task>

</tasks>

<verification>
- [ ] `rg "GET /api/pump/config|POST /api/pump/config|handle_api_pump_config" main/web_server.c`
- [ ] `rg "require_auth\\(req\\)|is_same_origin\\(req\\)" main/web_server.c`
- [ ] `rg "timer1_on_sec|timer1_off_sec|timer2_on_sec|timer2_off_sec|auto_start|relay_polarity" main/web_server.c`
- [ ] `rg "float_gpio|relay_gpio|debounce_ms|read_only" main/web_server.c`
- [ ] `rg "nvs_store_save_pump_settings|pump_control_init|pump_control_stop|pump_control_start" main/web_server.c`
</verification>

<success_criteria>
- API-01 is covered by authenticated config read.
- API-02 is covered by validated full-replacement config save.
- API-06 is covered for config routes through existing auth and same-origin patterns.
- Save-before-apply behavior preserves Phase 2 persistence safety.
- Phase 3 does not add web UI controls or editable GPIO/debounce settings.
</success_criteria>

<output>
After completion, create `.planning/phases/03-authenticated-pump-control-api/03-01-SUMMARY.md`.
</output>

