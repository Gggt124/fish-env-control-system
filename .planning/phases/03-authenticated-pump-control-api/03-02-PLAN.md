---
phase: 03-authenticated-pump-control-api
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - main/web_server.c
autonomous: true
requirements:
  - RUN-01
  - RUN-02
  - API-03
  - API-04
  - API-05
  - API-06
user_setup: []
must_haves:
  truths:
    - "D-03 pump runtime status lives at `GET /api/pump/status`; Phase 3 does not extend `GET /api/status`."
    - "D-04 start and stop are separate routes: `POST /api/pump/start` and `POST /api/pump/stop`."
    - "D-05 all new pump routes use protected API auth, and POST routes use same-origin checks."
    - "D-14 start is idempotent and does not reset countdown or timer state when already running."
    - "D-15 stop is idempotent and keeps the relay inactive when already stopped."
    - "D-16 successful start/stop responses include a pump status snapshot."
    - "D-17 start failure fails safe by calling or confirming stop/relay inactive, then returns ok:false with a stable error code and status snapshot if possible."
    - "D-19 runtime/internal failures use meaningful HTTP statuses, including 500 or 409 where appropriate."
    - "D-20 error bodies include stable error code and message."
    - "D-22 pump status returns machine-friendly fields."
    - "D-23 enum-like JSON values are lowercase stable strings."
    - "D-24 pump status includes runtime fields plus auto_start and does not duplicate full timer duration config."
    - "D-25 pump status includes settings_status reflecting Phase 2 load health."
  artifacts:
    - path: "main/web_server.c"
      provides: "Authenticated pump status, start, and stop handlers"
      contains: "handle_api_pump_status"
  key_links:
    - from: "main/web_server.c"
      to: "components/pump_control/pump_control.h"
      via: "runtime start, stop, and status snapshot APIs"
      pattern: "pump_control_get_status"
    - from: "main/web_server.c"
      to: "components/nvs_store/nvs_store.h"
      via: "status response includes auto_start and settings_status"
      pattern: "nvs_store_load_pump_settings"
---

<objective>
Add authenticated pump runtime status, start, and stop APIs.

Purpose: Give authenticated local clients machine-friendly runtime snapshots and safe idempotent controls for starting and stopping pump control.
Output: `GET /api/pump/status`, `POST /api/pump/start`, and `POST /api/pump/stop` handlers exist in `main/web_server.c`, reuse the shared status serializer, and preserve existing auth and same-origin protections.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-authenticated-pump-control-api/03-CONTEXT.md
@.planning/phases/03-authenticated-pump-control-api/03-RESEARCH.md
@.planning/phases/03-authenticated-pump-control-api/03-01-SUMMARY.md
@.planning/phases/01-hardware-safe-pump-control-core/01-VERIFICATION.md
@.planning/phases/02-pump-settings-persistence-and-boot-behavior/02-VERIFICATION.md
@main/web_server.c
@components/nvs_store/nvs_store.h
@components/pump_control/pump_control.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pump status serialization helper</name>
  <files>main/web_server.c</files>
  <read_first>main/web_server.c, components/pump_control/pump_control.h, .planning/phases/03-authenticated-pump-control-api/03-CONTEXT.md</read_first>
  <action>Add file-local helpers that convert `pump_control_float_state_t`, `pump_control_active_timer_t`, and `pump_control_timer_phase_t` to stable lowercase JSON strings. Add a shared status JSON builder that reads `pump_control_get_status()` and `nvs_store_load_pump_settings()`, then appends `running`, `initialized`, `config_valid`, `initial_stabilizing`, `float_state`, `active_timer`, `phase`, `countdown_sec`, `relay_energized`, `float_gpio`, `relay_gpio`, `auto_start`, and `settings_status`. Do not include full timer duration config in the status object.</action>
  <verify>Run `rg "float_state|active_timer|countdown_sec|relay_energized|settings_status|initial_stabilizing" main/web_server.c`.</verify>
  <acceptance_criteria>
    - Status fields are booleans, seconds, numbers, and stable lowercase strings.
    - `auto_start` and `settings_status` come from the storage load contract.
    - Full timer duration settings remain exclusive to `/api/pump/config`.
    - The helper is reusable by status, start, stop, and config-save responses.
  </acceptance_criteria>
  <done>Pump runtime snapshots have a single serializer.</done>
</task>

<task type="auto">
  <name>Task 2: Implement GET /api/pump/status</name>
  <files>main/web_server.c</files>
  <read_first>main/web_server.c, .planning/phases/03-authenticated-pump-control-api/03-CONTEXT.md</read_first>
  <action>Add a protected GET handler for `/api/pump/status`. It must call `require_auth(req)` first and return `401 Unauthorized` if authentication fails. On success, return `ok:true` and the machine-friendly pump status fields from the shared serializer. If status cannot be read or JSON allocation fails, return `ok:false` with a meaningful stable error code and HTTP 500 or 409 as appropriate. Do not alter the existing `/api/status` handler.</action>
  <verify>Run `rg "handle_api_pump_status|/api/pump/status|handle_api_status" main/web_server.c` and confirm the existing system status handler is not extended with pump fields.</verify>
  <acceptance_criteria>
    - Unauthenticated status access is rejected.
    - Authenticated status response covers API-05 fields.
    - Existing `/api/status` remains a system/Wi-Fi status endpoint only.
    - Response is suitable for Phase 4 UI rendering without label parsing.
  </acceptance_criteria>
  <done>Authenticated clients can poll pump runtime status.</done>
</task>

<task type="auto">
  <name>Task 3: Implement POST /api/pump/start</name>
  <files>main/web_server.c</files>
  <read_first>main/web_server.c, components/pump_control/pump_control.c, .planning/phases/03-authenticated-pump-control-api/03-CONTEXT.md</read_first>
  <action>Add a protected POST handler for `/api/pump/start`. It must call `require_auth(req)` and `is_same_origin(req)` before mutation. Read a status snapshot before starting. If already running, return `ok:true`, `already_running:true`, and a status snapshot without calling `pump_control_start()`. If not running, call `pump_control_start()`. On success, return `ok:true`, `already_running:false`, and a fresh status snapshot. On failure, call or confirm `pump_control_stop()` for fail-safe relay inactive behavior, then return `ok:false`, a stable error code such as `start_failed`, a short message, and a best-effort status snapshot.</action>
  <verify>Run `rg "handle_api_pump_start|already_running|start_failed|pump_control_start|pump_control_stop" main/web_server.c`.</verify>
  <acceptance_criteria>
    - Start route is authenticated and same-origin protected.
    - Calling start while already running does not reset countdown or timer state.
    - Successful start returns a status snapshot.
    - Failed start fails safe and reports a stable error body.
  </acceptance_criteria>
  <done>Authenticated clients can start pump control idempotently.</done>
</task>

<task type="auto">
  <name>Task 4: Implement POST /api/pump/stop</name>
  <files>main/web_server.c</files>
  <read_first>main/web_server.c, components/pump_control/pump_control.c, .planning/phases/03-authenticated-pump-control-api/03-CONTEXT.md</read_first>
  <action>Add a protected POST handler for `/api/pump/stop`. It must call `require_auth(req)` and `is_same_origin(req)` before mutation. Read a status snapshot before stopping. If already stopped, return `ok:true`, `already_stopped:true`, and a status snapshot while keeping the relay inactive. If running, call `pump_control_stop()` and return `ok:true`, `already_stopped:false`, and a fresh status snapshot. If stop fails because pump control is not ready, return `ok:false` with a stable error code and meaningful HTTP status; do not report success unless the relay is inactive or the component already indicates stopped.</action>
  <verify>Run `rg "handle_api_pump_stop|already_stopped|stop_failed|pump_control_stop" main/web_server.c`.</verify>
  <acceptance_criteria>
    - Stop route is authenticated and same-origin protected.
    - Calling stop while already stopped returns success and keeps relay inactive.
    - Successful stop returns a status snapshot.
    - Stop failure returns a stable error body instead of ambiguous success.
  </acceptance_criteria>
  <done>Authenticated clients can stop pump control idempotently.</done>
</task>

</tasks>

<verification>
- [ ] `rg "handle_api_pump_status|handle_api_pump_start|handle_api_pump_stop" main/web_server.c`
- [ ] `rg "float_state|active_timer|phase|countdown_sec|relay_energized|auto_start|settings_status" main/web_server.c`
- [ ] `rg "already_running|already_stopped|start_failed|stop_failed" main/web_server.c`
- [ ] `rg "is_same_origin\\(req\\)" main/web_server.c`
- [ ] Confirm `/api/status` still contains only the existing system/Wi-Fi status response.
</verification>

<success_criteria>
- RUN-01 and API-03 are covered by authenticated idempotent start.
- RUN-02 and API-04 are covered by authenticated idempotent stop.
- API-05 is covered by machine-friendly pump runtime status.
- API-06 is covered for action routes through existing auth and same-origin patterns.
- Start failure behavior is fail-safe and returns a stable error response.
</success_criteria>

<output>
After completion, create `.planning/phases/03-authenticated-pump-control-api/03-02-SUMMARY.md`.
</output>

