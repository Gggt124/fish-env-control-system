---
phase: 06-hardware-contract-and-persistent-map-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
files_modified:
  - main/CMakeLists.txt
  - main/app_main.c
  - main/web_server.c
  - components/pump_control/pump_control.h
  - components/pump_control/pump_control.c
  - components/nvs_store/nvs_store.h
  - components/nvs_store/nvs_store.c
  - docs/components.md
  - docs/development-notes.md
autonomous: true
requirements:
  - HW-01
  - HW-02
  - HW-03
  - HW-04
  - HW-05
user_setup: []
must_haves:
  truths:
    - "Plan 03 consumes Plan 01 hardware defaults and Plan 02 persistence, but still must not implement Phase 7 dual-relay pump switching or Phase 8 cooling runtime."
    - "Existing v1.0 behavior remains single pump relay control; active hardware map Relay 1 maps to the current relay output until Phase 7 changes runtime behavior."
    - "Existing `/api/pump/config` remains the only current config route; Phase 9 owns new hardware/cooling APIs."
    - "If persisted hardware map or pump/cooling settings are invalid, boot must fail safe to defaults and suppress unsafe auto-start where appropriate."
    - "Build validation through ESP-IDF is required for phase planning success."
  artifacts:
    - path: "main/app_main.c"
      provides: "Boot-time consumption of active hardware map and expanded pump settings"
      contains: "nvs_store_load_hardware_map"
    - path: "main/web_server.c"
      provides: "Existing pump config/status responses reflect active hardware defaults without opening mutation routes"
      contains: "relay1"
    - path: "docs/development-notes.md"
      provides: "Implementation notes for later Phase 7-10 agents"
      contains: "Phase 6"
  key_links:
    - from: "main/app_main.c"
      to: "components/nvs_store/nvs_store.h"
      via: "loads active hardware map, pump settings, and safe defaults at boot"
      pattern: "nvs_store_load_hardware_map"
    - from: "main/app_main.c"
      to: "components/pump_control/pump_control.h"
      via: "maps active Relay 1 and float GPIO into current single-relay pump runtime"
      pattern: "pump_control_config_t"
---

<objective>
Integrate the hardware contract foundation without changing runtime control semantics.

Purpose: Make active hardware map and expanded settings visible to boot/config/status code so the foundation is real and build-verified, while leaving dual pump relays, DS18B20 readings, cooling control, and installer APIs to later phases.
Output: Current firmware boots with active/default hardware map, existing pump config/status responses reflect the active map and compatibility fields, docs capture integration notes, and ESP-IDF build passes.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-hardware-contract-and-persistent-map-foundation/06-RESEARCH.md
@.planning/phases/06-hardware-contract-and-persistent-map-foundation/06-01-PLAN.md
@.planning/phases/06-hardware-contract-and-persistent-map-foundation/06-02-PLAN.md
@components/hardware_map/hardware_map.h
@components/hardware_map/hardware_map.c
@components/pump_control/pump_control.h
@components/pump_control/pump_control.c
@components/nvs_store/nvs_store.h
@components/nvs_store/nvs_store.c
@main/CMakeLists.txt
@main/app_main.c
@main/web_server.c
@docs/components.md
@docs/development-notes.md
@scripts/build.ps1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add component dependencies for integration points</name>
  <files>main/CMakeLists.txt, components/pump_control/CMakeLists.txt</files>
  <read_first>main/CMakeLists.txt, components/pump_control/CMakeLists.txt, components/hardware_map/CMakeLists.txt</read_first>
  <action>Add `hardware_map` to `main` component requirements because `app_main.c` and `web_server.c` will consume hardware map types. Add `hardware_map` to `pump_control` only if the implementation directly consumes shared polarity or map types; otherwise keep pump runtime independent for now. Do not add unused dependencies.</action>
  <verify>`rg "hardware_map" main/CMakeLists.txt components/pump_control/CMakeLists.txt components/nvs_store/CMakeLists.txt`</verify>
  <acceptance_criteria>
    - CMake dependencies match actual includes.
    - No circular component dependency is introduced.
    - `web_server` remains in `main` because it owns embedded file symbols.
  </acceptance_criteria>
  <done>Build graph supports the new hardware foundation cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Load active map and expanded settings at boot</name>
  <files>main/app_main.c</files>
  <read_first>main/app_main.c, components/nvs_store/nvs_store.h, components/hardware_map/hardware_map.h, components/pump_control/pump_control.h</read_first>
  <action>Update boot logic to load the active hardware map through `nvs_store_load_hardware_map()` before creating the pump runtime config. Merge the active float GPIO and pump Relay 1 GPIO into the existing `pump_control_config_t`; Relay 2 and cooling relay remain stored and logged but unused by runtime until later phases. Merge Relay 1 polarity into the existing single-relay runtime. If hardware map or pump settings are invalid, use defaults and suppress auto-start for that boot. Log active map and pending/reboot-required status without exposing raw unsafe values.</action>
  <verify>`rg "nvs_store_load_hardware_map|hardware_map_defaults|pump_relay1|pump_relay2|ds18b20|cooling|auto_start" main/app_main.c`</verify>
  <acceptance_criteria>
    - Existing GPIO32/GPIO26 behavior is preserved when no NVS hardware map exists.
    - Invalid persisted hardware map does not initialize pump runtime with unsafe pins.
    - Relay 2 and cooling relay are not driven in this phase.
    - Auto-start is suppressed if persisted hardware or pump settings are invalid.
    - Boot logs are useful for manual validation without creating new behavior.
  </acceptance_criteria>
  <done>Boot consumes active/default hardware map safely.</done>
</task>

<task type="auto">
  <name>Task 3: Keep existing pump config/status API compatible while reflecting active map</name>
  <files>main/web_server.c</files>
  <read_first>main/web_server.c, components/nvs_store/nvs_store.h, components/hardware_map/hardware_map.h, components/pump_control/pump_control.h</read_first>
  <action>Update existing pump config/status helper code so read-only GPIO fields come from the active hardware map or safe defaults instead of hardcoded `pump_control_default_config()` only. Preserve current v1.0 JSON fields (`float_gpio`, `relay_gpio`, `relay_polarity`) for compatibility. Add additive read-only fields only if low-risk, such as `pump_relay1_gpio`, `pump_relay2_gpio`, `ds18b20_gpio`, `cooling_relay_gpio`, `relay1_polarity`, `relay2_polarity`, `cooling_relay_polarity`, `timer1_start_phase`, `timer2_start_phase`, and `hardware_reboot_required`. Do not accept these fields in POST `/api/pump/config` yet unless they are existing editable fields; reject hardware/cooling fields as read-only until Phase 9.</action>
  <verify>`rg "pump_relay1_gpio|pump_relay2_gpio|ds18b20_gpio|cooling_relay_gpio|hardware_reboot_required|read_only|timer1_start_phase|timer2_start_phase" main/web_server.c`</verify>
  <acceptance_criteria>
    - Existing dashboard/client code remains compatible.
    - Existing save payloads still work.
    - Hardware map fields are read-only in current API.
    - No new hardware mutation route is added.
    - Auth and same-origin behavior for existing POST routes remains unchanged.
  </acceptance_criteria>
  <done>Current API surfaces the foundation without expanding mutation scope.</done>
</task>

<task type="auto">
  <name>Task 4: Verify storage migration and invalid-data behavior</name>
  <files>components/nvs_store/nvs_store.c, components/nvs_store/nvs_store.h, .planning/phases/06-hardware-contract-and-persistent-map-foundation/06-03-SUMMARY.md</files>
  <read_first>components/nvs_store/nvs_store.c, components/nvs_store/nvs_store.h, .planning/phases/06-hardware-contract-and-persistent-map-foundation/06-02-PLAN.md</read_first>
  <action>Run focused source checks for all new NVS namespaces and compatibility branches. Confirm that legacy `relay_low` remains readable, missing new keys default safely, invalid bool/enum values are rejected, and active/pending map validation calls `hardware_map_validate()`. Record the checks and findings in the summary. If the implementation accidentally treats pending map as active immediately, fix it before continuing.</action>
  <verify>`rg "relay_low|r1_low|r2_low|t1_start|t2_start|hardware_map_validate|pend_valid|threshold_x10|min_off_s" components/nvs_store/nvs_store.c`</verify>
  <acceptance_criteria>
    - Active map and pending map are distinct in code.
    - Pending values do not affect runtime before reboot/activation.
    - Legacy settings load path is explicit.
    - Invalid persisted values do not silently become active hardware config.
  </acceptance_criteria>
  <done>Persistence behavior is source-verified and documented in the summary.</done>
</task>

<task type="auto">
  <name>Task 5: Update development notes and run ESP-IDF build</name>
  <files>docs/components.md, docs/development-notes.md, .planning/phases/06-hardware-contract-and-persistent-map-foundation/06-03-SUMMARY.md, build/</files>
  <read_first>docs/components.md, docs/development-notes.md, scripts/build.ps1, AGENTS.md</read_first>
  <action>Update component/development notes with the Phase 6 contract: `hardware_map` owns safe options, `nvs_store` owns raw schemas, current pump runtime consumes only float and Relay 1, and later phases own dual-relay/cooling/UI behavior. Run `.\scripts\build.ps1` using the documented ESP-IDF workflow. If ESP-IDF is unavailable, record the exact blocker and do not substitute Arduino, PlatformIO, or a generic C compiler. Confirm whether `build\fish_pump_relay_timer_control.bin` exists after the build attempt.</action>
  <verify>`.\scripts\build.ps1`; then `Test-Path build\fish_pump_relay_timer_control.bin`; then `rg "Phase 6|hardware_map|active hardware map|pending hardware map|Relay 2|cooling relay" docs/components.md docs/development-notes.md`</verify>
  <acceptance_criteria>
    - ESP-IDF build passes, or the summary records the exact environment blocker.
    - Documentation gives Phase 7-10 agents clear handoff notes.
    - No relay/cooling behavior is added without a later phase plan.
    - Summary maps each HW requirement to implemented files.
  </acceptance_criteria>
  <done>Phase 6 is build-verified and ready for verification.</done>
</task>

</tasks>

<verification>
- [ ] `rg "hardware_map" main/CMakeLists.txt components/nvs_store/CMakeLists.txt`
- [ ] `rg "nvs_store_load_hardware_map|hardware_reboot_required|pump_relay1|pump_relay2|ds18b20|cooling" main/app_main.c main/web_server.c`
- [ ] `rg "read_only|float_gpio|relay_gpio|pump_relay1_gpio|pump_relay2_gpio|cooling_relay_gpio" main/web_server.c`
- [ ] `rg "relay_low|r1_low|r2_low|t1_start|t2_start|pend_valid|threshold_x10|min_off_s" components/nvs_store/nvs_store.c`
- [ ] `rg "HW-01|HW-02|HW-03|HW-04|HW-05" .planning/phases/06-hardware-contract-and-persistent-map-foundation/06-03-SUMMARY.md`
- [ ] `.\scripts\build.ps1`
- [ ] `Test-Path build\fish_pump_relay_timer_control.bin`
</verification>

<success_criteria>
- Existing single-relay pump behavior remains equivalent when active map is default.
- Firmware loads and reports the active hardware map foundation safely.
- Existing pump config/status API remains compatible while exposing read-only foundation fields where appropriate.
- Documentation and summary prove coverage for HW-01 through HW-05.
- ESP-IDF build validates the integrated foundation.
</success_criteria>

<output>
After completion, create `.planning/phases/06-hardware-contract-and-persistent-map-foundation/06-03-SUMMARY.md`.
</output>

