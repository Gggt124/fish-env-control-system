---
phase: 18-modern-app-shell
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - main/static/index.html
  - main/static/app.js
autonomous: true
requirements:
  - ARCH-UI-01
  - ARCH-UI-03
must_haves:
  truths:
    - The SPA shell layout encapsulates the application.
    - Navigation uses client-side routing without full page reloads.
  artifacts:
    - path: "main/static/index.html"
      provides: "Unified application layout and view containers"
    - path: "main/static/app.js"
      provides: "Client-side SPA routing logic"
  key_links:
    - from: "main/static/app.js"
      to: "main/static/index.html"
      via: "DOM manipulation toggling .hidden class on view containers"
---

<objective>
Create the unified SPA shell HTML structure and establish vanilla JavaScript routing logic, moving away from multi-page document loads to seamless view transitions.

Purpose: ARCH-UI-01 requires a base HTML structure with sidebar, mobile header, and main content wrapper.
Output: New `index.html` skeleton and updated `app.js` with routing logic.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-modern-app-shell/18-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SPA Layout Skeleton</name>
  <files>main/static/index.html</files>
  <action>Create `index.html`. Scaffold the HTML5 boilerplate using Thai lang attribute. Extract the `<header class="mobile-topbar">`, `<div class="drawer-overlay">`, and `<aside class="app-sidebar">` from the current dashboard.html and place them into `index.html`. Add a `<main id="app-content">` wrapper with a max-width constraint (e.g., 1200px) and margin-auto centering. Inside `<main>`, add empty placeholder section tags for each view: `<section id="view-login" class="view hidden">`, `#view-dashboard`, `#view-status`, `#view-wifi`, `#view-hardware`. Do not load any external CDNs, use inline SVGs/unicode for icons as per ARCH-UI-03.</action>
  <verify>
    <automated>grep -c "id=\"app-content\"" main/static/index.html > /dev/null</automated>
  </verify>
  <done>index.html exists with the required app shell DOM structure and hidden view sections.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Client-Side SPA Routing</name>
  <files>main/static/app.js</files>
  <action>Update `app.js` to implement vanilla SPA routing. Create a `function navigateTo(path)` that uses `history.pushState` and updates the view. Create `function handleRoute()` that reads `window.location.pathname`, hides all `.view` sections, unhides the section matching the path (e.g., `/dashboard` unhides `#view-dashboard`), and updates the `.active` class on sidebar links. Attach `handleRoute` to `window.addEventListener('popstate')`. Modify sidebar click events to use `navigateTo(path)` instead of standard link navigation to prevent full page reloads.</action>
  <verify>
    <automated>grep -c "history.pushState" main/static/app.js > /dev/null</automated>
  </verify>
  <done>app.js correctly handles pushState routing and view toggling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client Browser | DOM manipulation strictly on client side; routing does not bypass server API auth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-02-JS | Tampering | app.js | mitigate | Client-side routing still relies on server endpoints enforcing auth checks via cookies. |
</threat_model>

<verification>
- `index.html` structure passes validation.
- `app.js` contains `history.pushState` routing handlers.
</verification>

<success_criteria>
- SPA logic prepares the frontend for single-page operation.
- No external CDNs are present in index.html.
</success_criteria>

<output>
Create `.planning/phases/18-modern-app-shell/18-02-SUMMARY.md` when done
</output>
