---
phase: 18-modern-app-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - main/static/style.css
autonomous: true
requirements:
  - ARCH-UI-02
must_haves:
  truths:
    - CSS utilizes HSL variables for programmatic color themes.
    - System typography uses a Major Third (1.25) scale.
    - 4px/8px baseline grid is implemented for spacing.
  artifacts:
    - path: "main/static/style.css"
      provides: "Design system variables and rules"
  key_links:
    - from: "main/static/style.css"
      to: "CSS variables"
      via: ":root and @media (prefers-color-scheme: dark)"
---

<objective>
Refactor the CSS design system to use modern HSL variables, a 1.25 typography scale, and a 4px/8px spacing grid, establishing the foundation for dark mode and structural consistency.

Purpose: ARCH-UI-02 requires CSS variables (HSL colors, radius, shadows) and system font stack.
Output: Updated `style.css` with a robust `:root` design token structure.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-modern-app-shell/18-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement CSS Design Tokens</name>
  <files>main/static/style.css</files>
  <action>Refactor `:root` in `style.css` to use HSL color variables (e.g., `--primary-h`, `--primary-s`, `--primary-l`) so alpha and lightness can be modified programmatically. Introduce a typography scale based on a 1.25 ratio (Major Third) with `--text-base: 1rem;`, `--text-lg: 1.25rem;`, etc. Update the font stack to use system-ui (`system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto`). Implement a 4px/8px spacing grid using `--space-1: 0.25rem`, `--space-2: 0.5rem`, etc. Preserve existing class names but update their property values to reference these new variables.</action>
  <verify>
    <automated>grep -c "prefers-color-scheme" main/static/style.css > /dev/null</automated>
  </verify>
  <done>CSS variables for colors, typography, and spacing are defined in `:root` and applied throughout the stylesheet.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Dark Mode Media Query</name>
  <files>main/static/style.css</files>
  <action>Add an `@media (prefers-color-scheme: dark)` block at the bottom of the variable definitions to override the HSL lightness values for background surfaces, text, and outlines. Ensure the existing design maintains its legibility in both modes.</action>
  <verify>
    <automated>grep -c "@media.*prefers-color-scheme.*dark" main/static/style.css > /dev/null</automated>
  </verify>
  <done>Dark mode overrides are established.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client Browser | CSS variables execute purely in the presentation layer; no security boundary crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01-CSS | Spoofing | style.css | accept | Static CSS file served without dynamic injection; no risk. |
</threat_model>

<verification>
- `idf.py build` succeeds
- `style.css` uses HSL variables and system-ui font stack
</verification>

<success_criteria>
- CSS structure meets ARCH-UI-02.
- Dark mode preference toggles color schema.
</success_criteria>

<output>
Create `.planning/phases/18-modern-app-shell/18-01-SUMMARY.md` when done
</output>
