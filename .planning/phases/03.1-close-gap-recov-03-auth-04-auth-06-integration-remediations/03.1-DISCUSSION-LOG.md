# Phase 03.1: Close gap: RECOV-03 & AUTH-04 & AUTH-06 — Integration remediations - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-14
**Phase:** 03.1-Close gap: RECOV-03 & AUTH-04 & AUTH-06 — Integration remediations
**Areas discussed:** IP Binding Validation (AUTH-04), Global Session Invalidation (AUTH-06), Wi-Fi Staging and Reboot Flow (RECOV-03), Staging Commits & NVS Integrity (RECOV-03)

---

## IP Binding Validation (AUTH-04)

| Option | Description | Selected |
|--------|-------------|----------|
| Option A | Remove the IP check completely inside `session_validate()` while keeping the parameter for compatibility. | ✓ |
| Option B | Keep the IP check as-is. | |

**User's choice:** Option A.
**Notes:** User agreed to remove the IP binding check to prevent connection drops during fallback transitions between SoftAP and STA.

---

## Global Session Invalidation (AUTH-06)

| Option | Description | Selected |
|--------|-------------|----------|
| Option A | Add `session_invalidate_others()` to terminate all other active sessions except the current one upon credential confirm. | ✓ |
| Option B | Terminate all active sessions unconditionally, forcing the user to log in again. | |

**User's choice:** Option A.
**Notes:** User agreed to invalidate all other active sessions while maintaining the current session to avoid the double-login bounce UX issue.

---

## Wi-Fi Staging and Reboot Flow (RECOV-03)

| Option | Description | Selected |
|--------|-------------|----------|
| Option A | Bypass staging for Wi-Fi profile saves completely. Wi-Fi connections/profiles save instantly and directly. | ✓ |
| Option B | Staging and reboot flow applied to all Wi-Fi changes. | |

**User's choice:** Option A.
**Notes:** User clarified that Wi-Fi connects/profile saves must bypass staging and be instant/direct without confirmation, according to the latest design choice. Only credentials changes use staging.

---

## Staging Commits & NVS Integrity (RECOV-03)

| Option | Description | Selected |
|--------|-------------|----------|
| Option A | Fix missing `nvs_commit()` in `nvs_store_commit_staging()` for credentials. | ✓ |
| Option B | Retain current implementation without explicit commit. | |

**User's choice:** Option A.
**Notes:** Fixes the bug where credential changes were lost after a reboot due to the missing `nvs_commit()`.

---

## the agent's Discretion
None.

## Deferred Ideas
None.
