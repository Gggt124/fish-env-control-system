---
context: phase
phase: 03-hardware-recovery-anti-lockout
task: 3
total_tasks: 3
status: paused
last_updated: 2026-06-14T15:21:50+07:00
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: GPIO 0 Strapping Conflict — Pressing BOOT button (GPIO 0) during power-on/reset enters download mode — Bootloader veto ignores download mode holds to prevent accidental factory resets.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Double Redirect | Invalidating sessions unconditionally on staging confirmation causes double redirects. | advisory | Preserve authenticated session when staging is confirmed. |
| Redundant Staging | Connect requests for known/saved Wi-Fi profiles trigger confirmation flow. | advisory | Check existing NVS profiles and bypass staging if profile matches exactly. |

<current_state>
We have successfully executed the Phase 3 Gap Closure (Plan 05) to fix the UX issues with the staging confirmation banner. The firmware has been flashed to the ESP32. We are currently paused waiting for the user to perform the manual UAT (Test 2: Verify Staging Confirmation UI Banner).
</current_state>

<completed_work>

Completed Tasks:
- Task 1: Replace alert() with showToast() in app.js - Done
- Task 2: Prevent unconditional session invalidation - Done
- Task 3: Skip staging for known Wi-Fi profiles - Done
</completed_work>

<remaining_work>

- Perform manual UAT on the physical hardware (specifically Test 2: Verify Staging Confirmation UI Banner).
- Close out Phase 3 upon successful UAT verification.
</remaining_work>

<decisions_made>

- Replaced blocking browser alert() with showToast() to avoid blocking UI.
- Removed unconditional session invalidation upon staging confirmation to prevent double redirects.
- Skipped staging for Wi-Fi profiles that match existing credentials in NVS.
</decisions_made>

<blockers>
None
</blockers>

## Required Reading (in order)
1. `.planning/phases/03-hardware-recovery-anti-lockout/03-UAT.md` — To see the UAT test cases (specifically Test 2)
2. `.planning/phases/03-hardware-recovery-anti-lockout/03-05-SUMMARY.md` — Summary of what was just fixed in Plan 05

## Infrastructure State
- Firmware: Built and flashed to COM5

<context>
The gap closure execution completed successfully and the build succeeded. We are currently waiting for the user to perform the manual UAT. The user requested to pause the work.
</context>

<next_action>
Start with: Ask the user if they have completed the hardware UAT for the staging confirmation UI, or if they need assistance running the test.
</next_action>
