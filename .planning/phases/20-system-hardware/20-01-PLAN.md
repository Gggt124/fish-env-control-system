---
phase: 20-system-hardware
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - main/static/index.html
  - main/static/style.css
  - main/static/app.js
autonomous: true
requirements:
  - SYS-UI-02
  - SYS-UI-03
must_haves:
  truths:
    - "System status displays memory usage with small, inline horizontal progress bars next to labels."
    - "Hardware/Install panel displays a yellow Unsaved Changes banner at the top when fields are modified."
    - "The Unsaved Changes banner contains the explicit Apply button."
    - "Pages are readable and responsive on mobile devices."
  artifacts:
    - path: "main/static/index.html"
      provides: "Status and Hardware views with new banner and progress bars"
    - path: "main/static/style.css"
      provides: "Styling for inline progress bars and unsaved changes banner"
    - path: "main/static/app.js"
      provides: "Logic to show/hide the unsaved changes banner and handle apply"
  key_links:
    - from: "main/static/app.js"
      to: "hardware-unsaved-banner"
      via: "DOM manipulation"
      pattern: "hardwareEl\\('hardware-unsaved-banner'\\)"
---

<objective>
Overhaul the system status view and the GPIO configuration interface for improved clarity and responsiveness.

Purpose: Make system health metrics easier to digest with inline progress bars and clarify pending hardware changes with a prominent yellow banner.
Output: Updated UI with inline progress bars, a new Unsaved Changes banner, and streamlined layout.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: System Status UI Updates</name>
  <files>main/static/index.html, main/static/style.css</files>
  <action>
    Update the System Status view to use small, inline horizontal progress bars next to the labels (per D-01).
    - In `style.css`, add a `.progress-bar-inline` class (and accompanying `.fill` class) that creates a small, inline bar (e.g., width 80px, height 6px, display inline-block, vertical-align middle, margin-left 8px) that sits directly next to an `.info-value` text.
    - In `index.html` within `<section id="view-status">`, locate the Memory card. 
    - Remove the large bottom `progress-bar` div and the `st-heap-pct` text.
    - Modify the "Free" or "Total DRAM" table rows to include the `.progress-bar-inline` element so the progress visually sits next to the value (e.g. `<span id="st-heap-pct-val"></span> <div class="progress-bar-inline"><div class="fill" id="st-heap-bar-inline"></div></div>`). 
    - Ensure responsive scaling so the inline bars do not break the table layout on mobile devices.
  </action>
  <verify>
    <automated>grep -q "progress-bar-inline" main/static/style.css</automated>
  </verify>
  <done>Memory card displays inline progress bars next to the text labels.</done>
</task>

<task type="auto">
  <name>Task 2: Hardware/Install Unsaved Changes Banner</name>
  <files>main/static/index.html, main/static/style.css</files>
  <action>
    Create the yellow "Unsaved Changes" banner in the Hardware/Install panel (per D-02).
    - In `style.css`, define an `.unsaved-banner` class using `var(--tertiary)` background/border colors (or a specific warning color) to create a distinct yellow/orange warning container. It should be a flex container with space-between alignment for the text and the button.
    - In `index.html` within `<section class="section-card hardware-map-panel">`, insert the new banner `div` (e.g., `id="hardware-unsaved-banner"`) at the top of the card (just under the `section-heading-row`).
    - The banner must contain a clear warning message ("Unsaved Changes") and the "Apply" button (e.g., `<button type="submit" form="hardware-map-form" class="btn btn-primary btn-sm">Apply</button>`).
    - Add the `hidden` class to the banner by default.
    - Remove the old `hardware-unsaved-warning` span and the old "Save Pending Map" submit button from the bottom of the form.
  </action>
  <verify>
    <automated>grep -q "hardware-unsaved-banner" main/static/index.html</automated>
  </verify>
  <done>A yellow banner container with an Apply button sits at the top of the Hardware Map panel.</done>
</task>

<task type="auto">
  <name>Task 3: Wiring Banner Logic in JS</name>
  <files>main/static/app.js</files>
  <action>
    Update the JavaScript to toggle the new Unsaved Changes banner (per D-02).
    - In `markHardwareDirty()`, modify the logic to remove the `hidden` class from `hardware-unsaved-banner` when `dirty` is true, and add `hidden` when `dirty` is false.
    - In `setHardwareClean()`, ensure the `hardware-unsaved-banner` is hidden.
    - Update `renderHardwareBanner()` if necessary to ensure it doesn't conflict. (Note: `hardware-reboot-banner` is a separate banner for the reboot requirement; keep them distinct).
    - In `updateHardwareSaveButton()`, ensure the logic now targets the new Apply button inside the banner instead of the old button, enabling/disabling it based on the `hardware-confirm-reboot` checkbox if applicable.
  </action>
  <verify>
    <automated>grep -q "hardware-unsaved-banner" main/static/app.js</automated>
  </verify>
  <done>Banner appears when dropdowns change and disappears after a successful save.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> UI | Untrusted client rendering HTML/JS locally |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | Hardware Map Config | mitigate | Backend validation must reject invalid pins (handled in prior phases); UI only controls presentation |
| T-20-02 | Information Disclosure | System Status | accept | Status is protected behind authenticated session route |
</threat_model>

<verification>
- View System Status and confirm memory usage has inline progress bars.
- Make a change to the Hardware map dropdowns and confirm the yellow banner appears at the top.
- Click Apply and confirm the banner disappears and the pending reboot card is updated.
- Verify both pages render correctly on mobile widths.
</verification>

<success_criteria>
1. Inline progress bars exist for memory status.
2. Unsaved changes banner appears prominently with an Apply button.
3. Form submission works via the new Apply button.
4. Active and Pending configurations remain visually distinct.
</success_criteria>

<output>
Create `.planning/phases/20-system-hardware/20-01-SUMMARY.md` when done
</output>
