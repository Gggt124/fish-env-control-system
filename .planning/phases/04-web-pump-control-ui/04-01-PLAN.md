---
phase: 04-web-pump-control-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - main/static/dashboard.html
  - main/static/style.css
autonomous: true
requirements:
  - UI-01
  - UI-03
  - UI-04
  - UI-05
  - UI-06
  - UI-07
user_setup: []
must_haves:
  truths:
    - "D-01 `/dashboard` becomes the main Pump Control page; pump status and controls are first-viewport priority."
    - "D-02 the top dashboard panel shows running state, countdown, active timer/phase, relay/float state, and Start/Stop controls."
    - "D-03 Timer 1/Timer 2 settings and auto-start controls remain on the same dashboard below the runtime panel."
    - "D-04 Wi-Fi/system information is reduced to a compact summary while `/status` and `/wifi` remain available."
    - "D-05 timer inputs use separate minute and second fields for Timer 1/Timer 2 ON/OFF durations."
    - "D-11 Start and Stop are clear separate buttons in the runtime panel."
    - "D-14 runtime display priority is running state, countdown, active timer/phase, then relay and float state."
    - "D-16 Thai running wording distinguishes controller state from relay state."
    - "D-17 float wording presents a binary switch and ties OFF to Timer 1 and ON to Timer 2."
    - "D-18 relay wording uses ON/OFF plus a short Thai explanation."
    - "D-19 timer phase wording uses `ช่วงเปิด` and `ช่วงปิด`."
    - "UI-07 remains local-only: no CDN links, external fonts, package assets, or internet dependencies."
  artifacts:
    - path: "main/static/dashboard.html"
      provides: "Pump-first dashboard structure and stable DOM IDs for Phase 4 behavior"
      contains: "pump-runtime-panel"
    - path: "main/static/style.css"
      provides: "Responsive pump dashboard styling using the existing CSS design system"
      contains: "pump-runtime"
  key_links:
    - from: "main/static/dashboard.html"
      to: "main/static/app.js"
      via: "stable element IDs for later pump config/status wiring"
      pattern: "pump-start-btn"
---

<objective>
Reshape the authenticated dashboard into the pump-control surface.

Purpose: Make `/dashboard` immediately useful for operating the pump controller while preserving navigation to Wi-Fi setup and system status.
Output: `dashboard.html` and `style.css` define the pump runtime panel, timer settings form, auto-start control, Start/Stop actions, degraded/stale placeholders, and compact system quick status with no external dependencies.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-web-pump-control-ui/04-CONTEXT.md
@.planning/phases/04-web-pump-control-ui/04-RESEARCH.md
@.planning/phases/03-authenticated-pump-control-api/03-VERIFICATION.md
@main/static/dashboard.html
@main/static/style.css
@main/static/app.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace dashboard hero with pump runtime panel</name>
  <files>main/static/dashboard.html, main/static/style.css</files>
  <read_first>main/static/dashboard.html, main/static/style.css, .planning/phases/04-web-pump-control-ui/04-CONTEXT.md</read_first>
  <action>Rewrite the first dashboard content area so the page header identifies Pump Control and the first panel contains the controller running label, large MM:SS countdown, active timer, current phase, relay state, float switch state, status freshness indicator, pump-status error placeholder, and separate Start and Stop buttons. Use stable IDs such as `pump-running-label`, `pump-countdown`, `pump-active-timer`, `pump-phase`, `pump-relay-state`, `pump-float-state`, `pump-sync-state`, `pump-status-error`, `pump-start-btn`, and `pump-stop-btn`. Keep the existing app shell, sidebar links, logout link, `/wifi` header action, and script include.</action>
  <verify>Run `rg "pump-runtime|pump-countdown|pump-start-btn|pump-stop-btn|pump-float-state|pump-relay-state" main/static/dashboard.html main/static/style.css`.</verify>
  <acceptance_criteria>
    - Pump runtime status and Start/Stop controls appear before Wi-Fi/system summaries.
    - Runtime panel has stable IDs for JavaScript wiring.
    - Float state wording is binary ON/OFF, not a water-level percentage.
    - Relay state wording distinguishes relay output from controller running state.
    - Existing navigation to dashboard, system status, Wi-Fi settings, and logout remains present.
  </acceptance_criteria>
  <done>The first viewport is a pump runtime control panel.</done>
</task>

<task type="auto">
  <name>Task 2: Add timer and auto-start settings form</name>
  <files>main/static/dashboard.html, main/static/style.css</files>
  <read_first>main/static/dashboard.html, .planning/phases/04-web-pump-control-ui/04-CONTEXT.md</read_first>
  <action>Add a settings section below the runtime panel with Timer 1 and Timer 2 groups. Each timer group must provide ON minutes, ON seconds, OFF minutes, and OFF seconds inputs, for eight timer inputs total. Add field-level error elements for each duration, an `auto_start` checkbox/toggle, a save button, and an unsaved-changes warning placeholder. Use input IDs that clearly map to the API fields, for example `timer1-on-min`, `timer1-on-sec`, `timer1-off-min`, `timer1-off-sec`, `timer2-on-min`, `timer2-on-sec`, `timer2-off-min`, and `timer2-off-sec`. Do not add GPIO, relay pin, float pin, or debounce editing controls.</action>
  <verify>Run `rg "timer1-on-min|timer1-off-sec|timer2-on-min|timer2-off-sec|pump-auto-start|pump-save-config|unsaved" main/static/dashboard.html`.</verify>
  <acceptance_criteria>
    - UI-01 is represented by visible Timer 1 and Timer 2 ON/OFF setting fields.
    - TIME-01 through TIME-04 each have minute/second inputs.
    - Auto-start can be edited in the same dashboard section.
    - The form leaves room for validation errors and saved/unsaved feedback.
    - Phase 4 does not expose editable GPIO/debounce settings.
  </acceptance_criteria>
  <done>Dashboard markup contains the complete editable timer and auto-start form.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve compact system and Wi-Fi quick status</name>
  <files>main/static/dashboard.html, main/static/style.css</files>
  <read_first>main/static/dashboard.html, main/static/app.js, main/static/style.css</read_first>
  <action>Replace the old Wi-Fi/system status-card grid with a compact quick-status strip or summary section lower on the dashboard. Preserve existing IDs that `refreshStatus()` currently updates when practical, or document and update the follow-up JavaScript plan if IDs change. Keep links to `/status` and `/wifi`, and remove or rewrite obsolete copy that says relay control will be added later.</action>
  <verify>Run `rg "dash-chip|dash-heap-pct|dash-rssi|dash-ap-clients|/status|/wifi|Relay control and scheduling will be added" main/static/dashboard.html`.</verify>
  <acceptance_criteria>
    - Existing `/api/status` based quick status can still be rendered after JavaScript wiring.
    - Full system and Wi-Fi detail pages remain discoverable.
    - Obsolete Phase 1 placeholder wording is removed.
    - System status no longer competes with pump controls in the first viewport.
  </acceptance_criteria>
  <done>Dashboard keeps Wi-Fi/system awareness without displacing pump control.</done>
</task>

</tasks>

<verification>
- [ ] `rg "pump-runtime|pump-countdown|pump-start-btn|pump-stop-btn" main/static/dashboard.html main/static/style.css`
- [ ] `rg "timer1-on-min|timer1-on-sec|timer1-off-min|timer1-off-sec|timer2-on-min|timer2-on-sec|timer2-off-min|timer2-off-sec" main/static/dashboard.html`
- [ ] `rg "pump-auto-start|pump-save-config|unsaved" main/static/dashboard.html`
- [ ] `rg "Relay control and scheduling will be added" main/static/dashboard.html` returns no matches.
- [ ] Confirm `dashboard.html` still includes `/style.css`, `/app.js`, `/status`, `/wifi`, and logout navigation.
</verification>

<success_criteria>
- The dashboard is pump-first and no longer reads like a Wi-Fi/system dashboard.
- Timer settings, auto-start, Start/Stop, live-status placeholders, and compact system status are all present.
- UI wording preserves binary float-switch semantics and relay/controller distinction.
- No CDN or external asset dependency is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/04-web-pump-control-ui/04-01-SUMMARY.md`.
</output>
