---
phase: 04-web-pump-control-ui
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - main/static/app.js
autonomous: true
requirements:
  - TIME-01
  - TIME-02
  - TIME-03
  - TIME-04
  - UI-01
  - UI-02
  - UI-03
  - UI-07
user_setup: []
must_haves:
  truths:
    - "D-05 timer inputs use separate minute and second fields for each Timer 1/Timer 2 ON/OFF duration."
    - "D-06 the UI converts minute/second inputs to seconds-only API fields."
    - "D-07 the UI validates required numeric input, minimum 5 seconds, and maximum 86400 seconds before save."
    - "D-08 after successful save, the form stays editable and shows saved feedback."
    - "D-09 unsaved timer or auto-start changes show an in-page warning; Start/Stop still acts on applied device config."
    - "D-10 auto-start is saved with timer configuration but does not imply runtime Start or Stop."
    - "D-11 Start and Stop are separate controls."
    - "D-12 while Start or Stop is in flight, both action buttons are disabled and a pending state is visible."
    - "D-13 after Start/Stop returns, the dashboard updates from the returned status snapshot when available."
    - "The UI preserves the loaded `relay_polarity` value when saving config and never submits read-only GPIO/debounce fields."
  artifacts:
    - path: "main/static/app.js"
      provides: "Pump config form loading, validation, saving, unsaved tracking, and Start/Stop actions"
      contains: "initPumpDashboard"
  key_links:
    - from: "main/static/app.js"
      to: "main/web_server.c"
      via: "Phase 3 pump config and action APIs"
      pattern: "/api/pump/config"
---

<objective>
Wire the pump settings form and runtime action buttons to the authenticated pump APIs.

Purpose: Let the operator edit Timer 1/Timer 2 durations in minutes and seconds, save auto-start preference, and start/stop the pump controller through the existing Phase 3 API contract.
Output: `app.js` loads pump config, validates and saves full replacement config payloads, tracks unsaved changes, preserves relay polarity, and performs pending-safe Start/Stop actions.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-web-pump-control-ui/04-CONTEXT.md
@.planning/phases/04-web-pump-control-ui/04-RESEARCH.md
@.planning/phases/04-web-pump-control-ui/04-01-SUMMARY.md
@.planning/phases/03-authenticated-pump-control-api/03-CONTEXT.md
@main/static/dashboard.html
@main/static/app.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dashboard pump state and initialization</name>
  <files>main/static/app.js</files>
  <read_first>main/static/app.js, main/static/dashboard.html, .planning/phases/04-web-pump-control-ui/04-CONTEXT.md</read_first>
  <action>Replace the existing dashboard initializer with a pump-aware `initPumpDashboard()` flow for `/dashboard`. Add small file-local state for loaded config, loaded relay polarity, dirty form state, pending action state, latest pump status snapshot, and timers used by the later live status plan. Keep existing `/api/status` quick-status refresh available for compact system status. Initialize form event handlers, Start/Stop handlers, config load, pump status load hook, and system quick status refresh.</action>
  <verify>Run `rg "initPumpDashboard|pumpConfig|relay_polarity|pumpPending|initDashboard" main/static/app.js`.</verify>
  <acceptance_criteria>
    - `/dashboard` initializes pump-specific behavior instead of only polling `/api/status`.
    - Existing status page and Wi-Fi page initialization still work.
    - Shared API helpers remain framework-free and unchanged unless a narrow error-handling improvement is needed.
    - State needed for config and action handling is centralized enough to avoid duplicate DOM parsing.
  </acceptance_criteria>
  <done>Dashboard JavaScript has a pump-specific initialization path.</done>
</task>

<task type="auto">
  <name>Task 2: Load and render pump configuration</name>
  <files>main/static/app.js</files>
  <read_first>main/static/app.js, main/static/dashboard.html, .planning/phases/03-authenticated-pump-control-api/03-CONTEXT.md</read_first>
  <action>Add `loadPumpConfig()` using `apiGet('/api/pump/config')`. Convert seconds values into minute/second input pairs with integer math. Populate Timer 1/Timer 2 ON/OFF fields and the auto-start control. Preserve the loaded `relay_polarity` value for future saves. Show a degraded config-load error without breaking the rest of the dashboard if the config endpoint fails.</action>
  <verify>Run `rg "loadPumpConfig|timer1_on_sec|timer1_off_sec|timer2_on_sec|timer2_off_sec|relay_polarity" main/static/app.js`.</verify>
  <acceptance_criteria>
    - Config load populates all eight timer inputs.
    - Auto-start reflects the loaded API value.
    - Loaded relay polarity is stored for save payloads.
    - Read-only GPIO/debounce fields are not rendered as editable controls.
    - Config load failure leaves Start/Stop status handling independent where possible.
  </acceptance_criteria>
  <done>Pump settings load from the device into the editable dashboard form.</done>
</task>

<task type="auto">
  <name>Task 3: Validate and save timer settings</name>
  <files>main/static/app.js</files>
  <read_first>main/static/app.js, main/static/dashboard.html, .planning/phases/04-web-pump-control-ui/04-CONTEXT.md</read_first>
  <action>Add helpers to read each minute/second pair, require numeric input, reject negative or non-integer values, calculate total seconds, and enforce 5..86400 seconds. Display field-level errors for invalid durations and avoid sending a request while validation fails. On save, POST the full replacement payload with `timer1_on_sec`, `timer1_off_sec`, `timer2_on_sec`, `timer2_off_sec`, `auto_start`, and the preserved `relay_polarity`. Do not submit `float_gpio`, `relay_gpio`, or `debounce_ms`. After a successful save, clear dirty state, keep inputs editable, show saved feedback, refresh config, and request an immediate pump status sync.</action>
  <verify>Run `rg "validatePump|duration|86400|/api/pump/config|auto_start|relay_polarity" main/static/app.js`.</verify>
  <acceptance_criteria>
    - TIME-01 through TIME-04 convert to the API's seconds-only fields.
    - Invalid form values produce visible field errors and no API request.
    - Save payload contains all editable fields required by the full replacement API.
    - Save payload preserves relay polarity and excludes GPIO/debounce fields.
    - Successful save does not lock the form and does not start or stop the pump solely because auto-start changed.
  </acceptance_criteria>
  <done>Timer settings and auto-start can be validated and saved from the dashboard.</done>
</task>

<task type="auto">
  <name>Task 4: Implement unsaved tracking and Start/Stop actions</name>
  <files>main/static/app.js</files>
  <read_first>main/static/app.js, main/static/dashboard.html, .planning/phases/04-web-pump-control-ui/04-CONTEXT.md</read_first>
  <action>Track dirty state whenever timer or auto-start inputs change, show the unsaved-changes warning, and keep Start/Stop enabled according to applied runtime status rather than form dirty state. Add `startPump()` and `stopPump()` handlers using `apiPost('/api/pump/start', {})` and `apiPost('/api/pump/stop', {})`. While either request is pending, disable both Start and Stop, update pending text/state, and after success apply the returned nested `status` snapshot if present before triggering immediate status sync. Show clear toast/error feedback for failures.</action>
  <verify>Run `rg "unsaved|startPump|stopPump|/api/pump/start|/api/pump/stop|already_running|already_stopped" main/static/app.js`.</verify>
  <acceptance_criteria>
    - Unsaved timer/auto-start edits are visible in-page.
    - Start and Stop are not blocked by dirty form state.
    - Pending Start/Stop requests disable both action buttons until completion.
    - Returned action status snapshots update the dashboard immediately when available.
    - Failed action requests leave the dashboard in a degraded but usable state.
  </acceptance_criteria>
  <done>Operator controls can save settings and safely request runtime Start/Stop.</done>
</task>

</tasks>

<verification>
- [ ] `rg "/api/pump/config|/api/pump/start|/api/pump/stop" main/static/app.js`
- [ ] `rg "timer1_on_sec|timer1_off_sec|timer2_on_sec|timer2_off_sec|auto_start|relay_polarity" main/static/app.js`
- [ ] `rg "float_gpio|relay_gpio|debounce_ms" main/static/app.js` confirms those fields are not included in save payload construction.
- [ ] `rg "86400|5" main/static/app.js` confirms min/max validation is present.
- [ ] `rg "unsaved|pending|already_running|already_stopped" main/static/app.js`
</verification>

<success_criteria>
- User can configure Timer 1 and Timer 2 ON/OFF durations in minutes and seconds.
- User can save timer settings and auto-start preference from the UI.
- User can start and stop pump control from the UI.
- Start/Stop actions are pending-safe and use returned status snapshots.
- UI remains embedded, local-only, and dependency-free.
</success_criteria>

<output>
After completion, create `.planning/phases/04-web-pump-control-ui/04-02-SUMMARY.md`.
</output>
