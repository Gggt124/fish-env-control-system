---
phase: 22-uat-bug-fixes
plan: 03
type: execute
wave: 3
depends_on: ["22-02"]
files_modified:
  - main/static/style.css
  - main/static/index.html
autonomous: true
requirements:
  - UAT-UI-03
must_haves:
  truths:
    - "Wi-Fi connection modal is usable on mobile devices even when advanced options are expanded"
    - "Modal content scrolls vertically if it exceeds viewport height"
  artifacts:
    - path: "main/static/style.css"
      provides: "Max-height and overflow-y rules for modals"
---

<objective>
Fix the Wi-Fi connection modal mobile overflow bug where expanding advanced options (like Static IP) pushes content off-screen, making the modal unusable.
Purpose: Ensure users can configure advanced Wi-Fi settings on small screens.
Output: Updated `style.css` (and potentially `index.html` if structural changes are needed) to support modal scrolling.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/22-uat-bug-fixes/22-CONTEXT.md
@main/static/style.css
@main/static/index.html
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add Scroll Support to Modals</name>
  <files>main/static/style.css</files>
  <action>In `main/static/style.css`, locate the modal container classes (likely `.modal-content` or similar). Add CSS rules to constrain the maximum height relative to the viewport (e.g., `max-height: 90vh;` or `max-height: calc(100svh - 40px);`) and enable vertical scrolling (`overflow-y: auto;`). Ensure the modal header (title/close button) and footer (action buttons) remain accessible if possible, or at least ensure the entire modal content is scrollable without breaking the page layout.</action>
  <verify>
    <automated>grep -c "overflow-y" main/static/style.css</automated>
  </verify>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client Browser | HTML and CSS UI |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03 | Denial of Service | Static UI (Modal) | accept | Purely visual fix. Prevents accidental UI lockup on mobile. |
</threat_model>

<verification>
1. `style.css` contains `overflow-y` and `max-height` constraints on modal content.
</verification>

<success_criteria>
- Modals that contain large amounts of content can be scrolled on mobile screens without breaking the layout.
</success_criteria>

<output>
Create `.planning/phases/22-uat-bug-fixes/22-03-SUMMARY.md` when done
</output>
