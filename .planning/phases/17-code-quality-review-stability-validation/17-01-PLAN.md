---
phase: 17-code-quality-review-stability-validation
plan: 01
type: execute
wave: 1
depends_on: ["16-01"]
files_modified: []
autonomous: false
requirements: [CODE-REV-01, CODE-REV-02, CODE-REV-03]
user_setup: []

must_haves:
  truths:
    - "Firmware compiles cleanly with zero warnings in custom sources"
    - "JavaScript/HTML web assets load and execute with zero browser console errors"
    - "Firmware runs stably during manual regression and soak testing without watchdog resets"
  artifacts:
    - path: "main/app_main.c"
      provides: "Firmware entrypoint and watchdog reset loop"
    - path: "main/static/app.js"
      provides: "Frontend API logic and state rendering"
  key_links:
    - from: "main/app_main.c"
      to: "main/web_server.c"
      via: "http server start"
      pattern: "start_webserver"
---

<objective>
Review custom firmware source code and web static assets to resolve any compilation warnings and JavaScript console errors, ensuring complete runtime stability during manual regression checks.

Purpose: Maintain the integrity, safety, and reliability of the pump, cooling, and Wi-Fi system.
Output: Verification of clean build logs, clean browser execution, and stable hardware runtime logs.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@main/app_main.c
@main/web_server.c
@main/static/app.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compiler Warning Audit & Watchdog Verification</name>
  <files>main/app_main.c, main/web_server.c</files>
  <action>Perform a full clean rebuild using scripts/build.ps1. Inspect the compile log to verify zero compilation warnings exist in main/ or custom components/ (per D-01/D-02). Review the main task watchdog loop to ensure task watchdog is fed/reset correctly under a default 10s timeout (per D-07/D-08). Ensure default log level remains ESP_LOG_INFO (per D-05). Fix any detected warnings in C files.</action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File .\scripts\build.ps1 -FullClean</automated>
  </verify>
  <done>Clean rebuild has zero warnings in main/ and components/ compiled outputs.</done>
</task>

<task type="auto">
  <name>Task 2: Web Assets Syntax & Console Log Audit</name>
  <files>main/static/app.js</files>
  <action>Audit static files (app.js, style.css, and HTML pages) for syntax consistency and browser issues. Clean up any temporary or debug console.log statements in app.js, while retaining required operation diagnostics (per D-03/D-04). Verify diagnostics and status calculations are robust and correctly formatted (per D-06).</action>
  <verify>
    <automated>powershell -Command "Select-String -Path 'main\static\*.js' -Pattern 'console\.log'"</automated>
  </verify>
  <done>JavaScript and web assets contain zero syntax/runtime console errors, and debug console logs are eliminated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Runtime Stability & Manual Regression Validation Check</name>
  <files>main/app_main.c</files>
  <action>Flash the clean firmware to the ESP32 DevKit V1 board. Run the monitor tool to watch serial outputs. Load the web interface, log in, configure timers, toggle the float switch, and verify that the system runs stably under load without watchdog resets, crashes, or connection drops.</action>
  <verify>
    <what-built>Clean firmware and web interface with zero warnings and zero console errors</what-built>
    <how-to-verify>
      1. Run idf.py -p COMx flash monitor (or corresponding serial tool) to flash and boot.
      2. Verify log level remains INFO and no watchdog resets occur.
      3. Connect to SoftAP "FishPump-Setup" and log in at http://192.168.4.1.
      4. Open browser console and verify no JS errors are printed.
      5. Perform manual regression checks on relay timers, DS18B20 cooling logic, and float switch state transitions.
    </how-to-verify>
    <resume-signal>Type "approved" once device runtime stability is verified without resets or browser console errors.</resume-signal>
  </verify>
  <done>Device is flashed, tested, and works stably with zero errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Web Client -> Web Server | HTTP requests to API endpoints must be validated and authenticated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | HTTP API Route Entry | mitigate | Enforce role-based token checking and input validation bounds for all POST endpoints (e.g. settings write) |
| T-17-02 | Denial of Service | Task Watchdog | mitigate | Main loop resets task watchdog every 5 seconds (D-07). Validate tasks do not block CPU. |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies added in this review phase. Managed components are frozen. |
</threat_model>

<verification>
Verify compile outputs, check browser JS logs, and trace runtime serial output during regression tests.
</verification>

<success_criteria>
1. Clean rebuild via build.ps1 completes with zero compiler warnings in main/ and components/.
2. Web UI functions with zero JavaScript console errors.
3. System runs stably on physical hardware with no watchdog resets or connection crashes.
</success_criteria>

<output>
Create .planning/phases/17-code-quality-review-stability-validation/17-01-SUMMARY.md when done
</output>
