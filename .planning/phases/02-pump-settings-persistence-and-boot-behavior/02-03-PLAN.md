---
phase: 02-pump-settings-persistence-and-boot-behavior
plan: 03
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - components/app_config/app_config.h
  - components/nvs_store/nvs_store.h
  - components/nvs_store/nvs_store.c
  - main/app_main.c
  - build/
autonomous: true
requirements:
  - TIME-08
  - RUN-04
  - RUN-05
user_setup: []
must_haves:
  truths:
    - "Phase 2 remains build-valid after persistence and boot changes."
    - "TIME-08 is covered by NVS storage for Timer 1/2 ON/OFF settings."
    - "RUN-04 is covered by default auto-start after saved/default settings load."
    - "RUN-05 is covered at the firmware persistence layer by saving/loading auto_start; authenticated API and UI controls remain future phases."
    - "Existing Wi-Fi setup, SoftAP fallback, login/session, mDNS, captive DNS, and status loop startup remain wired."
    - "No Phase 2 work adds pump HTTP APIs, web UI controls, editable GPIO/debounce settings, event logs, factory reset routes, or hardware validation."
  artifacts:
    - path: "build/fish_pump_relay_timer_control.bin"
      provides: "ESP-IDF build output"
      contains: "firmware binary"
    - path: ".planning/phases/02-pump-settings-persistence-and-boot-behavior/02-03-SUMMARY.md"
      provides: "Execution summary and validation evidence"
      contains: "idf.py build"
  key_links:
    - from: "components/nvs_store/nvs_store.c"
      to: "main/app_main.c"
      via: "stored pump settings drive boot config and auto-start"
      pattern: "nvs_store_load_pump_settings"
---

<objective>
Validate Phase 2 integration and document the execution evidence.

Purpose: Ensure the persistence and boot behavior changes compile as ESP-IDF firmware and preserve the existing recovery-oriented Wi-Fi/web startup surface.
Output: `idf.py build` succeeds, the firmware binary exists, and the plan summary records requirement coverage and any deviations.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-pump-settings-persistence-and-boot-behavior/02-CONTEXT.md
@.planning/phases/02-pump-settings-persistence-and-boot-behavior/02-RESEARCH.md
@.planning/phases/02-pump-settings-persistence-and-boot-behavior/02-01-SUMMARY.md
@.planning/phases/02-pump-settings-persistence-and-boot-behavior/02-02-SUMMARY.md
@AGENTS.md
@scripts/build.ps1
@components/app_config/app_config.h
@components/nvs_store/nvs_store.h
@components/nvs_store/nvs_store.c
@main/app_main.c
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run static coverage checks</name>
  <files>components/app_config/app_config.h, components/nvs_store/nvs_store.h, components/nvs_store/nvs_store.c, main/app_main.c</files>
  <read_first>.planning/phases/02-pump-settings-persistence-and-boot-behavior/02-CONTEXT.md, .planning/REQUIREMENTS.md</read_first>
  <action>Run focused `rg` checks to confirm auto-start default, pump settings APIs, `pump_cfg` namespace, boot load, boot merge, and conditional `pump_control_start()` are present. Also check that no Phase 2 route/UI files were modified unless execution legitimately needed a build-only metadata update.</action>
  <verify>Run the commands listed in this plan's verification block.</verify>
  <acceptance_criteria>
    - Static checks show the storage contract and boot integration exist.
    - No HTTP routes or static UI controls are added in Phase 2.
    - The plan summary records any intentionally deferred UI/API part of RUN-05.
  </acceptance_criteria>
  <done>Requirement coverage can be traced before build validation.</done>
</task>

<task type="auto">
  <name>Task 2: Build validate firmware</name>
  <files>build/</files>
  <read_first>AGENTS.md, scripts/build.ps1, sdkconfig.defaults</read_first>
  <action>Run the documented ESP-IDF build workflow. Prefer `.\scripts\build.ps1` because it sets code page 65001, `PYTHONUTF8=1`, exports ESP-IDF, prints `idf.py --version`, and runs `idf.py build`. If the script is unavailable, run `chcp 65001 > $null`, export ESP-IDF from `$env:IDF_PATH` or `C:\esp-idf`, then run `idf.py build`. Do not flash hardware.</action>
  <verify>`idf.py build` succeeds and `build/fish_pump_relay_timer_control.bin` exists.</verify>
  <acceptance_criteria>
    - Build completes without C compiler, linker, or CMake errors.
    - `build/fish_pump_relay_timer_control.bin` is generated.
    - No hardware flashing or monitor session is required.
  </acceptance_criteria>
  <done>Phase 2 automated validation gate passes.</done>
</task>

<task type="auto">
  <name>Task 3: Summarize Phase 2 requirement coverage</name>
  <files>.planning/phases/02-pump-settings-persistence-and-boot-behavior/02-03-SUMMARY.md</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/02-pump-settings-persistence-and-boot-behavior/02-CONTEXT.md, previous Phase 2 summaries</read_first>
  <action>Create the plan summary using the established GSD summary format. Include evidence that TIME-08 is implemented through NVS timer persistence, RUN-04 through default enabled boot auto-start after settings load, and RUN-05 through persisted auto_start firmware support while web UI controls remain scheduled for Phase 4 and API support for Phase 3.</action>
  <verify>Summary file exists and names TIME-08, RUN-04, and RUN-05.</verify>
  <acceptance_criteria>
    - Summary records build command and result.
    - Summary records any deviations and compatibility fixes.
    - Summary explicitly notes that no UI/API controls were added in Phase 2.
  </acceptance_criteria>
  <done>Phase 2 execution evidence is ready for verification.</done>
</task>

</tasks>

<verification>
- [ ] `rg "APP_TEMPLATE_PUMP_AUTO_START_DEFAULT" components/app_config/app_config.h`
- [ ] `rg "nvs_store_pump_settings_t|nvs_store_load_pump_settings|nvs_store_save_pump_settings" components/nvs_store`
- [ ] `rg "pump_cfg|auto_start|relay_low" components/nvs_store/nvs_store.c`
- [ ] `rg -n "nvs_store_init|nvs_store_load_pump_settings|pump_control_init|pump_control_start" main/app_main.c`
- [ ] `rg "/api/pump|pump settings|auto-start" main/web_server.c main/static` returns no Phase 2 route/UI additions unless explicitly justified.
- [ ] `.\scripts\build.ps1` or documented `idf.py build` succeeds.
- [ ] `Test-Path build\fish_pump_relay_timer_control.bin` returns `True`.
</verification>

<success_criteria>
- Firmware builds successfully after Phase 2 persistence and boot changes.
- Timer settings survive reboot via NVS storage APIs.
- Boot auto-start defaults enabled after settings load.
- Persisted auto-start disabled suppresses boot start.
- Existing local recovery services remain available if pump settings are invalid or pump startup fails.
</success_criteria>

<output>
After completion, create `.planning/phases/02-pump-settings-persistence-and-boot-behavior/02-03-SUMMARY.md`.
</output>

