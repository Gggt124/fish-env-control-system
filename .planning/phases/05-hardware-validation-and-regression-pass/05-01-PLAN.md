---
phase: 05-hardware-validation-and-regression-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05-hardware-validation-and-regression-pass/05-01-SUMMARY.md
  - build/
autonomous: true
requirements:
  - VAL-02
  - VAL-03
  - VAL-04
  - VAL-05
user_setup: []
must_haves:
  truths:
    - "D-01 relay validation uses GPIO26 as relay output with active-low relay polarity, which the user reported works on real hardware."
    - "D-02 float validation uses GPIO32 as binary float input with internal pull-up and switch-to-GND treated as ON."
    - "D-03 relay phase validation passed on hardware: relay follows ON/OFF timer phase behavior and Stop forces inactive immediately."
    - "D-04 reboot validation passed on hardware: persisted timer settings and auto-start behavior survive reboot and take effect."
    - "D-05 web and Wi-Fi regression validation passed: SoftAP, login, dashboard, status page, Wi-Fi page, Wi-Fi scan/connect, and pump UI remain reachable."
    - "D-06 manual hardware results are the primary evidence source because there is no automated ESP32 hardware test rig."
    - "D-07 build validation remains required, but build success alone cannot close VAL-02 through VAL-05."
    - "D-08 new findings should be documented in phase verification notes or project docs rather than converted into new Phase 5 feature work."
  artifacts:
    - path: ".planning/phases/05-hardware-validation-and-regression-pass/05-01-SUMMARY.md"
      provides: "Phase 5 validation evidence and requirement traceability"
      contains: "VAL-02"
    - path: "build/fish_pump_relay_timer_control.bin"
      provides: "ESP-IDF build output for the validated firmware state"
      contains: "firmware binary"
  key_links:
    - from: ".planning/phases/05-hardware-validation-and-regression-pass/05-CONTEXT.md"
      to: ".planning/phases/05-hardware-validation-and-regression-pass/05-01-SUMMARY.md"
      via: "user-confirmed manual hardware and regression evidence"
      pattern: "D-01"
---

<objective>
Close the hardware validation and regression pass for the completed ESP32 pump controller.

Purpose: Preserve the real-device evidence for float switching, relay phase behavior, Stop safety, reboot persistence, and web/Wi-Fi reachability while confirming the current firmware still builds.
Output: `05-01-SUMMARY.md` records build/static checks, user-confirmed manual hardware results, web/Wi-Fi regression evidence, requirement traceability for `VAL-02` through `VAL-05`, and any findings discovered during the final pass.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-hardware-validation-and-regression-pass/05-CONTEXT.md
@.planning/phases/05-hardware-validation-and-regression-pass/05-RESEARCH.md
@.planning/phases/01-hardware-safe-pump-control-core/01-VERIFICATION.md
@.planning/phases/02-pump-settings-persistence-and-boot-behavior/02-VERIFICATION.md
@.planning/phases/03-authenticated-pump-control-api/03-VERIFICATION.md
@.planning/phases/04-web-pump-control-ui/04-VERIFICATION.md
@components/app_config/app_config.h
@components/pump_control/pump_control.h
@components/pump_control/pump_control.c
@components/nvs_store/nvs_store.h
@components/nvs_store/nvs_store.c
@main/app_main.c
@main/web_server.c
@main/static/dashboard.html
@main/static/app.js
@scripts/build.ps1
@scripts/flash.ps1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm the current firmware build state</name>
  <files>build/</files>
  <read_first>AGENTS.md, scripts/build.ps1, .planning/phases/05-hardware-validation-and-regression-pass/05-CONTEXT.md</read_first>
  <action>Run the documented Windows ESP-IDF build wrapper with `.\scripts\build.ps1`. If the wrapper cannot run because ESP-IDF is unavailable in the current environment, record the exact blocker and do not substitute a non-ESP-IDF build. Confirm whether `build\fish_pump_relay_timer_control.bin` exists after the build attempt.</action>
  <verify>`.\scripts\build.ps1` exits successfully and `Test-Path build\fish_pump_relay_timer_control.bin` returns `True`, or the summary records the exact environment blocker.</verify>
  <acceptance_criteria>
    - Build validation uses ESP-IDF, not Arduino or PlatformIO.
    - The build command and result are recorded in the summary.
    - Existing ESP-IDF warning noise is distinguished from a failed build.
    - No flash command is run unless the executor has an explicit COM port and user direction.
  </acceptance_criteria>
  <done>Current firmware build state is known and documented.</done>
</task>

<task type="auto">
  <name>Task 2: Run source-level regression checks for the validated surface</name>
  <files>.planning/phases/05-hardware-validation-and-regression-pass/05-01-SUMMARY.md</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/05-hardware-validation-and-regression-pass/05-CONTEXT.md, components/app_config/app_config.h, components/pump_control/pump_control.c, main/web_server.c, main/static/dashboard.html, main/static/app.js</read_first>
  <action>Run focused `rg` checks that confirm the validation surface still exists: GPIO32/GPIO26 defaults, pump start/stop/status/config routes, dashboard timer fields, binary float/relay wording, Wi-Fi/status routes, and absence of CDN/external frontend dependencies. These checks do not replace hardware evidence; they guard against obvious code drift before closeout.</action>
  <verify>Run the `rg` commands listed in the plan-level verification block and record pass/fail in the summary.</verify>
  <acceptance_criteria>
    - GPIO32 float and GPIO26 relay defaults remain visible in source.
    - `/api/pump/config`, `/api/pump/status`, `/api/pump/start`, and `/api/pump/stop` remain registered.
    - Dashboard still contains Timer 1/Timer 2 controls and pump status labels.
    - Existing `/status`, `/wifi`, `/api/status`, and Wi-Fi scan/connect routes remain present.
    - Static frontend remains local-only with no CDN or internet dependency.
  </acceptance_criteria>
  <done>Static regression checks support the manual validation closeout.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve manual hardware validation evidence</name>
  <files>.planning/phases/05-hardware-validation-and-regression-pass/05-01-SUMMARY.md</files>
  <read_first>.planning/phases/05-hardware-validation-and-regression-pass/05-CONTEXT.md, .planning/REQUIREMENTS.md, .planning/PROJECT.md</read_first>
  <action>Create `05-01-SUMMARY.md` using the established GSD summary style. Preserve the user-confirmed hardware facts from `05-CONTEXT.md`: GPIO26 active-low relay wiring works, GPIO32 active-low float switch works, Float OFF selects Timer 1, Float ON selects Timer 2, relay follows ON/OFF phase, Stop forces relay inactive, reboot persistence works, and web/Wi-Fi regression passed. Clearly state that these are manual hardware results, not automated tests.</action>
  <verify>`rg "VAL-02|VAL-03|VAL-04|VAL-05|GPIO26|GPIO32|Float OFF|Timer 1|Timer 2|Stop forces|SoftAP" .planning/phases/05-hardware-validation-and-regression-pass/05-01-SUMMARY.md`.</verify>
  <acceptance_criteria>
    - Summary names the exact hardware defaults under validation.
    - Summary maps `VAL-02` to Float OFF/ON selecting Timer 1/Timer 2.
    - Summary maps `VAL-03` to relay ON/OFF phase behavior and Stop inactive behavior.
    - Summary maps `VAL-04` to persisted timer settings and auto-start behavior after reboot.
    - Summary maps `VAL-05` to SoftAP, login, dashboard/status/Wi-Fi pages, Wi-Fi scan/connect, and pump UI reachability.
  </acceptance_criteria>
  <done>Manual device evidence is captured in the Phase 5 summary.</done>
</task>

<task type="auto">
  <name>Task 4: Record closeout findings and residual risk</name>
  <files>.planning/phases/05-hardware-validation-and-regression-pass/05-01-SUMMARY.md</files>
  <read_first>.planning/codebase/TESTING.md, .planning/codebase/CONCERNS.md, .planning/phases/05-hardware-validation-and-regression-pass/05-RESEARCH.md</read_first>
  <action>Finish the summary with deviations, findings, residual risk, and next-step guidance. If validation remains clean, say so explicitly. If a repeat check finds a defect, record the exact symptom, affected requirement, command or manual step used to find it, and whether it blocks verification. Keep any follow-up work as documentation or a new future task unless the issue directly blocks `VAL-02` through `VAL-05`.</action>
  <verify>The summary contains `## Deviations`, `## Requirement Coverage`, `## Residual Risk`, and `## Gaps` sections.</verify>
  <acceptance_criteria>
    - No new Phase 5 feature scope is introduced.
    - Findings are traceable to commands, source checks, or user-confirmed hardware evidence.
    - Residual risk distinguishes manual hardware limitations from build/static validation.
    - The summary is sufficient for `gsd-verify-work` or a verifier to produce `05-VERIFICATION.md`.
  </acceptance_criteria>
  <done>Phase 5 has closeout-ready validation notes.</done>
</task>

</tasks>

<verification>
- [ ] `.\scripts\build.ps1`
- [ ] `Test-Path build\fish_pump_relay_timer_control.bin`
- [ ] `rg "APP_TEMPLATE_PUMP_FLOAT_GPIO|APP_TEMPLATE_PUMP_RELAY_GPIO|APP_TEMPLATE_PUMP_RELAY_ACTIVE_LOW|APP_TEMPLATE_PUMP_FLOAT_ACTIVE_LOW" components/app_config/app_config.h`
- [ ] `rg "select_timer_for_float_locked|PUMP_CONTROL_TIMER_1|PUMP_CONTROL_TIMER_2|set_relay_energized_locked|pump_control_stop" components/pump_control/pump_control.c`
- [ ] `rg "/api/pump/config|/api/pump/status|/api/pump/start|/api/pump/stop|/api/wifi/scan|/api/wifi/connect|/api/status" main/web_server.c`
- [ ] `rg "timer1-on-min|timer2-on-min|pump-float-state|pump-relay-state|pump-start-btn|pump-stop-btn|/status|/wifi" main/static/dashboard.html`
- [ ] `rg "/api/pump/status|/api/pump/config|/api/pump/start|/api/pump/stop|ลูกลอย|รีเลย์" main/static/app.js`
- [ ] `rg "http://|https://|cdn|Tailwind|Google Fonts|Material Symbols" main/static` returns no matches.
- [ ] `rg "VAL-02|VAL-03|VAL-04|VAL-05" .planning/phases/05-hardware-validation-and-regression-pass/05-01-SUMMARY.md`
</verification>

<success_criteria>
- Current firmware build status is documented.
- User-confirmed hardware evidence closes float/timer switching, relay ON/OFF behavior, Stop inactive safety, reboot persistence, and web/Wi-Fi reachability.
- Static checks show the pump control, web UI, Wi-Fi, login/status, and local-only frontend surfaces remain present.
- Any findings are documented without expanding Phase 5 into new feature development.
- The summary gives the verifier enough evidence to close `VAL-02`, `VAL-03`, `VAL-04`, and `VAL-05`.
</success_criteria>

<output>
After completion, create `.planning/phases/05-hardware-validation-and-regression-pass/05-01-SUMMARY.md`.
</output>
