---
phase: 10-owner-dashboard-hardware-install-ui-and-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - main/static/dashboard.html
  - main/static/style.css
  - main/static/app.js
autonomous: true
requirements:
  - UI-01
user_setup: []
must_haves:
  truths:
    - "Phase 10 has no CONTEXT.md; use ROADMAP.md, REQUIREMENTS.md, 10-RESEARCH.md, the owner/installer UI note, and previous phase summaries as the planning contract."
    - "The owner dashboard remains daily-operation focused; GPIO editing is not added to the dashboard."
    - "The dashboard must show pump enabled/running state, float state, active timer/relay, countdown, temperature, cooling relay state, threshold, auto-enable setting, and sensor fault state."
    - "Cooling Test ON is runtime-only and must call /api/cooling/mode, not /api/cooling/config."
    - "Cooling config save must submit all required fields from the loaded config because the backend validates a full replacement payload."
    - "Static assets must remain offline-only: no CDN, external fonts, web packages, or internet dependencies."
  artifacts:
    - path: "main/static/dashboard.html"
      provides: "Owner-facing pump and cooling operation surface"
      contains: "cooling-temperature"
    - path: "main/static/app.js"
      provides: "Vanilla JS polling and mutation wiring for pump and cooling APIs"
      contains: "/api/cooling/status"
  key_links:
    - from: "main/static/app.js"
      to: "main/web_server.c"
      via: "Consumes Phase 9 cooling config/status/mode JSON fields"
      pattern: "/api/cooling/config"
---

<objective>
Extend the existing pump-first dashboard into the owner daily-operation surface for pump and cooling control.

Purpose: Let the owner see the full runtime state in one place without mixing installer GPIO editing into the dashboard.
Output: `/dashboard` shows pump enabled/running state, float state, active timer/relay, countdown, temperature, cooling relay state, cooling threshold, auto-enable setting, sensor fault state, and safe cooling mode controls while preserving existing pump timer controls.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/notes/owner-installer-ui-shape.md
@.planning/phases/10-owner-dashboard-hardware-install-ui-and-validation/10-RESEARCH.md
@.planning/phases/07-dual-timer-relay-pump-runtime/07-VERIFICATION.md
@.planning/phases/08-ds18b20-cooling-runtime/08-01-SUMMARY.md
@.planning/phases/08-ds18b20-cooling-runtime/08-02-SUMMARY.md
@.planning/phases/08-ds18b20-cooling-runtime/08-03-SUMMARY.md
@.planning/phases/09-authenticated-hardware-and-cooling-apis/09-VERIFICATION.md
@main/static/dashboard.html
@main/static/app.js
@main/static/style.css
@main/web_server.c
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cooling status and control markup to the dashboard</name>
  <files>main/static/dashboard.html, main/static/style.css</files>
  <read_first>main/static/dashboard.html, main/static/style.css, .planning/notes/owner-installer-ui-shape.md, .planning/phases/10-owner-dashboard-hardware-install-ui-and-validation/10-RESEARCH.md</read_first>
  <action>Add a cooling operation section after the pump runtime area and before lower-priority system status. Include stable IDs for `cooling-sync-state`, `cooling-temperature`, `cooling-relay-state`, `cooling-mode-state`, `cooling-sensor-state`, `cooling-fault-state`, `cooling-threshold`, `cooling-hysteresis`, `cooling-auto-enable-state`, `cooling-blocked-reason`, `cooling-lockout`, and `cooling-test-remaining`. Add controls for Auto, Force OFF, and Test ON, plus a small cooling config form for threshold, hysteresis, auto-enable, and test timeout. Keep GPIO edit controls off this page and link to `/hardware` for installer settings.</action>
  <verify>`rg "cooling-temperature|cooling-relay-state|cooling-sensor-state|cooling-auto-enable|cooling-mode-auto|cooling-mode-force-off|cooling-test-on|/hardware" main/static/dashboard.html main/static/style.css`</verify>
  <acceptance_criteria>
    - Cooling information is visible without leaving `/dashboard`.
    - Sensor fault and invalid temperature states have dedicated visible targets.
    - Test ON is presented as a bounded runtime action, not a saved boot mode.
    - GPIO editing is deferred to a Hardware/Install link.
    - Layout stays responsive and does not crowd or resize the pump countdown panel.
  </acceptance_criteria>
  <done>Dashboard markup and styles include the cooling operation surface.</done>
</task>

<task type="auto">
  <name>Task 2: Wire cooling config load/save and runtime mode actions</name>
  <files>main/static/app.js</files>
  <read_first>main/static/app.js, main/web_server.c, .planning/phases/09-authenticated-hardware-and-cooling-apis/09-02-SUMMARY.md</read_first>
  <action>Add dashboard JS state and helpers for cooling. On dashboard init, call `loadCoolingConfig()` and start a cooling status poll using `/api/cooling/status` at a slower interval than pump countdown polling. Implement `saveCoolingConfig()` against `/api/cooling/config` with a full payload containing `threshold_c_x10`, `hysteresis_c_x10`, `auto_enable`, `mode`, `test_timeout_sec`, `compressor_min_off_sec`, and `cooling_relay_polarity` from loaded state plus edited fields. Implement Auto and Force OFF buttons through `/api/cooling/mode`; implement Test ON through `/api/cooling/mode` with `mode:"test_on"` only. Never POST `mode:"test_on"` to `/api/cooling/config`.</action>
  <verify>`rg "/api/cooling/config|/api/cooling/status|/api/cooling/mode|test_on|threshold_c_x10|hysteresis_c_x10|cooling_relay_polarity" main/static/app.js`</verify>
  <acceptance_criteria>
    - Cooling config loads from the backend before config save is enabled.
    - Config save sends all backend-required fields and preserves loaded compressor min-off and relay polarity when not edited.
    - Test ON is runtime-only and calls `/api/cooling/mode`.
    - Auto and Force OFF mode actions refresh status after completion.
    - API errors render into visible dashboard error text and do not break pump controls.
  </acceptance_criteria>
  <done>Cooling API consumption is wired into the dashboard.</done>
</task>

<task type="auto">
  <name>Task 3: Render cooling status, stale state, and owner-safe labels</name>
  <files>main/static/app.js, main/static/style.css</files>
  <read_first>main/static/app.js, main/static/style.css, components/cooling_control/cooling_control.h, main/web_server.c</read_first>
  <action>Add render helpers for cooling mode, sensor state, fault code, relay state, blocked reason, lockout countdown, Test ON countdown, threshold, hysteresis, and temperature validity. Display temperature as `--` when `temperature_valid` is false. Show sensor fault state prominently and keep relay OFF wording explicit. Disable Test ON while a cooling request is pending or when status is stale. Use labels that make Auto, Force OFF, and Test ON distinct from boot auto-enable.</action>
  <verify>`rg "renderCooling|cooling.*stale|temperature_valid|sensor_state|fault_code|blocked_reason|lockout_remaining_sec|test_remaining_sec" main/static/app.js main/static/style.css`</verify>
  <acceptance_criteria>
    - Dashboard does not show stale or invalid temperature as a real reading.
    - Sensor fault state and cooling relay OFF state are both understandable.
    - Compressor lockout and Test ON remaining time are visible when present.
    - Auto-enable is presented as boot behavior, not immediate relay command.
    - Pump rendering and countdown prediction continue to work.
  </acceptance_criteria>
  <done>Cooling status rendering is owner-safe and resilient to stale/fault data.</done>
</task>

<task type="auto">
  <name>Task 4: Preserve dashboard system summary and local-only static assets</name>
  <files>main/static/dashboard.html, main/static/app.js, main/static/style.css</files>
  <read_first>main/static/dashboard.html, main/static/app.js, main/static/style.css</read_first>
  <action>Keep existing quick system/Wi-Fi status and links to `/status` and `/wifi`. Add `/hardware` as an installer link without making it the primary daily action. Confirm no CDN, external font, remote script, icon font, or package asset is introduced. Ensure app initialization still only starts dashboard polling on `/dashboard`.</action>
  <verify>`rg "/status|/wifi|/hardware|dash-chip|dash-heap-pct|dash-rssi|dash-ap-clients" main/static/dashboard.html main/static/app.js`; `rg "http://|https://|cdn|Tailwind|Google Fonts|Material Symbols" main/static` returns no matches.</verify>
  <acceptance_criteria>
    - Existing Wi-Fi and system pages remain discoverable.
    - Hardware/Install is linked but not mixed into daily controls.
    - Static frontend remains offline-capable in SoftAP mode.
    - Dashboard init does not start cooling timers on non-dashboard pages.
  </acceptance_criteria>
  <done>The dashboard remains local-only and keeps existing navigation/status behavior.</done>
</task>

</tasks>

<verification>
- [ ] `rg "cooling-temperature|cooling-relay-state|cooling-sensor-state|cooling-auto-enable|cooling-test-on" main/static/dashboard.html main/static/style.css`
- [ ] `rg "/api/cooling/config|/api/cooling/status|/api/cooling/mode|threshold_c_x10|hysteresis_c_x10|test_on" main/static/app.js`
- [ ] Inspect `main/static/app.js` to confirm `mode:"test_on"` is used only with `/api/cooling/mode`.
- [ ] `rg "/status|/wifi|/hardware|dash-chip|dash-heap-pct|dash-rssi|dash-ap-clients" main/static/dashboard.html main/static/app.js`
- [ ] `rg "http://|https://|cdn|Tailwind|Google Fonts|Material Symbols" main/static` returns no matches.
</verification>

<success_criteria>
- UI-01 is covered for pump, float, timer, relay, countdown, temperature, cooling relay, threshold, auto-enable, and sensor fault display.
- Cooling controls use the authenticated Phase 9 APIs without persisting Test ON.
- Existing pump timer controls and countdown behavior are preserved.
- Dashboard remains daily-operation focused and offline-capable.
</success_criteria>

<output>
After completion, create `.planning/phases/10-owner-dashboard-hardware-install-ui-and-validation/10-01-SUMMARY.md`.
</output>
