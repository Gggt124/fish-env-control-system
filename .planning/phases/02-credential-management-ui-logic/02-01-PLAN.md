---
phase: 02
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/nvs_store/nvs_store.h
  - components/nvs_store/nvs_store.c
  - components/session/session.h
  - components/session/session.c
autonomous: true
requirements:
  - AUTH-05
  - AUTH-06
must_haves:
  truths:
    - "System can load and save user credentials to NVS."
    - "System can forcefully invalidate all active sessions by rotating the JWT secret."
  artifacts:
    - path: "components/nvs_store/nvs_store.c"
      provides: "nvs_store_get_credentials, nvs_store_set_credentials"
    - path: "components/session/session.c"
      provides: "session_regenerate_secret"
  key_links:
    - from: "components/nvs_store/nvs_store.c"
      to: "nvs"
      via: "nvs_open, nvs_set_str"
      pattern: "nvs_set_str.*admin_pass"
---

<objective>
Implement core backend state for credential management.

Purpose: Allow secure persistence of administrator credentials and provide a native mechanism to instantly invalidate all JWT sessions when credentials change.
Output: NVS credential functions and session secret rotation function.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-credential-management-ui-logic/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add NVS Credential Storage</name>
  <files>components/nvs_store/nvs_store.h, components/nvs_store/nvs_store.c</files>
  <action>Add `nvs_store_get_credentials` and `nvs_store_set_credentials` to manage admin credentials in the "wifi_cfg" namespace. 
`nvs_store_get_credentials` must take pointers for username and password buffers and their sizes. It should attempt to load "admin_user" and "admin_pass" from NVS. If either is not found (or on error), it MUST copy `APP_TEMPLATE_DEFAULT_USERNAME` and `APP_TEMPLATE_DEFAULT_PASSWORD` into the buffers as fallbacks.
`nvs_store_set_credentials` must take the new username and password, validate they are not empty, and save them to NVS as "admin_user" and "admin_pass", followed by `nvs_commit()`.
Per D-02.</action>
  <verify>
    <automated>idf.py build</automated>
  </verify>
  <done>NVS wrapper has functions to get/set admin credentials.</done>
</task>

<task type="auto">
  <name>Task 2: Add Session Secret Rotation</name>
  <files>components/session/session.h, components/session/session.c</files>
  <action>Add `void session_regenerate_secret(void)` to session.c.
This function MUST call `bootloader_random_enable()`, overwrite the internal `s_jwt_secret` array using `esp_random()`, call `bootloader_random_disable()`, and then call `nvs_store_set_jwt_secret()` to persist the new secret.
This directly implements the global logout behavior by invalidating all existing JWT signatures.
Per D-03.</action>
  <verify>
    <automated>idf.py build</automated>
  </verify>
  <done>Session system supports secret regeneration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Memory->NVS | Plaintext credentials are saved to ESP32 flash |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | NVS Credential Storage | accept | Flash encryption is out of scope for local dev environment; acceptable risk. |
</threat_model>

<verification>
idf.py build succeeds with no warnings.
</verification>

<success_criteria>
Credentials can be saved/loaded, and JWT secret can be regenerated.
</success_criteria>

<output>
Create `.planning/phases/02-credential-management-ui-logic/02-01-SUMMARY.md` when done
</output>
