---
phase: 02
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - main/static/index.html
  - main/static/style.css
  - main/static/app.js
autonomous: true
requirements:
  - AUTH-05
  - AUTH-07
must_haves:
  truths:
    - "Users can open a Change Password modal from the Dashboard Settings area."
    - "The modal prompts for current password, new username, and new password."
    - "Form submission fetches a nonce, updates credentials, and redirects to Login."
  artifacts:
    - path: "main/static/index.html"
      provides: "Change password modal"
    - path: "main/static/app.js"
      provides: "doChangePassword() flow"
  key_links:
    - from: "main/static/app.js"
      to: "/api/auth/credentials"
      via: "apiPost"
      pattern: "apiPost\\('/api/auth/credentials'"
---

<objective>
Implement the Credential Management UI logic.

Purpose: Provide the dashboard operator a way to change their credentials through a native popup modal.
Output: HTML modal structure, required CSS styling, and JavaScript API integration.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-credential-management-ui-logic/02-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add HTML Modal and CSS</name>
  <files>main/static/index.html, main/static/style.css</files>
  <action>In `index.html`, add a "Change Password" button in the Quick Links area (or a dedicated Security section) to trigger the modal.
Add the HTML for a new popup modal `id="password-modal"` containing a form with `current_password`, `new_username`, and `new_password` inputs.
Follow `02-UI-SPEC.md`: Use a Primary action button ("Change Password"), include a warning label "Note: Changing your password will log you out of all devices immediately." (styled carefully, maybe using the 'destructive' class if appropriate, or just small text), and an error state container.
Update `style.css` if any additional modal utility classes are needed, though existing modal styles (e.g., from wifi disconnect confirm) might suffice.
Per D-01, D-02.</action>
  <verify>
    <automated>idf.py build</automated>
  </verify>
  <done>HTML structure for password change is embedded in index.html.</done>
</task>

<task type="auto">
  <name>Task 2: Add Frontend API Logic</name>
  <files>main/static/app.js</files>
  <action>In `app.js`, add DOM listeners for the new "Change Password" modal (open/close).
Add form submit handler `doChangePassword(e)`.
The logic MUST:
1. `apiGet('/api/auth/nonce')` to fetch the one-time nonce.
2. `apiPost('/api/auth/credentials', { nonce, current_password, new_username, new_password })`.
3. If successful, wait 2-3 seconds, then call `doLogout()` or redirect to `/login` to clear the local session state (as the server secret rotated).
4. If error (e.g. invalid current password), show "Invalid current password. Please try again." in the error container.
Per D-03, D-04, 02-UI-SPEC.md.</action>
  <verify>
    <automated>idf.py build</automated>
  </verify>
  <done>JS logic orchestrates nonce fetching, POSTing credentials, and logout redirect.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser->API | Inputs from DOM are sent to the device |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Spoofing | Frontend Form | accept | UI logic assumes valid auth cookie is present; API enforces the actual check. |
</threat_model>

<verification>
idf.py build succeeds with no warnings.
</verification>

<success_criteria>
Dashboard has a working Change Password flow that interacts with the backend.
</success_criteria>

<output>
Create `.planning/phases/02-credential-management-ui-logic/02-03-SUMMARY.md` when done
</output>
