---
phase: 10-owner-dashboard-hardware-install-ui-and-validation
plan: 03
type: execute
wave: 3
depends_on:
  - 10-01
  - 10-02
files_modified:
  - README.md
  - docs/components.md
  - docs/development-notes.md
  - docs/hardware.md
  - build/
  - .planning/phases/10-owner-dashboard-hardware-install-ui-and-validation/10-03-SUMMARY.md
autonomous: false
requirements:
  - UI-01
  - UI-02
user_setup:
  - ESP32 DevKit V1 30-pin connected for flash/monitor when hardware validation is run.
  - Float switch on active/default GPIO32 as active-low switch to GND.
  - Pump Relay 1 on active/default GPIO26 and Pump Relay 2 on active/default GPIO27 with configured polarity.
  - DS18B20 powered mode on active/default GPIO33 with external 4.7 kOhm pull-up to 3.3 V.
  - Cooling relay on active/default GPIO25 with configured polarity.
must_haves:
  truths:
    - "Plan 03 depends on dashboard cooling UI from 10-01 and Hardware/Install UI from 10-02."
    - "The automated validation gate is ESP-IDF build through `./scripts/build.ps1` and generated `build/fish_pump_relay_timer_control.bin`."
    - "Manual hardware validation must distinguish build/source validation from flashed-device evidence."
    - "Manual validation must cover dual pump relays, DS18B20 sensor fault safe-off, cooling relay behavior, hardware-map reboot persistence, and Wi-Fi/setup regression."
    - "If hardware is not available during execution, record manual validation as not-run instead of marking it passed."
  artifacts:
    - path: ".planning/phases/10-owner-dashboard-hardware-install-ui-and-validation/10-03-SUMMARY.md"
      provides: "Build/source/manual validation evidence for v1.1 closeout"
      contains: "UI-01"
    - path: "README.md"
      provides: "Operator-facing documentation for dashboard, hardware install, and validation"
      contains: "Hardware/Install"
  key_links:
    - from: ".planning/phases/10-owner-dashboard-hardware-install-ui-and-validation/10-03-SUMMARY.md"
      to: ".planning/REQUIREMENTS.md"
      via: "Requirement traceability for UI-01 and UI-02"
      pattern: "UI-01"
---

<objective>
Validate and document the completed v1.1 owner dashboard, Hardware/Install UI, and integrated pump/cooling behavior.

Purpose: Close Phase 10 with build evidence, source checks, operator docs, and a manual hardware validation record that clearly separates repository validation from device validation.
Output: Updated docs plus `10-03-SUMMARY.md` containing ESP-IDF build result, source check results, manual hardware validation evidence or explicit not-run status, and requirement coverage for UI-01 and UI-02.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-owner-dashboard-hardware-install-ui-and-validation/10-RESEARCH.md
@.planning/phases/10-owner-dashboard-hardware-install-ui-and-validation/10-01-PLAN.md
@.planning/phases/10-owner-dashboard-hardware-install-ui-and-validation/10-02-PLAN.md
@.planning/phases/07-dual-timer-relay-pump-runtime/07-VERIFICATION.md
@.planning/phases/08-ds18b20-cooling-runtime/08-01-SUMMARY.md
@.planning/phases/08-ds18b20-cooling-runtime/08-02-SUMMARY.md
@.planning/phases/08-ds18b20-cooling-runtime/08-03-SUMMARY.md
@.planning/phases/09-authenticated-hardware-and-cooling-apis/09-VERIFICATION.md
@README.md
@docs/components.md
@docs/development-notes.md
@docs/hardware.md
@main/static/dashboard.html
@main/static/hardware.html
@main/static/app.js
@main/static/style.css
@main/web_server.c
@scripts/build.ps1
@scripts/flash.ps1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run source-level UI/API coverage checks</name>
  <files>.planning/phases/10-owner-dashboard-hardware-install-ui-and-validation/10-03-SUMMARY.md</files>
  <read_first>.planning/REQUIREMENTS.md, main/static/dashboard.html, main/static/hardware.html, main/static/app.js, main/web_server.c</read_first>
  <action>Run focused `rg` checks for dashboard cooling fields, Hardware/Install route/embed, hardware map API consumption, cooling API consumption, no external frontend dependencies, and route auth. Record command results in the summary. Inspect results for false positives rather than blindly marking pass.</action>
  <verify>Run the plan-level `rg` commands for dashboard, hardware, APIs, route auth, and external dependencies.</verify>
  <acceptance_criteria>
    - UI-01 fields are represented in dashboard markup/JS.
    - UI-02 fields and `/hardware` route are represented in markup/JS/web server.
    - Cooling and hardware APIs are consumed by frontend JS.
    - No external CDN/internet dependency is present under `main/static`.
    - Protected route and mutation auth patterns remain intact.
  </acceptance_criteria>
  <done>Repository-level UI/API coverage is documented.</done>
</task>

<task type="auto">
  <name>Task 2: Run ESP-IDF build validation</name>
  <files>build/, .planning/phases/10-owner-dashboard-hardware-install-ui-and-validation/10-03-SUMMARY.md</files>
  <read_first>AGENTS.md, scripts/build.ps1</read_first>
  <action>Run `.\\scripts\\build.ps1` from the repository root using the documented Windows ESP-IDF workflow. Confirm whether `build\\fish_pump_relay_timer_control.bin` exists and record the build result, binary path, and size/free-space information if available. If ESP-IDF is unavailable in the execution environment, record the exact blocker and do not substitute Arduino/PlatformIO or a non-ESP-IDF build.</action>
  <verify>`.\\scripts\\build.ps1` exits successfully and `Test-Path build\\fish_pump_relay_timer_control.bin` returns `True`, or the summary records the exact environment blocker.</verify>
  <acceptance_criteria>
    - Build validation uses ESP-IDF only.
    - Generated binary path is recorded on success.
    - Build failure/environment blocker includes enough detail for retry.
    - Build artifacts are not committed unless the repository already tracks them.
  </acceptance_criteria>
  <done>Firmware build state is known and documented.</done>
</task>

<task type="manual">
  <name>Task 3: Run flashed-device v1.1 hardware validation</name>
  <files>.planning/phases/10-owner-dashboard-hardware-install-ui-and-validation/10-03-SUMMARY.md</files>
  <read_first>AGENTS.md, scripts/flash.ps1, docs/hardware.md, .planning/phases/10-owner-dashboard-hardware-install-ui-and-validation/10-RESEARCH.md</read_first>
  <action>When hardware is available, flash the build to the ESP32 and validate the full v1.1 flow. Record the COM port, firmware build, active/pending GPIO map, relay polarity, and observed results. Validate: SoftAP/login/dashboard load; Float ON selects Timer 1/Relay 1; Float OFF selects Timer 2/Relay 2; Relay 1 and Relay 2 are never ON together; Stop forces both pump relays OFF; DS18B20 missing/unreadable state forces cooling relay OFF and shows sensor fault; valid sensor readings plus threshold/hysteresis drive cooling relay; Force OFF keeps cooling relay OFF; Test ON is bounded and lockout-aware; Hardware/Install saves pending GPIO changes with confirmation; reboot-required appears before reboot; active map changes only after reboot if the pending map is applied by firmware boot flow; Wi-Fi scan/connect still works.</action>
  <verify>Manual observations are recorded in the summary with pass/fail/not-run for each validation item.</verify>
  <acceptance_criteria>
    - Manual validation does not connect a real pump/compressor load until relay polarity and safe-off behavior are confirmed.
    - Dual pump relay behavior is validated against the binary float switch.
    - Cooling fault safe-off and cooling relay behavior are validated.
    - Hardware map pending/reboot behavior is validated or explicitly marked not-run.
    - Wi-Fi/setup regression is validated after UI changes.
  </acceptance_criteria>
  <done>Device validation evidence is captured, or the summary explicitly records that hardware validation was not run.</done>
</task>

<task type="auto">
  <name>Task 4: Update operator docs and write closeout summary</name>
  <files>README.md, docs/components.md, docs/development-notes.md, docs/hardware.md, .planning/phases/10-owner-dashboard-hardware-install-ui-and-validation/10-03-SUMMARY.md</files>
  <read_first>README.md, docs/components.md, docs/development-notes.md, docs/hardware.md, .planning/REQUIREMENTS.md</read_first>
  <action>Update concise docs for the completed Phase 10 surfaces: dashboard pump/cooling operation, Hardware/Install pending GPIO flow, cooling Test ON runtime behavior, and manual validation expectations. Create `10-03-SUMMARY.md` in the established GSD summary style with accomplishments, checks, build result, manual hardware validation table, requirement coverage for UI-01/UI-02, deviations, residual risk, and next steps. Do not claim manual validation passed unless it actually ran.</action>
  <verify>`rg "Dashboard|Hardware/Install|/hardware|cooling|pending|reboot|required|Test ON|UI-01|UI-02" README.md docs .planning/phases/10-owner-dashboard-hardware-install-ui-and-validation/10-03-SUMMARY.md`</verify>
  <acceptance_criteria>
    - Docs describe owner dashboard and Hardware/Install behavior accurately.
    - Pending GPIO and reboot-required semantics are documented.
    - Summary maps UI-01 and UI-02 to implementation and evidence.
    - Residual risks distinguish unrun hardware checks from source/build checks.
    - No out-of-scope feature is documented as implemented.
  </acceptance_criteria>
  <done>Phase 10 closeout docs and validation summary are ready for verifier review.</done>
</task>

</tasks>

<verification>
- [ ] `rg "cooling-temperature|cooling-relay-state|cooling-sensor-state|cooling-auto-enable|cooling-test-on" main/static/dashboard.html main/static/app.js main/static/style.css`
- [ ] `rg "hardware.html|_binary_hardware_html|/hardware|handle_get_hardware|hardware-wiring|hardware-save-map" main/CMakeLists.txt main/web_server.c main/static`
- [ ] `rg "/api/cooling/config|/api/cooling/status|/api/cooling/mode|/api/hardware/map|confirm_reboot_required|reboot_required" main/static/app.js main/static/hardware.html`
- [ ] `rg "require_auth|is_same_origin\\(req, false\\)|/api/hardware/map|/api/cooling/config|/api/cooling/mode" main/web_server.c`
- [ ] `rg "http://|https://|cdn|Tailwind|Google Fonts|Material Symbols" main/static` returns no matches.
- [ ] `.\\scripts\\build.ps1`
- [ ] `Test-Path build\\fish_pump_relay_timer_control.bin`
- [ ] Manual hardware validation table completed in `10-03-SUMMARY.md`, or explicitly marked not-run with blocker.
</verification>

<success_criteria>
- ESP-IDF build passes after Phase 10 UI/route changes.
- UI-01 and UI-02 are traceably covered by implementation and summary evidence.
- Manual validation covers, or explicitly blocks, dual pump relays, DS18B20 fault handling, cooling relay behavior, pending GPIO reboot behavior, and Wi-Fi/setup regression.
- Operator docs reflect the completed v1.1 owner and installer workflows.
</success_criteria>

<output>
After completion, create `.planning/phases/10-owner-dashboard-hardware-install-ui-and-validation/10-03-SUMMARY.md`.
</output>
