---
phase: 10-owner-dashboard-hardware-install-ui-and-validation
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - main/CMakeLists.txt
  - main/web_server.c
  - main/static/hardware.html
  - main/static/dashboard.html
  - main/static/status.html
  - main/static/wifi.html
  - main/static/style.css
  - main/static/app.js
autonomous: true
requirements:
  - UI-02
user_setup: []
must_haves:
  truths:
    - "Hardware/Install is a protected local page at `/hardware`; it is owner-accessible but intentionally advanced."
    - "EMBED_FILES symbol names strip the directory prefix, so use `_binary_hardware_html_start` and `_binary_hardware_html_end`."
    - "GPIO edits use only safe options returned by `/api/hardware/map`; no freeform GPIO numeric entry is allowed."
    - "Hardware map POST payload is `{map:{...}, confirm_reboot_required:true}` and saves pending values only."
    - "Active values remain runtime-active until reboot; pending values must be visually distinct and paired with reboot-required messaging."
    - "The page order is wiring visualization first, GPIO summary next, technical pinout secondary."
  artifacts:
    - path: "main/static/hardware.html"
      provides: "Hardware/Install UI shell and stable DOM IDs"
      contains: "hardware-wiring"
    - path: "main/static/app.js"
      provides: "Hardware map load/render/save behavior"
      contains: "/api/hardware/map"
  key_links:
    - from: "main/static/app.js"
      to: "main/web_server.c"
      via: "Consumes active/pending/options/reboot_required hardware map API fields"
      pattern: "confirm_reboot_required"
---

<objective>
Add a protected Hardware/Install page that helps an owner or installer wire the ESP32 safely and save pending GPIO map changes.

Purpose: Make wiring and hardware configuration understandable without exposing unsafe freeform pin entry or changing runtime pins before reboot.
Output: `/hardware` serves an embedded offline page with wiring visualization, active/pending GPIO summary, safe dropdown editors, confirmation guardrail, reboot-required messaging, and a secondary technical pinout view.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/notes/owner-installer-ui-shape.md
@.planning/phases/10-owner-dashboard-hardware-install-ui-and-validation/10-RESEARCH.md
@.planning/phases/06-hardware-contract-and-persistent-map-foundation/06-VERIFICATION.md
@.planning/phases/09-authenticated-hardware-and-cooling-apis/09-01-SUMMARY.md
@.planning/phases/09-authenticated-hardware-and-cooling-apis/09-VERIFICATION.md
@main/CMakeLists.txt
@main/web_server.c
@main/static/dashboard.html
@main/static/status.html
@main/static/wifi.html
@main/static/app.js
@main/static/style.css
@docs/hardware.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add protected `/hardware` page route and embedded static file</name>
  <files>main/CMakeLists.txt, main/web_server.c, main/static/hardware.html</files>
  <read_first>main/CMakeLists.txt, main/web_server.c, AGENTS.md, .planning/phases/10-owner-dashboard-hardware-install-ui-and-validation/10-RESEARCH.md</read_first>
  <action>Create `main/static/hardware.html` with the existing app shell pattern and no external assets. Add it to `main/CMakeLists.txt` `EMBED_FILES`. In `web_server.c`, declare `_binary_hardware_html_start` and `_binary_hardware_html_end`, add `handle_get_hardware()` matching the auth/redirect behavior of `handle_get_dashboard()`, and register `GET /hardware`. Do not use `_binary_static_hardware_html_*` symbol names.</action>
  <verify>`rg "hardware.html|_binary_hardware_html_start|_binary_hardware_html_end|handle_get_hardware|/hardware" main/CMakeLists.txt main/web_server.c main/static/hardware.html`</verify>
  <acceptance_criteria>
    - `/hardware` is protected by session auth and redirects unauthenticated users to `/login`.
    - `hardware.html` is embedded through ESP-IDF `EMBED_FILES`.
    - Embedded symbol names use filename-only symbols.
    - No route capacity regression is introduced.
  </acceptance_criteria>
  <done>The protected Hardware/Install page can be served by the firmware.</done>
</task>

<task type="auto">
  <name>Task 2: Build wiring-first Hardware/Install markup and navigation</name>
  <files>main/static/hardware.html, main/static/dashboard.html, main/static/status.html, main/static/wifi.html, main/static/style.css</files>
  <read_first>main/static/dashboard.html, main/static/status.html, main/static/wifi.html, main/static/style.css, .planning/notes/owner-installer-ui-shape.md, docs/hardware.md</read_first>
  <action>Implement the page order as wiring visualization first, GPIO summary second, technical pinout secondary. Use stable IDs/classes for `hardware-wiring`, `hardware-reboot-banner`, `hardware-active-summary`, `hardware-pending-summary`, `hardware-map-form`, `hardware-confirm-reboot`, `hardware-save-map`, and a technical pinout tab/section. Add a Hardware/Install nav link to dashboard, status, and Wi-Fi pages, with active state on `hardware.html`. The wiring visualization should name float input, pump Relay 1, pump Relay 2, DS18B20 data, and cooling relay roles with active/pending GPIO labels.</action>
  <verify>`rg "hardware-wiring|hardware-reboot-banner|hardware-active-summary|hardware-pending-summary|hardware-map-form|hardware-confirm-reboot|hardware-save-map|technical-pinout" main/static/hardware.html main/static/style.css`; `rg "/hardware|Hardware" main/static/dashboard.html main/static/status.html main/static/wifi.html`</verify>
  <acceptance_criteria>
    - Wiring guidance appears before editor controls.
    - Active and pending values have separate visual areas.
    - Technical pinout details are secondary, not the first thing on the page.
    - Navigation exposes Hardware/Install from existing protected pages.
    - Page remains usable on mobile without horizontal overflow.
  </acceptance_criteria>
  <done>Hardware/Install page structure matches the owner/installer UI shape.</done>
</task>

<task type="auto">
  <name>Task 3: Wire safe GPIO dropdowns, active/pending rendering, and pending save</name>
  <files>main/static/app.js, main/static/hardware.html</files>
  <read_first>main/static/app.js, main/static/hardware.html, main/web_server.c, .planning/phases/09-authenticated-hardware-and-cooling-apis/09-01-SUMMARY.md</read_first>
  <action>Add `initHardwareInstall()` to the app init router for `/hardware`. Implement `loadHardwareMap()` with `GET /api/hardware/map`, render active/pending maps, populate role dropdowns from `options.float_input_gpio`, `options.pump_relay1_gpio`, `options.pump_relay2_gpio`, `options.ds18b20_gpio`, and `options.cooling_relay_gpio`, and select pending values when `pending_valid` is true otherwise active values. Implement dirty-state tracking and save through `POST /api/hardware/map` with `{map, confirm_reboot_required:true}` only when `hardware-confirm-reboot` is checked. Do not allow manual numeric GPIO typing.</action>
  <verify>`rg "initHardwareInstall|loadHardwareMap|/api/hardware/map|confirm_reboot_required|pending_valid|reboot_required|hardware-confirm-reboot|hardware-save-map" main/static/app.js main/static/hardware.html`</verify>
  <acceptance_criteria>
    - Dropdown options come from backend safe option arrays.
    - No freeform GPIO input is introduced.
    - Pending values are shown and edited separately from active runtime values.
    - Save is blocked until the confirmation checkbox is checked.
    - Successful save refreshes active/pending/reboot-required display from the server response.
  </acceptance_criteria>
  <done>Hardware page safely consumes the active/pending hardware map API.</done>
</task>

<task type="auto">
  <name>Task 4: Render reboot-required messaging and technical pinout metadata</name>
  <files>main/static/app.js, main/static/style.css, main/static/hardware.html</files>
  <read_first>main/static/app.js, main/static/style.css, components/hardware_map/hardware_map.h, docs/hardware.md</read_first>
  <action>Add rendering helpers for role labels, GPIO option labels, default markers, input/output/internal-pull capability flags, pending-vs-active comparisons, and reboot-required messaging. The reboot banner must explicitly say pending values do not become active until reboot. The technical pinout view should list each role's safe options and capability flags from API metadata without duplicating hardcoded safe GPIO lists.</action>
  <verify>`rg "reboot_required|pending.*active|input_capable|output_capable|internal_pull_capable|is_default|technical.*pinout|safe.*option" main/static/app.js main/static/hardware.html main/static/style.css`</verify>
  <acceptance_criteria>
    - Reboot-required messaging is visible whenever `reboot_required` is true.
    - Users can distinguish active runtime pins from pending saved pins.
    - Technical pinout data is generated from API option metadata.
    - The UI does not duplicate the firmware's safe GPIO list as an independent source of truth.
  </acceptance_criteria>
  <done>Install UI explains pending GPIO behavior and pin safety metadata.</done>
</task>

</tasks>

<verification>
- [ ] `rg "hardware.html|_binary_hardware_html_start|_binary_hardware_html_end|handle_get_hardware|/hardware" main/CMakeLists.txt main/web_server.c main/static`
- [ ] `rg "hardware-wiring|hardware-reboot-banner|hardware-active-summary|hardware-pending-summary|hardware-map-form|hardware-confirm-reboot|hardware-save-map|technical-pinout" main/static/hardware.html main/static/style.css`
- [ ] `rg "initHardwareInstall|loadHardwareMap|/api/hardware/map|confirm_reboot_required|pending_valid|reboot_required" main/static/app.js`
- [ ] `rg "input_capable|output_capable|internal_pull_capable|is_default" main/static/app.js main/static/hardware.html`
- [ ] `rg "http://|https://|cdn|Tailwind|Google Fonts|Material Symbols" main/static` returns no matches.
</verification>

<success_criteria>
- UI-02 is covered with wiring visualization, GPIO summary, and secondary technical pinout view.
- GPIO changes use safe dropdown enum options and save only as pending values.
- Reboot-required messaging and confirmation guardrails are present.
- Hardware/Install is authenticated and works without internet access.
</success_criteria>

<output>
After completion, create `.planning/phases/10-owner-dashboard-hardware-install-ui-and-validation/10-02-SUMMARY.md`.
</output>
