---
phase: 06-hardware-contract-and-persistent-map-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/hardware_map/CMakeLists.txt
  - components/hardware_map/hardware_map.h
  - components/hardware_map/hardware_map.c
  - components/app_config/app_config.h
  - docs/hardware.md
autonomous: true
requirements:
  - HW-01
  - HW-02
  - HW-04
user_setup: []
must_haves:
  truths:
    - "Phase 6 proceeds without 06-CONTEXT.md by explicit user choice; roadmap and requirements are the source of truth."
    - "HW-01 requires firmware-defined role-based GPIO options for float input, pump Relay 1, pump Relay 2, DS18B20 data, and cooling relay."
    - "HW-02 requires conservative ESP32 DevKit V1 defaults that avoid flash pins, programming UART pins, input-only output mistakes, and boot-strapping hazards."
    - "HW-04 requires DS18B20 powered-mode wiring assumptions, pull-up resistor expectations, voltage rail, and connector labeling to be documented before cooling implementation."
    - "Do not add dual-relay pump runtime behavior, DS18B20 reading logic, cooling relay control, GPIO mutation APIs, or install UI in this plan."
  artifacts:
    - path: "components/hardware_map/hardware_map.h"
      provides: "Public typed hardware roles, safe GPIO options, defaults, and validation API"
      contains: "hardware_role_t"
    - path: "docs/hardware.md"
      provides: "Installer-facing hardware contract, safe defaults, and DS18B20 wiring assumptions"
      contains: "DS18B20"
  key_links:
    - from: "components/hardware_map/hardware_map.h"
      to: "components/app_config/app_config.h"
      via: "default GPIO and cooling protection constants"
      pattern: "APP_TEMPLATE_HW_DEFAULT"
    - from: "docs/hardware.md"
      to: "components/hardware_map/hardware_map.h"
      via: "documents the same role/default contract exposed to firmware"
      pattern: "hardware_role_t"
---

<objective>
Add the firmware-owned hardware contract for v1.1.

Purpose: Give later pump, cooling, API, and UI phases a single typed source for safe GPIO roles, safe option lists, conservative ESP32 DevKit V1 defaults, relay polarity naming, and DS18B20 wiring assumptions.
Output: A new `hardware_map` component compiles under ESP-IDF, `app_config.h` contains role/default constants, and `docs/hardware.md` documents the contract before runtime behavior changes.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-hardware-contract-and-persistent-map-foundation/06-RESEARCH.md
@components/app_config/app_config.h
@components/pump_control/pump_control.h
@components/pump_control/pump_control.c
@components/nvs_store/nvs_store.h
@components/nvs_store/nvs_store.c
@components/pump_control/CMakeLists.txt
@docs/components.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the hardware_map component shell and public types</name>
  <files>components/hardware_map/CMakeLists.txt, components/hardware_map/hardware_map.h, components/hardware_map/hardware_map.c</files>
  <read_first>components/pump_control/CMakeLists.txt, components/pump_control/pump_control.h, components/app_config/app_config.h, .planning/phases/06-hardware-contract-and-persistent-map-foundation/06-RESEARCH.md</read_first>
  <action>Create `components/hardware_map/` with an ESP-IDF `idf_component_register` that requires `app_config` and `esp_driver_gpio`. Add public types for hardware roles (`float_input`, `pump_relay_1`, `pump_relay_2`, `ds18b20_data`, `cooling_relay`), relay polarity (`active_high`, `active_low`), timer start phase (`on`, `off`), cooling mode (`auto`, `force_off`, `test_on`), `hardware_map_t`, `hardware_relay_polarity_map_t`, and `hardware_gpio_option_t`. Keep names prefix-scoped with `hardware_map_` or `hardware_` and avoid dependence on `nvs_store`, `pump_control`, or `web_server`.</action>
  <verify>`rg "hardware_role_t|HARDWARE_ROLE_FLOAT_INPUT|HARDWARE_ROLE_PUMP_RELAY_1|HARDWARE_ROLE_PUMP_RELAY_2|HARDWARE_ROLE_DS18B20_DATA|HARDWARE_ROLE_COOLING_RELAY|hardware_map_t|hardware_relay_polarity" components/hardware_map`</verify>
  <acceptance_criteria>
    - Component compiles as a reusable low-level component.
    - Public API does not include raw NVS keys or web JSON details.
    - Role enum names are stable and match requirements language.
    - Relay polarity can represent at least active-high and active-low independently of pump runtime internals.
  </acceptance_criteria>
  <done>The project has a typed hardware contract component boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Define safe role-specific GPIO options and defaults</name>
  <files>components/hardware_map/hardware_map.h, components/hardware_map/hardware_map.c, components/app_config/app_config.h</files>
  <read_first>components/app_config/app_config.h, .planning/phases/06-hardware-contract-and-persistent-map-foundation/06-RESEARCH.md</read_first>
  <action>Add `APP_TEMPLATE_HW_DEFAULT_*` constants for float GPIO32, pump Relay 1 GPIO26, pump Relay 2 GPIO27, DS18B20 data GPIO33, cooling relay GPIO25, active-low defaults for all three relay outputs, timer start phase defaults of ON, cooling threshold 30.0 C as x10 integer, hysteresis 1.0 C as x10 integer, cooling auto-enable false, test timeout 10 seconds, and compressor minimum off-time 300 seconds. Implement role-specific option lists in `hardware_map.c` that avoid GPIO0, GPIO2, GPIO5, GPIO6-11, GPIO12, GPIO15, GPIO16-17, GPIO34-39 for relay outputs and DS18B20 data by default. Include metadata flags for input capability, output capability, internal pull availability, and whether an option is the default for a role.</action>
  <verify>`rg "APP_TEMPLATE_HW_DEFAULT_FLOAT_GPIO|APP_TEMPLATE_HW_DEFAULT_PUMP_RELAY1_GPIO|APP_TEMPLATE_HW_DEFAULT_PUMP_RELAY2_GPIO|APP_TEMPLATE_HW_DEFAULT_DS18B20_GPIO|APP_TEMPLATE_HW_DEFAULT_COOLING_RELAY_GPIO|APP_TEMPLATE_COOLING_MIN_OFF_SEC|APP_TEMPLATE_COOLING_THRESHOLD_C_X10|APP_TEMPLATE_PUMP_TIMER_START_ON" components/app_config/app_config.h components/hardware_map`</verify>
  <acceptance_criteria>
    - GPIO32 and GPIO26 remain the v1.0-compatible defaults for float input and pump Relay 1.
    - Defaults include GPIO27 for pump Relay 2, GPIO33 for DS18B20 data, and GPIO25 for cooling relay.
    - Role options are not freeform numeric values.
    - Output role options exclude input-only GPIO34-39.
    - Default option metadata can be consumed by future API/UI code without duplicating lists.
  </acceptance_criteria>
  <done>Firmware exposes safe GPIO option lists and conservative defaults.</done>
</task>

<task type="auto">
  <name>Task 3: Add validation helpers for maps and roles</name>
  <files>components/hardware_map/hardware_map.h, components/hardware_map/hardware_map.c</files>
  <read_first>components/hardware_map/hardware_map.h, components/hardware_map/hardware_map.c</read_first>
  <action>Implement helpers such as `hardware_map_defaults()`, `hardware_map_role_name()`, `hardware_map_polarity_name()`, `hardware_map_timer_start_phase_name()`, `hardware_map_gpio_allowed_for_role()`, `hardware_map_validate()`, `hardware_map_equal()`, and `hardware_map_reboot_required()`. Validation must reject duplicate GPIO assignment across active roles, reject GPIOs not present in the role's safe list, reject invalid enum values, and return false for null pointers. Do not initialize actual GPIO hardware in this component.</action>
  <verify>`rg "hardware_map_defaults|hardware_map_validate|hardware_map_gpio_allowed_for_role|hardware_map_equal|hardware_map_reboot_required|hardware_map_role_name" components/hardware_map`</verify>
  <acceptance_criteria>
    - Defaults return the Phase 6 default map consistently.
    - Validation catches duplicate role assignments.
    - Validation catches unsafe role/GPIO combinations.
    - Helper names are stable enough for `nvs_store`, `web_server`, and future UI/API code.
    - Component has no runtime side effects.
  </acceptance_criteria>
  <done>Future phases can validate active and pending hardware maps without reimplementing GPIO safety rules.</done>
</task>

<task type="auto">
  <name>Task 4: Document hardware defaults and DS18B20 wiring</name>
  <files>docs/hardware.md, docs/components.md, README.md</files>
  <read_first>docs/components.md, README.md, .planning/phases/06-hardware-contract-and-persistent-map-foundation/06-RESEARCH.md</read_first>
  <action>Create `docs/hardware.md` documenting the ESP32 DevKit V1 30-pin assumptions, hardware roles, default GPIO map, safe-option rationale, active-low relay default, independent polarity model, DS18B20 powered-mode wiring, 4.7 kOhm pull-up from DQ to 3.3 V, connector labels, and compressor-protection defaults. Update `docs/components.md` to include the new `hardware_map` component and add a short README link to the hardware contract. Keep documentation clear that Phase 6 does not implement cooling runtime yet.</action>
  <verify>`rg "hardware_map|GPIO32|GPIO26|GPIO27|GPIO33|GPIO25|DS18B20|4.7|3.3|minimum off|compressor" docs README.md`</verify>
  <acceptance_criteria>
    - HW-04 wiring assumptions are documented before any DS18B20 runtime work.
    - Documentation warns that relay module polarity varies.
    - Documentation distinguishes firmware defaults from installer wiring decisions.
    - README links to the detailed hardware document without duplicating it.
  </acceptance_criteria>
  <done>Installer-facing hardware assumptions are documented and traceable to the firmware contract.</done>
</task>

</tasks>

<verification>
- [ ] `rg "hardware_role_t|hardware_map_t|hardware_map_defaults|hardware_map_validate" components/hardware_map`
- [ ] `rg "APP_TEMPLATE_HW_DEFAULT_FLOAT_GPIO|APP_TEMPLATE_HW_DEFAULT_PUMP_RELAY1_GPIO|APP_TEMPLATE_HW_DEFAULT_PUMP_RELAY2_GPIO|APP_TEMPLATE_HW_DEFAULT_DS18B20_GPIO|APP_TEMPLATE_HW_DEFAULT_COOLING_RELAY_GPIO" components/app_config/app_config.h components/hardware_map`
- [ ] `rg "GPIO34|GPIO35|GPIO36|GPIO39" components/hardware_map/hardware_map.c` only appears in input-only metadata if intentionally supported for input roles.
- [ ] `rg "DS18B20|4.7|3.3|powered mode|minimum off|compressor" docs/hardware.md`
- [ ] `.\scripts\build.ps1`
</verification>

<success_criteria>
- HW-01 is covered by role-based hardware enums and safe GPIO option APIs.
- HW-02 is covered by conservative ESP32 DevKit V1 defaults and validation rules.
- HW-04 is covered by DS18B20 powered-mode wiring documentation.
- No runtime relay/timer/cooling behavior changes are introduced.
- The new component is ready for NVS persistence in Plan 02 and runtime/API consumption in later phases.
</success_criteria>

<output>
After completion, create `.planning/phases/06-hardware-contract-and-persistent-map-foundation/06-01-SUMMARY.md`.
</output>

