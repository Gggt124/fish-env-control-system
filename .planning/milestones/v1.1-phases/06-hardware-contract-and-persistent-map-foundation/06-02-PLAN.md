---
phase: 06-hardware-contract-and-persistent-map-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - components/nvs_store/CMakeLists.txt
  - components/nvs_store/nvs_store.h
  - components/nvs_store/nvs_store.c
  - docs/hardware.md
autonomous: true
requirements:
  - HW-03
  - HW-05
user_setup: []
must_haves:
  truths:
    - "Plan 02 depends on Plan 01's `hardware_map` component and must use its structs and validation helpers instead of duplicating GPIO safety rules."
    - "HW-03 requires pump Relay 1, pump Relay 2, and cooling relay polarity to be represented independently."
    - "HW-05 requires NVS support for active GPIO map, pending GPIO map, relay polarity, cooling settings, and timer start phases."
    - "Existing v1.0 pump settings keys must remain backward-compatible so current devices do not lose timer durations, auto-start, or single relay polarity."
    - "Do not add authenticated GPIO/cooling mutation routes in this plan; Phase 9 owns those APIs."
  artifacts:
    - path: "components/nvs_store/nvs_store.h"
      provides: "Typed persistence API for hardware map, pump relay polarity/start phases, and cooling settings"
      contains: "nvs_store_load_hardware_map"
    - path: "components/nvs_store/nvs_store.c"
      provides: "Raw NVS key ownership and defaults/validation for v1.1 schemas"
      contains: "NVS_HW_NAMESPACE"
  key_links:
    - from: "components/nvs_store/nvs_store.c"
      to: "components/hardware_map/hardware_map.h"
      via: "validates loaded and pending hardware maps through shared safety helpers"
      pattern: "hardware_map_validate"
    - from: "docs/hardware.md"
      to: "components/nvs_store/nvs_store.c"
      via: "documents the namespaces and keys owned by the storage layer"
      pattern: "hw_cfg"
---

<objective>
Add the persistent hardware and cooling schema foundation.

Purpose: Let later phases read/write active and pending GPIO maps, independent relay polarities, cooling defaults, and timer start phases through typed `nvs_store` wrappers while preserving existing v1.0 pump settings.
Output: `nvs_store` exposes validated load/save/clear APIs for the new v1.1 schemas and documents raw NVS namespaces/keys.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-hardware-contract-and-persistent-map-foundation/06-RESEARCH.md
@.planning/phases/06-hardware-contract-and-persistent-map-foundation/06-01-PLAN.md
@components/hardware_map/hardware_map.h
@components/hardware_map/hardware_map.c
@components/nvs_store/CMakeLists.txt
@components/nvs_store/nvs_store.h
@components/nvs_store/nvs_store.c
@components/app_config/app_config.h
@docs/hardware.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add storage types and dependency on hardware_map</name>
  <files>components/nvs_store/CMakeLists.txt, components/nvs_store/nvs_store.h</files>
  <read_first>components/nvs_store/CMakeLists.txt, components/nvs_store/nvs_store.h, components/hardware_map/hardware_map.h</read_first>
  <action>Add `hardware_map` to `nvs_store` component requirements. Include `hardware_map.h` in `nvs_store.h`. Extend `nvs_store_pump_settings_t` with `relay1_active_low`, `relay2_active_low`, `timer1_start_phase`, and `timer2_start_phase` while preserving compatibility for existing callers. Add a `nvs_store_cooling_settings_t` with threshold x10, hysteresis x10, auto-enable, mode, test timeout seconds, compressor minimum off seconds, and cooling relay polarity. Add load status enums for hardware map and cooling settings following the existing pump status pattern.</action>
  <verify>`rg "hardware_map|relay1_active_low|relay2_active_low|timer1_start_phase|timer2_start_phase|nvs_store_cooling_settings_t|nvs_store_hardware" components/nvs_store`</verify>
  <acceptance_criteria>
    - Public storage structs expose all fields required by HW-03 and HW-05.
    - Existing pump settings functions remain available.
    - New public types use `hardware_map` enums for polarity, timer start phase, and cooling mode where appropriate.
    - Component dependencies remain one-way: `nvs_store` may depend on `hardware_map`, but `hardware_map` does not depend on `nvs_store`.
  </acceptance_criteria>
  <done>`nvs_store` has typed v1.1 storage contracts.</done>
</task>

<task type="auto">
  <name>Task 2: Implement active and pending hardware map persistence</name>
  <files>components/nvs_store/nvs_store.h, components/nvs_store/nvs_store.c</files>
  <read_first>components/nvs_store/nvs_store.c, components/hardware_map/hardware_map.h, components/hardware_map/hardware_map.c</read_first>
  <action>Add namespace `hw_cfg` and keys for active map (`act_float`, `act_r1`, `act_r2`, `act_ds`, `act_cool`) plus pending map (`pend_valid`, `pend_float`, `pend_r1`, `pend_r2`, `pend_ds`, `pend_cool`). Implement defaults, load, save, clear, and pending-load helpers. Missing active map should return safe defaults with a defaults-missing status. Invalid active map should return defaults with an invalid status. Pending map should be optional; load should report no pending when `pend_valid` is missing/false. Save paths must validate maps through `hardware_map_validate()`. Add a helper that reports reboot-required when a valid pending map exists and differs from active.</action>
  <verify>`rg "NVS_HW_NAMESPACE|act_float|act_r1|act_r2|act_ds|act_cool|pend_valid|pend_float|nvs_store_load_hardware_map|nvs_store_save_hardware_map|nvs_store_load_pending_hardware_map|nvs_store_hardware_reboot_required" components/nvs_store`</verify>
  <acceptance_criteria>
    - Active map defaults preserve GPIO32/GPIO26/GPIO27/GPIO33/GPIO25.
    - Pending map can be stored without becoming active immediately.
    - Reboot-required status can be computed from active vs pending.
    - Invalid persisted maps fail closed to defaults and do not leak unsafe GPIO assignments into runtime config.
    - Raw NVS key names stay file-local to `nvs_store.c`.
  </acceptance_criteria>
  <done>Hardware map persistence supports active and pending values.</done>
</task>

<task type="auto">
  <name>Task 3: Extend pump settings with independent polarity and start phases</name>
  <files>components/nvs_store/nvs_store.h, components/nvs_store/nvs_store.c</files>
  <read_first>components/nvs_store/nvs_store.c, components/app_config/app_config.h, components/hardware_map/hardware_map.h</read_first>
  <action>Extend pump settings defaults/load/save/clear to support independent Relay 1 and Relay 2 polarity plus Timer 1 and Timer 2 start phases. Keep existing `t1_on`, `t1_off`, `t2_on`, `t2_off`, `relay_low`, and `auto_start` keys readable. For backward compatibility, if new `r1_low` is missing, seed Relay 1 from legacy `relay_low`; if `r2_low` is missing, use the configured default. If start-phase keys are missing, default both timers to ON. Save should write the new keys and may continue writing `relay_low` as a Relay 1 compatibility alias until the old API is replaced.</action>
  <verify>`rg "r1_low|r2_low|t1_start|t2_start|relay_low|timer1_start_phase|timer2_start_phase|relay1_active_low|relay2_active_low" components/nvs_store/nvs_store.c components/nvs_store/nvs_store.h`</verify>
  <acceptance_criteria>
    - Existing v1.0 pump settings can still load.
    - Relay 1 and Relay 2 polarity are represented independently.
    - Timer 1 and Timer 2 start phases are persisted and validate as ON/OFF only.
    - Missing new fields default safely without erasing existing timer durations.
    - Save failures return false and do not report success before commit.
  </acceptance_criteria>
  <done>Pump settings schema is ready for dual-channel runtime in Phase 7.</done>
</task>

<task type="auto">
  <name>Task 4: Add cooling settings persistence defaults</name>
  <files>components/nvs_store/nvs_store.h, components/nvs_store/nvs_store.c</files>
  <read_first>components/nvs_store/nvs_store.c, components/app_config/app_config.h, components/hardware_map/hardware_map.h, .planning/REQUIREMENTS.md</read_first>
  <action>Add defaults, validation, load, save, and clear helpers for cooling settings in namespace `cool_cfg`. Store threshold and hysteresis as x10 integers, mode as an enum value, auto-enable as u8 bool, test timeout seconds, compressor minimum off seconds, and cooling relay polarity. Defaults must be threshold 30.0 C, hysteresis 1.0 C, auto-enable false, mode force-off, test timeout 10 seconds, minimum off 300 seconds, and cooling relay active-low. Do not create a cooling runtime or touch GPIO levels in this task.</action>
  <verify>`rg "NVS_COOL_NAMESPACE|threshold_x10|hyst_x10|auto_en|test_tmo_s|min_off_s|cooling_settings|nvs_store_load_cooling_settings|nvs_store_save_cooling_settings" components/nvs_store`</verify>
  <acceptance_criteria>
    - Cooling settings default to safe disabled/off behavior.
    - Cooling relay polarity is independent of both pump relays.
    - Validation rejects nonsensical threshold/hysteresis/timeout/min-off values.
    - Storage is ready for Phase 8 runtime and Phase 9 APIs.
  </acceptance_criteria>
  <done>Cooling persistence foundation exists without activating cooling hardware.</done>
</task>

<task type="auto">
  <name>Task 5: Document the NVS schema</name>
  <files>docs/hardware.md, docs/components.md</files>
  <read_first>docs/hardware.md, docs/components.md, components/nvs_store/nvs_store.h, components/nvs_store/nvs_store.c</read_first>
  <action>Update hardware/component documentation with the new namespaces, key groups, defaults, migration behavior from legacy `relay_low`, and the active-vs-pending hardware map semantics. State clearly that pending GPIO values require reboot before becoming active and that Phase 9 will expose authenticated mutation APIs.</action>
  <verify>`rg "hw_cfg|pump_cfg|cool_cfg|pending|reboot|required|relay_low|r1_low|r2_low|t1_start|t2_start" docs/hardware.md docs/components.md`</verify>
  <acceptance_criteria>
    - HW-05 schema is documented with namespace and key-level intent.
    - Backward compatibility behavior is documented.
    - Active/pending map semantics are understandable without reading C code.
  </acceptance_criteria>
  <done>NVS schema is documented for implementers and future phase agents.</done>
</task>

</tasks>

<verification>
- [ ] `rg "hardware_map" components/nvs_store/CMakeLists.txt components/nvs_store/nvs_store.h`
- [ ] `rg "nvs_store_load_hardware_map|nvs_store_save_hardware_map|nvs_store_load_pending_hardware_map|nvs_store_hardware_reboot_required" components/nvs_store`
- [ ] `rg "relay1_active_low|relay2_active_low|timer1_start_phase|timer2_start_phase" components/nvs_store`
- [ ] `rg "nvs_store_cooling_settings_t|nvs_store_load_cooling_settings|nvs_store_save_cooling_settings" components/nvs_store`
- [ ] `rg "hw_cfg|pump_cfg|cool_cfg|pending|reboot" docs/hardware.md docs/components.md`
- [ ] `.\scripts\build.ps1`
</verification>

<success_criteria>
- HW-03 is covered by independent Relay 1, Relay 2, and cooling relay polarity storage.
- HW-05 is covered by active/pending hardware map, pump start-phase, relay polarity, and cooling settings storage.
- Existing saved v1.0 pump settings continue to load safely.
- No web/API mutation surface is introduced ahead of Phase 9.
- Invalid persisted data fails closed to defaults or a documented invalid status.
</success_criteria>

<output>
After completion, create `.planning/phases/06-hardware-contract-and-persistent-map-foundation/06-02-SUMMARY.md`.
</output>

