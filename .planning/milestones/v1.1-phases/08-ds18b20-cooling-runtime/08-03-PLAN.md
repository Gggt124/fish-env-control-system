---
phase: 08-ds18b20-cooling-runtime
plan: 03
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-02
files_modified:
  - main/CMakeLists.txt
  - main/app_main.c
  - main/web_server.c
  - components/cooling_control/CMakeLists.txt
  - docs/components.md
  - docs/development-notes.md
  - docs/hardware.md
  - REFERENCE.md
  - dependencies.lock
  - build/
autonomous: true
requirements:
  - COOL-01
  - COOL-02
  - COOL-03
  - COOL-04
  - COOL-05
  - COOL-06
  - COOL-07
user_setup:
  - "Manual Phase 8 validation needs powered-mode DS18B20 wiring on active/default GPIO33 and cooling relay wiring on active/default GPIO25 before flash/device testing."
must_haves:
  truths:
    - "Plan 03 depends on Plan 01 and Plan 02 `cooling_control` runtime/status APIs."
    - "D-15: Add separate authenticated `GET /api/cooling/status`; keep full config/mutation APIs for Phase 9."
    - "D-16: Status exposes mode, auto_enable, temperature_c, temperature_valid, sensor_state, fault, fault_code, relay_energized, threshold_c, hysteresis_c, lockout_active, lockout_remaining_sec, and test_remaining_sec."
    - "D-17: Status exposes `cooling_demand` and `blocked_reason` so demand is separate from actual relay output."
    - "D-18: Use stable string enums for `sensor_state` and `fault_code`."
    - "D-19: Use stable `blocked_reason` enum values such as none, compressor_lockout, sensor_fault, force_off, and config_invalid."
    - "D-20: Cooling runtime details belong in `/api/cooling/status`, not `/api/pump/status`; `/api/status` may only get an optional summary if needed."
    - "Full cooling config POST routes, hardware mutation APIs, owner dashboard redesign, and Hardware/Install UI remain out of scope."
  artifacts:
    - path: "main/app_main.c"
      provides: "Boot-time cooling runtime initialization from active map and persisted cooling settings"
      contains: "cooling_control_init"
    - path: "main/web_server.c"
      provides: "Authenticated GET /api/cooling/status serialization"
      contains: "/api/cooling/status"
    - path: "docs/development-notes.md"
      provides: "Phase 8 handoff notes for later API/UI and hardware validation"
      contains: "Phase 8"
  key_links:
    - from: "main/web_server.c"
      to: "components/cooling_control/cooling_control.h"
      via: "serializes stable cooling status fields from runtime snapshot"
      pattern: "blocked_reason"
    - from: "main/app_main.c"
      to: "components/nvs_store/nvs_store.h"
      via: "loads active hardware map and cooling settings for runtime config"
      pattern: "nvs_store_load_cooling_settings"
---

<objective>
Integrate the cooling runtime into boot and expose the Phase 8 authenticated status API.

Purpose: Make the DS18B20 cooling runtime actually start with firmware, remain safe when settings or hardware are invalid, and provide a machine-friendly status route for validation and later UI/API phases without introducing mutation routes early.
Output: Boot initializes `cooling_control` from active hardware and persisted cooling settings, `GET /api/cooling/status` returns stable runtime state, documentation records the handoff, and ESP-IDF build validation passes.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-ds18b20-cooling-runtime/08-CONTEXT.md
@.planning/phases/08-ds18b20-cooling-runtime/08-RESEARCH.md
@.planning/phases/08-ds18b20-cooling-runtime/08-01-PLAN.md
@.planning/phases/08-ds18b20-cooling-runtime/08-02-PLAN.md
@.planning/phases/06-hardware-contract-and-persistent-map-foundation/06-03-SUMMARY.md
@components/cooling_control/cooling_control.h
@components/cooling_control/cooling_control.c
@components/nvs_store/nvs_store.h
@components/hardware_map/hardware_map.h
@main/app_main.c
@main/web_server.c
@main/CMakeLists.txt
@docs/hardware.md
@docs/components.md
@docs/development-notes.md
@REFERENCE.md
@scripts/build.ps1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register cooling_control in build graph and boot initialization</name>
  <files>main/CMakeLists.txt, main/app_main.c, components/cooling_control/CMakeLists.txt</files>
  <read_first>main/CMakeLists.txt, main/app_main.c, components/cooling_control/cooling_control.h, components/nvs_store/nvs_store.h, components/hardware_map/hardware_map.h</read_first>
  <action>Add `cooling_control` to the main component dependencies. In `app_main.c`, include `cooling_control.h`, map `hardware_map_t.ds18b20_data_gpio`, `hardware_map_t.cooling_relay_gpio`, `nvs_store_cooling_settings_t`, and cooling relay polarity into `cooling_control_config_t`, and initialize cooling runtime after NVS loads and before service-ready logs. If active hardware map or cooling settings are invalid/error, initialize cooling runtime in a safe invalid/config-fault state or skip relay-capable init so cooling relay remains OFF and status explains the invalid config. Update boot logs to report cooling runtime status instead of saying runtime inactive in Phase 7.</action>
  <verify>`rg "cooling_control|ds18b20|cooling_relay|nvs_store_load_cooling_settings|auto_enable|compressor" main/CMakeLists.txt main/app_main.c components/cooling_control/CMakeLists.txt`</verify>
  <acceptance_criteria>
    - Firmware links `cooling_control`.
    - Boot uses active hardware map DS18B20 and cooling relay GPIO values.
    - Boot uses persisted/default cooling settings and auto-enable semantics.
    - Invalid hardware/settings cannot energize cooling relay.
    - Existing pump/Wi-Fi/session/HTTP startup continues if cooling init fails.
  </acceptance_criteria>
  <done>Cooling runtime starts safely during firmware boot.</done>
</task>

<task type="auto">
  <name>Task 2: Add authenticated GET /api/cooling/status</name>
  <files>main/web_server.c</files>
  <read_first>main/web_server.c, components/cooling_control/cooling_control.h, .planning/phases/08-ds18b20-cooling-runtime/08-CONTEXT.md</read_first>
  <action>Add helper functions to convert cooling mode, sensor state, fault code, and blocked reason enums to stable strings. Register authenticated `GET /api/cooling/status`. The handler must require auth, allocate JSON through cJSON, call `cooling_control_get_status()`, and return `ok:true` plus the locked fields: `mode`, `auto_enable`, `temperature_c`, `temperature_valid`, `sensor_state`, `fault`, `fault_code`, `relay_energized`, `threshold_c`, `hysteresis_c`, `lockout_active`, `lockout_remaining_sec`, `test_remaining_sec`, `cooling_demand`, and `blocked_reason`. If status is unavailable, return a JSON error consistent with existing API style. Do not add POST/mutation routes.</action>
  <verify>`rg "/api/cooling/status|handle_api_cooling_status|cooling_control_get_status|sensor_state|fault_code|blocked_reason|cooling_demand" main/web_server.c`</verify>
  <acceptance_criteria>
    - `GET /api/cooling/status` is protected by existing session auth.
    - Status field names match CONTEXT.md D-16 through D-19.
    - Stable string values are used for mode/sensor/fault/block enums.
    - No cooling config mutation route is added.
    - `/api/pump/status` remains focused on pump runtime.
  </acceptance_criteria>
  <done>Cooling runtime status is available through an authenticated local API.</done>
</task>

<task type="auto">
  <name>Task 3: Document Phase 8 runtime handoff and provenance</name>
  <files>docs/components.md, docs/development-notes.md, docs/hardware.md, REFERENCE.md</files>
  <read_first>docs/components.md, docs/development-notes.md, docs/hardware.md, REFERENCE.md, .planning/phases/08-ds18b20-cooling-runtime/08-CONTEXT.md, .planning/phases/08-ds18b20-cooling-runtime/08-RESEARCH.md</read_first>
  <action>Update documentation to reflect that Phase 8 now owns DS18B20 cooling runtime behavior. Document the `cooling_control` component boundary, status API route, safe fault behavior, mode semantics, compressor lockout behavior, and remaining Phase 9/10 handoff. Update `REFERENCE.md` if the Espressif DS18B20/1-Wire registry/GitHub references are not already recorded. Keep installer wiring assumptions in `docs/hardware.md` consistent with Phase 6 powered-mode DS18B20 guidance.</action>
  <verify>`rg "cooling_control|/api/cooling/status|DS18B20|sensor fault|compressor|onewire_bus|espressif/ds18b20|Phase 8" docs REFERENCE.md`</verify>
  <acceptance_criteria>
    - Component docs list `cooling_control` and its dependencies.
    - Development notes capture Phase 8 handoff constraints for Phase 9/10.
    - Hardware docs no longer imply cooling runtime is entirely future work after Phase 8.
    - External component provenance is recorded in `REFERENCE.md`.
    - Docs do not claim UI/config mutation work that remains out of scope.
  </acceptance_criteria>
  <done>Phase 8 documentation and provenance are current.</done>
</task>

<task type="auto">
  <name>Task 4: Run source checks and ESP-IDF build validation</name>
  <files>build/, dependencies.lock, .planning/phases/08-ds18b20-cooling-runtime/08-03-SUMMARY.md</files>
  <read_first>AGENTS.md, scripts/build.ps1, .planning/phases/08-ds18b20-cooling-runtime/08-CONTEXT.md</read_first>
  <action>Run focused source checks for cooling component, boot integration, status API, docs, and no-scope-creep routes. Run `.\scripts\build.ps1` using the documented Windows ESP-IDF workflow. If dependency download is blocked by network sandbox, retry with the required escalation rather than bypassing managed dependencies. Confirm `build\fish_pump_relay_timer_control.bin` exists after build. Record exact build result, dependency lockfile changes, and any hardware-validation-required notes in the summary.</action>
  <verify>`rg "cooling_control|/api/cooling/status|compressor|min_off|sensor_state|blocked_reason" components main docs`; then `rg "/api/cooling/config|Hardware/Install|WebSocket|MQTT|OTA" main components docs`; then `.\scripts\build.ps1`; then `Test-Path build\fish_pump_relay_timer_control.bin`</verify>
  <acceptance_criteria>
    - ESP-IDF build passes and binary exists, or exact environment blocker is recorded.
    - Source checks confirm all COOL-01 through COOL-07 contracts are represented.
    - No forbidden Phase 8 scope creep route/UI/cloud feature is added.
    - Summary captures manual hardware validation steps still needed.
    - Working tree changes are ready for normal GSD execution commits.
  </acceptance_criteria>
  <done>Phase 8 boot/API integration is build-validated.</done>
</task>

</tasks>

<verification>
- [ ] `rg "cooling_control|ds18b20|cooling_relay|auto_enable|compressor" main/CMakeLists.txt main/app_main.c components/cooling_control`
- [ ] `rg "/api/cooling/status|sensor_state|fault_code|blocked_reason|cooling_demand|test_remaining_sec" main/web_server.c`
- [ ] `rg "/api/cooling/config|POST.*cooling|Hardware/Install|WebSocket|MQTT|OTA" main components docs` and inspect that no out-of-scope feature was introduced.
- [ ] `rg "cooling_control|/api/cooling/status|DS18B20|compressor|Phase 8|espressif/ds18b20" docs REFERENCE.md`
- [ ] `.\scripts\build.ps1`
- [ ] `Test-Path build\fish_pump_relay_timer_control.bin`
</verification>

<success_criteria>
- COOL-01 through COOL-07 are wired into boot/API-visible firmware behavior.
- Cooling runtime uses active hardware map and persisted cooling settings safely.
- `/api/cooling/status` exposes all locked runtime status fields.
- Pump runtime/API and Wi-Fi/login/status routes remain in their existing scope.
- ESP-IDF build validates the full Phase 8 integration.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ds18b20-cooling-runtime/08-03-SUMMARY.md`.
</output>
