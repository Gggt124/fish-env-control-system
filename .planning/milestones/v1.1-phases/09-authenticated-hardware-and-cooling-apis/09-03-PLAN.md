---
phase: 09-authenticated-hardware-and-cooling-apis
plan: 03
type: execute
wave: 3
depends_on:
  - 09-01
  - 09-02
files_modified:
  - main/web_server.c
  - docs/components.md
  - docs/development-notes.md
  - README.md
  - build/
autonomous: true
requirements:
  - UI-07
user_setup: []
must_haves:
  truths:
    - "Plan 03 depends on hardware map APIs from Plan 01 and cooling APIs from Plan 02."
    - "Pump config POST must support Relay 1 and Relay 2 independently while preserving legacy `relay_polarity` as a Relay 1 compatibility alias."
    - "Pump config POST must continue to reject hardware GPIO fields; GPIO changes belong only to `/api/hardware/map` pending-save flow."
    - "Timer 1 and Timer 2 start phases must remain independent enum values."
    - "All new mutation routes from Phase 9 must be authenticated and same-origin protected."
    - "The final validation gate is `./scripts/build.ps1` and generated `build/fish_pump_relay_timer_control.bin`."
  artifacts:
    - path: "main/web_server.c"
      provides: "Completed authenticated pump, hardware, and cooling API surface for Phase 10 UI"
      contains: "relay2_polarity"
    - path: "docs/development-notes.md"
      provides: "Phase 9 API handoff notes for Phase 10 UI and manual testing"
      contains: "Phase 9"
  key_links:
    - from: "main/web_server.c"
      to: "components/pump_control/pump_control.h"
      via: "reapplies validated dual-channel pump settings to runtime"
      pattern: "relay2_polarity"
    - from: "docs/development-notes.md"
      to: ".planning/phases/09-authenticated-hardware-and-cooling-apis/09-RESEARCH.md"
      via: "records API scope and Phase 10 handoff constraints"
      pattern: "Phase 9"
---

<objective>
Complete the dual-channel pump config API, document the Phase 9 API surface, and build-validate the authenticated hardware/cooling/pump API set.

Purpose: Ensure Phase 10 can consume one coherent local API surface for owner dashboard and Hardware/Install UI work, with pump relay polarity/start phases, cooling config/mode, hardware active/pending maps, auth, and same-origin protection all verified.
Output: `/api/pump/config` supports both relay channels and independent start phases, Phase 9 API behavior is documented, and ESP-IDF build validation passes.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-authenticated-hardware-and-cooling-apis/09-RESEARCH.md
@.planning/phases/09-authenticated-hardware-and-cooling-apis/09-01-PLAN.md
@.planning/phases/09-authenticated-hardware-and-cooling-apis/09-02-PLAN.md
@.planning/phases/07-dual-timer-relay-pump-runtime/07-01-SUMMARY.md
@.planning/phases/07-dual-timer-relay-pump-runtime/07-02-SUMMARY.md
@.planning/phases/07-dual-timer-relay-pump-runtime/07-03-SUMMARY.md
@components/pump_control/pump_control.h
@components/nvs_store/nvs_store.h
@components/hardware_map/hardware_map.h
@main/web_server.c
@docs/components.md
@docs/development-notes.md
@README.md
@scripts/build.ps1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update pump config GET/POST for independent relay channel settings</name>
  <files>main/web_server.c</files>
  <read_first>main/web_server.c, components/nvs_store/nvs_store.h, components/pump_control/pump_control.h, components/hardware_map/hardware_map.h</read_first>
  <action>Update `/api/pump/config` serialization and parsing so Relay 1 and Relay 2 polarity are editable independently through `relay1_polarity` and `relay2_polarity`. Preserve legacy `relay_polarity` as a compatibility alias for Relay 1 when explicit `relay1_polarity` is absent. Continue returning `relay_polarity` as Relay 1 compatibility. Keep `timer1_start_phase` and `timer2_start_phase` as independent `on`/`off` enum fields and reject invalid enum values. Remove `relay1_polarity` and `relay2_polarity` from the read-only rejection list, but keep all hardware GPIO fields read-only. After saving, rebuild `pump_control_config_t` from active hardware map and saved settings and restart only if the pump was running before, matching current behavior.</action>
  <verify>`rg "relay1_polarity|relay2_polarity|relay_polarity|timer1_start_phase|timer2_start_phase|readonly_fields|api_pump_parse_settings_payload" main/web_server.c`</verify>
  <acceptance_criteria>
    - Relay 1 and Relay 2 polarity can be changed independently through authenticated API.
    - Legacy `relay_polarity` payloads still work for Relay 1.
    - Timer 1 and Timer 2 start phases remain independently editable.
    - Hardware GPIO fields are still rejected in pump config POST.
    - Runtime apply preserves existing start/stop behavior and no-overlap relay safety.
  </acceptance_criteria>
  <done>`/api/pump/config` fully supports dual-channel pump settings.</done>
</task>

<task type="auto">
  <name>Task 2: Audit auth, same-origin protection, and route registration</name>
  <files>main/web_server.c, components/app_config/app_config.h</files>
  <read_first>main/web_server.c, components/app_config/app_config.h, .planning/phases/09-authenticated-hardware-and-cooling-apis/09-01-PLAN.md, .planning/phases/09-authenticated-hardware-and-cooling-apis/09-02-PLAN.md</read_first>
  <action>Audit the final Phase 9 route table. Confirm each new GET route requires `require_auth(req)`. Confirm each new POST route requires both `require_auth(req)` and `is_same_origin(req, false)`. Confirm `/api/login` and `/api/logout` keep their existing missing-origin behavior. Confirm `APP_TEMPLATE_HTTP_MAX_URI_HANDLERS` is greater than the final route count. If route registration logs errors, fix capacity or duplicate route definitions before build validation.</action>
  <verify>`rg "/api/hardware/map|/api/cooling/config|/api/cooling/mode|/api/pump/config|require_auth|is_same_origin\\(req, false\\)|APP_TEMPLATE_HTTP_MAX_URI_HANDLERS" main/web_server.c components/app_config/app_config.h`</verify>
  <acceptance_criteria>
    - New hardware, cooling, and pump mutation routes are same-origin protected.
    - New GET APIs are authenticated.
    - Existing login/logout behavior is not accidentally tightened or loosened.
    - HTTP max handler count exceeds final route count.
    - Route registration contains no duplicate unintended handlers.
  </acceptance_criteria>
  <done>Phase 9 route security and capacity are audited.</done>
</task>

<task type="auto">
  <name>Task 3: Document Phase 9 API surface and Phase 10 handoff</name>
  <files>docs/components.md, docs/development-notes.md, README.md</files>
  <read_first>docs/components.md, docs/development-notes.md, README.md, .planning/phases/09-authenticated-hardware-and-cooling-apis/09-RESEARCH.md</read_first>
  <action>Update concise project docs to reflect the new authenticated API surface. Document `/api/hardware/map`, `/api/cooling/config`, `/api/cooling/mode`, existing `/api/cooling/status`, and `/api/pump/config` dual-channel fields. Record that GPIO map changes save as pending and require reboot, that Test ON is runtime-only, and that Phase 10 owns frontend UI. Keep docs operator-facing and avoid claiming dashboard/Hardware page UI work is complete.</action>
  <verify>`rg "/api/hardware/map|/api/cooling/config|/api/cooling/mode|/api/cooling/status|relay1_polarity|relay2_polarity|pending|reboot|Phase 9" docs README.md`</verify>
  <acceptance_criteria>
    - Docs describe active vs pending hardware map behavior.
    - Docs describe cooling config/mode/status API behavior.
    - Docs describe dual-channel pump config fields.
    - Docs clearly defer owner dashboard and Hardware/Install UI to Phase 10.
    - No external repo provenance is added unless execution uses external references.
  </acceptance_criteria>
  <done>Phase 9 API docs and handoff notes are current.</done>
</task>

<task type="auto">
  <name>Task 4: Run final source checks and ESP-IDF build validation</name>
  <files>build/, .planning/phases/09-authenticated-hardware-and-cooling-apis/09-03-SUMMARY.md</files>
  <read_first>AGENTS.md, scripts/build.ps1, .planning/phases/09-authenticated-hardware-and-cooling-apis/09-RESEARCH.md</read_first>
  <action>Run focused `rg` checks for hardware map APIs, cooling config/mode APIs, pump dual-channel fields, auth/same-origin protections, route capacity, and out-of-scope frontend/cloud features. Then run `.\scripts\build.ps1` using the documented Windows ESP-IDF workflow. Confirm `build\fish_pump_relay_timer_control.bin` exists. Record exact build result, binary path, and any manual hardware/API validation notes in the summary.</action>
  <verify>`rg "/api/hardware/map|/api/cooling/config|/api/cooling/mode|/api/pump/config|relay2_polarity|confirm_reboot_required" main/web_server.c`; then `rg "Hardware/Install|dashboard redesign|WebSocket|MQTT|OTA|cloud" main components docs`; then `.\scripts\build.ps1`; then `Test-Path build\fish_pump_relay_timer_control.bin`</verify>
  <acceptance_criteria>
    - ESP-IDF build passes and binary exists, or exact environment blocker is recorded.
    - UI-03 through UI-07 are represented in API code.
    - Auth and same-origin checks are present on all new mutation routes.
    - No Phase 10 frontend work or out-of-scope cloud/OTA feature is introduced.
    - Summary captures manual API validation suggestions for browser/curl testing after flash.
  </acceptance_criteria>
  <done>Phase 9 API implementation is build-validated.</done>
</task>

</tasks>

<verification>
- [ ] `rg "/api/hardware/map|confirm_reboot_required|pending_valid|reboot_required|hardware_map_options_for_role" main/web_server.c`
- [ ] `rg "/api/cooling/config|/api/cooling/mode|nvs_store_save_cooling_settings|cooling_control_set_mode|cooling_control_start_test" main/web_server.c`
- [ ] `rg "relay1_polarity|relay2_polarity|timer1_start_phase|timer2_start_phase|readonly_fields" main/web_server.c`
- [ ] `rg "require_auth|is_same_origin\\(req, false\\)|APP_TEMPLATE_HTTP_MAX_URI_HANDLERS" main/web_server.c components/app_config/app_config.h`
- [ ] `rg "Hardware/Install|dashboard redesign|WebSocket|MQTT|OTA|cloud" main components docs` and inspect that no out-of-scope feature was added.
- [ ] `.\scripts\build.ps1`
- [ ] `Test-Path build\fish_pump_relay_timer_control.bin`
</verification>

<success_criteria>
- `/api/pump/config` supports both relay channels and independent timer start phases.
- Hardware map, cooling config/mode/status, and pump config APIs form a coherent authenticated API surface.
- Same-origin POST protection and existing session auth remain intact for all mutation routes.
- Documentation records Phase 9 behavior and Phase 10 UI handoff.
- ESP-IDF build validates the final API integration.
</success_criteria>

<output>
After completion, create `.planning/phases/09-authenticated-hardware-and-cooling-apis/09-03-SUMMARY.md`.
</output>
