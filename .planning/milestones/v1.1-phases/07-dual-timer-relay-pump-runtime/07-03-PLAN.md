---
phase: 07-dual-timer-relay-pump-runtime
plan: 03
type: execute
wave: 3
depends_on:
  - 07-01
  - 07-02
files_modified:
  - main/static/dashboard.html
  - main/static/app.js
  - main/static/style.css
  - docs/development-notes.md
autonomous: true
requirements:
  - PUMP-01
  - PUMP-02
  - PUMP-03
  - PUMP-04
  - PUMP-05
  - PUMP-06
user_setup:
  - "Manual hardware validation after flashing should connect Relay 1 to the existing validated relay channel and Relay 2 to the second pump relay output before confirming real pump behavior."
must_haves:
  truths:
    - "D-15: Phase 7 makes only minimum static dashboard/UI updates needed so mapping and status are correct; do not redesign the full dashboard."
    - "D-16: UI wording must state Float ON -> Timer 1 / Relay 1 and Float OFF -> Timer 2 / Relay 2."
    - "D-17: Dashboard shows Relay 1 and Relay 2 state separately so manual hardware testing can confirm no overlap."
    - "D-18: Stopped-state preview wording must clearly mean ready-to-start, not currently running."
    - "Dashboard/status should not stay unknown before Start when float can be read."
    - "UI must remain no-CDN static HTML/CSS/vanilla JS and preserve existing Wi-Fi/login/status access."
  artifacts:
    - path: "main/static/dashboard.html"
      provides: "Minimal dual-relay dashboard markup and Timer start phase controls"
      contains: "Relay 1"
    - path: "main/static/app.js"
      provides: "Dual-relay status rendering, corrected float mapping, start-phase config payload"
      contains: "relay1_energized"
    - path: "docs/development-notes.md"
      provides: "Phase 7 handoff and manual hardware validation notes"
      contains: "Phase 7"
  key_links:
    - from: "main/static/app.js"
      to: "main/web_server.c"
      via: "consumes `/api/pump/config` start phases and `/api/pump/status` dual-relay fields"
      pattern: "timer1_start_phase"
    - from: "main/static/dashboard.html"
      to: ".planning/phases/07-dual-timer-relay-pump-runtime/07-CONTEXT.md"
      via: "implements D-15 through D-18 UI clarity decisions"
      pattern: "Relay 1"
---

<objective>
Make the local dashboard and validation notes accurate for the dual-relay runtime.

Purpose: Give the owner a minimally updated, no-internet UI that shows the new Float ON/OFF mapping, separate Relay 1/Relay 2 states, ready-to-start preview, and Timer 1/Timer 2 start-phase controls needed to satisfy Phase 7 without redesigning the dashboard.
Output: Static dashboard assets consume the new API fields, preserve existing polling/countdown behavior, expose start-phase configuration, and ESP-IDF build validation passes.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-dual-timer-relay-pump-runtime/07-CONTEXT.md
@.planning/phases/07-dual-timer-relay-pump-runtime/07-RESEARCH.md
@.planning/phases/07-dual-timer-relay-pump-runtime/07-01-PLAN.md
@.planning/phases/07-dual-timer-relay-pump-runtime/07-02-PLAN.md
@.planning/phases/04-web-pump-control-ui/04-CONTEXT.md
@main/static/dashboard.html
@main/static/app.js
@main/static/style.css
@main/web_server.c
@scripts/build.ps1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add minimal dashboard controls for timer start phases</name>
  <files>main/static/dashboard.html, main/static/app.js, main/static/style.css</files>
  <read_first>main/static/dashboard.html, main/static/app.js, main/static/style.css, .planning/phases/07-dual-timer-relay-pump-runtime/07-CONTEXT.md</read_first>
  <action>Add simple ON/OFF controls for Timer 1 start phase and Timer 2 start phase within the existing pump settings form. Load values from `GET /api/pump/config` fields `timer1_start_phase` and `timer2_start_phase`, include them in the full replacement payload for `POST /api/pump/config`, validate that only `on` or `off` is submitted, and preserve existing timer duration, auto-start, Relay 1 polarity, unsaved-change, and save-feedback behavior. Keep the visual treatment consistent with existing form controls; do not create a new page.</action>
  <verify>`rg "timer1_start_phase|timer2_start_phase|start phase|ช่วงเริ่ม|on|off" main/static/dashboard.html main/static/app.js main/static/style.css`</verify>
  <acceptance_criteria>
    - PUMP-04 is configurable from the existing authenticated dashboard.
    - Save payload includes both start-phase fields.
    - Existing timer duration and auto-start controls still work.
    - UI does not submit GPIO/hardware/cooling mutation fields.
    - No external frontend dependencies are added.
  </acceptance_criteria>
  <done>Dashboard can configure Timer 1 and Timer 2 start phases.</done>
</task>

<task type="auto">
  <name>Task 2: Correct float mapping and ready-state wording</name>
  <files>main/static/dashboard.html, main/static/app.js</files>
  <read_first>main/static/dashboard.html, main/static/app.js, .planning/phases/04-web-pump-control-ui/04-CONTEXT.md, .planning/phases/07-dual-timer-relay-pump-runtime/07-CONTEXT.md</read_first>
  <action>Update runtime labels/render helpers so Float ON is shown as Timer 1 / Relay 1 and Float OFF is shown as Timer 2 / Relay 2. Remove or replace any old v1.0 wording that says Float ON uses Timer 2 or Float OFF uses Timer 1. When `running:false` but status includes a ready `active_timer`, `active_relay`, phase, and countdown, render wording equivalent to ready-to-start rather than currently running. Keep Thai wording concise and consistent with existing UI style.</action>
  <verify>`rg "Float ON|Float OFF|Timer 1|Timer 2|Relay 1|Relay 2|พร้อมเริ่ม|กำลังทำงาน|unknown" main/static/dashboard.html main/static/app.js`</verify>
  <acceptance_criteria>
    - UI directly communicates Float ON -> Timer 1 / Relay 1.
    - UI directly communicates Float OFF -> Timer 2 / Relay 2.
    - Stopped preview is visibly not a running pump state.
    - Float state no longer appears unknown just because the controller is stopped when API can report it.
    - Existing degraded/stale status handling remains intact.
  </acceptance_criteria>
  <done>Dashboard mapping and stopped preview wording match Phase 7 behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Render Relay 1 and Relay 2 states separately</name>
  <files>main/static/dashboard.html, main/static/app.js, main/static/style.css</files>
  <read_first>main/static/dashboard.html, main/static/app.js, main/static/style.css, main/web_server.c</read_first>
  <action>Add separate Relay 1 and Relay 2 state displays to the existing runtime panel or compact runtime grid. Consume `relay1_energized`, `relay2_energized`, and `active_relay` from `/api/pump/status`. Preserve `relay_energized` fallback only for compatibility if the new fields are absent. Show OFF for both relays when stopped. Make the active/ready relay visually understandable without adding a full dashboard redesign.</action>
  <verify>`rg "relay1_energized|relay2_energized|active_relay|pump-relay-1|pump-relay-2|Relay 1|Relay 2" main/static/dashboard.html main/static/app.js main/static/style.css`</verify>
  <acceptance_criteria>
    - Dashboard shows Relay 1 and Relay 2 independently.
    - Manual hardware testers can see whether both relays are ever ON together.
    - Compatibility fallback does not mask new fields when they exist.
    - Layout remains stable on desktop and mobile.
  </acceptance_criteria>
  <done>Dashboard exposes separate pump relay states for hardware validation.</done>
</task>

<task type="auto">
  <name>Task 4: Document Phase 7 handoff and run build validation</name>
  <files>docs/development-notes.md, .planning/phases/07-dual-timer-relay-pump-runtime/07-03-SUMMARY.md, build/</files>
  <read_first>docs/development-notes.md, AGENTS.md, scripts/build.ps1, .planning/phases/07-dual-timer-relay-pump-runtime/07-CONTEXT.md</read_first>
  <action>Add concise development notes for Phase 7 behavior: v1.1 float mapping, dual-relay status fields, stopped-state preview semantics, start-phase save behavior, and manual hardware validation points. Run `.\scripts\build.ps1` using the documented ESP-IDF workflow. If ESP-IDF is unavailable, record the exact blocker in the summary and do not substitute another build system. Confirm `build\fish_pump_relay_timer_control.bin` exists after a successful build.</action>
  <verify>`.\scripts\build.ps1`; then `Test-Path build\fish_pump_relay_timer_control.bin`; then `rg "Phase 7|Float ON|Relay 1|Relay 2|active_relay|start phase" docs/development-notes.md`</verify>
  <acceptance_criteria>
    - ESP-IDF build passes or the exact environment blocker is recorded.
    - Development notes preserve the Phase 7 runtime contract for Phase 8-10 agents.
    - Summary records source checks and build result.
    - No cooling runtime, hardware mutation API, or full dashboard redesign is introduced.
  </acceptance_criteria>
  <done>Phase 7 has dashboard clarity, handoff notes, and build validation evidence.</done>
</task>

</tasks>

<verification>
- [ ] `rg "timer1_start_phase|timer2_start_phase" main/static/dashboard.html main/static/app.js main/web_server.c`
- [ ] `rg "Float ON.*Timer 1|Float OFF.*Timer 2|Relay 1|Relay 2|พร้อมเริ่ม" main/static/dashboard.html main/static/app.js`
- [ ] `rg "relay1_energized|relay2_energized|active_relay" main/static/dashboard.html main/static/app.js main/web_server.c`
- [ ] `rg "http://|https://|cdn|Tailwind|Google Fonts|Material Symbols" main/static` returns no external dependency additions.
- [ ] `.\scripts\build.ps1`
- [ ] `Test-Path build\fish_pump_relay_timer_control.bin`
</verification>

<success_criteria>
- Dashboard exposes Timer 1/Timer 2 start phase configuration.
- Dashboard communicates Float ON -> Timer 1 / Relay 1 and Float OFF -> Timer 2 / Relay 2.
- Dashboard shows Relay 1 and Relay 2 states separately.
- Stopped-state preview is clear and does not imply the pump is running.
- ESP-IDF build validates the complete Phase 7 integration.
</success_criteria>

<output>
After completion, create `.planning/phases/07-dual-timer-relay-pump-runtime/07-03-SUMMARY.md`.
</output>
