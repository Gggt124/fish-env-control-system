---
phase: 07-dual-timer-relay-pump-runtime
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - main/app_main.c
  - main/web_server.c
autonomous: true
requirements:
  - PUMP-01
  - PUMP-02
  - PUMP-03
  - PUMP-04
  - PUMP-05
  - PUMP-06
user_setup: []
must_haves:
  truths:
    - "Plan 02 depends on Plan 01's dual-relay `pump_control` config/status contract."
    - "D-06: Add dual-relay status fields while preserving compatibility fields such as `relay_energized`."
    - "D-07/D-08: Status must expose `active_relay` with relay1/relay2/none and separate `relay1_energized`/`relay2_energized` booleans."
    - "D-09: In stopped state, active_timer/active_relay preview the ready channel, but all energized fields are false."
    - "D-14: Config save while running preserves safe save semantics: save first, stop safe, reinit, and restart from current float/start_phase only after successful NVS commit."
    - "PUMP-04 requires user-configurable Timer 1 and Timer 2 start phases in this phase; do not defer that to hardware install APIs."
    - "Do not add GPIO mutation APIs, cooling APIs, or hardware install UI in this plan."
  artifacts:
    - path: "main/app_main.c"
      provides: "Boot-time merge of active hardware map, Relay 1/Relay 2 polarity, and timer start phases into pump runtime"
      contains: "pump_relay2_gpio"
    - path: "main/web_server.c"
      provides: "Backward-compatible pump config/status API with dual-relay status and start-phase save support"
      contains: "active_relay"
  key_links:
    - from: "main/app_main.c"
      to: "components/nvs_store/nvs_store.h"
      via: "loads active hardware map and pump settings for runtime config"
      pattern: "nvs_store_load_pump_settings"
    - from: "main/web_server.c"
      to: "components/pump_control/pump_control.h"
      via: "serializes new dual-relay runtime status fields"
      pattern: "relay1_energized"
---

<objective>
Integrate the dual-relay pump runtime into boot and the existing authenticated pump API.

Purpose: Connect Phase 6 storage/hardware-map data to the Phase 7 runtime and expose additive API/status fields so existing UI code remains compatible while new dashboard controls can configure timer start phases and display Relay 1/Relay 2 state.
Output: Boot initializes the dual-relay runtime with active float/Relay 1/Relay 2 GPIOs, Relay 1/Relay 2 polarities, timer durations, and timer start phases; pump APIs preserve existing fields and add dual-relay/status/start-phase support.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-dual-timer-relay-pump-runtime/07-CONTEXT.md
@.planning/phases/07-dual-timer-relay-pump-runtime/07-RESEARCH.md
@.planning/phases/07-dual-timer-relay-pump-runtime/07-01-PLAN.md
@components/pump_control/pump_control.h
@components/nvs_store/nvs_store.h
@components/hardware_map/hardware_map.h
@main/app_main.c
@main/web_server.c
</context>

<tasks>

<task type="auto">
  <name>Task 1: Map active hardware and pump settings into dual-relay runtime at boot</name>
  <files>main/app_main.c</files>
  <read_first>main/app_main.c, components/pump_control/pump_control.h, components/nvs_store/nvs_store.h, components/hardware_map/hardware_map.h</read_first>
  <action>Update the boot-time pump config merge helper to populate float GPIO, Relay 1 GPIO, Relay 2 GPIO, Relay 1 polarity, Relay 2 polarity, Timer 1/Timer 2 durations, and Timer 1/Timer 2 start phases from `nvs_store_load_hardware_map()` and `nvs_store_load_pump_settings()`. Keep existing auto-start suppression for invalid pump settings or invalid active hardware map. Update boot logs to mention Relay 1 and Relay 2 runtime initialization and to make clear DS18B20/cooling relay remain inactive until Phase 8.</action>
  <verify>`rg "relay1|relay2|pump_relay1_gpio|pump_relay2_gpio|timer1_start_phase|timer2_start_phase|auto_start" main/app_main.c`</verify>
  <acceptance_criteria>
    - Boot uses active map Relay 1/Relay 2 pins instead of hardcoded single relay only.
    - Relay 1 and Relay 2 polarity are consumed independently.
    - Timer start phases are passed to `pump_control`.
    - Invalid hardware/pump settings still suppress auto-start.
    - Cooling relay and DS18B20 are still not driven/read in Phase 7.
  </acceptance_criteria>
  <done>Boot initializes the new dual-relay runtime safely.</done>
</task>

<task type="auto">
  <name>Task 2: Serialize dual-relay status while preserving compatibility fields</name>
  <files>main/web_server.c</files>
  <read_first>main/web_server.c, components/pump_control/pump_control.h, .planning/phases/03-authenticated-pump-control-api/03-CONTEXT.md, .planning/phases/07-dual-timer-relay-pump-runtime/07-CONTEXT.md</read_first>
  <action>Update pump status JSON helpers to serialize `active_relay`, `relay1_energized`, and `relay2_energized` from `pump_control_get_status()`. Preserve `relay_energized` as a compatibility alias for the active/ready channel and preserve existing `relay_gpio` as Relay 1 compatibility unless a more precise active-relay GPIO alias is already established locally. Include Relay 1/Relay 2 GPIO and polarity read-only fields in config/status responses. Add a stable fault/status field if `pump_control` exposes unsafe relay state or config-invalid status. Ensure stopped preview has `running:false` and all energized fields false even when `active_timer`/`active_relay` show a ready channel.</action>
  <verify>`rg "active_relay|relay1_energized|relay2_energized|relay_energized|relay1_polarity|relay2_polarity|fault" main/web_server.c`</verify>
  <acceptance_criteria>
    - Existing clients still receive `relay_energized`, `relay_gpio`, `active_timer`, `phase`, and `countdown_sec`.
    - New clients can distinguish Relay 1 and Relay 2 energized state.
    - `active_relay` uses only `relay1`, `relay2`, or `none`.
    - Fault/config-invalid state is visible in API output when runtime reports it.
    - Auth and same-origin patterns are unchanged for existing routes.
  </acceptance_criteria>
  <done>Pump status API is dual-relay aware and backward-compatible.</done>
</task>

<task type="auto">
  <name>Task 3: Accept validated timer start phases in pump config save</name>
  <files>main/web_server.c</files>
  <read_first>main/web_server.c, components/nvs_store/nvs_store.h, components/hardware_map/hardware_map.h, .planning/REQUIREMENTS.md</read_first>
  <action>Extend `GET /api/pump/config` and `POST /api/pump/config` so Timer 1 and Timer 2 start phases are editable fields, expected as stable strings matching the hardware_map names (`on` or `off`). Validate both start phases using `hardware_map_timer_start_phase_valid()` or equivalent parser logic before saving. Preserve existing full-replacement semantics: required duration fields, `auto_start`, Relay 1 compatibility polarity, and now both start phases must be present. Continue rejecting GPIO, hardware map, cooling, and relay2 polarity mutation fields as read-only until later phases own them. Save the complete settings to NVS before applying runtime changes.</action>
  <verify>`rg "timer1_start_phase|timer2_start_phase|hardware_map_timer_start_phase_name|hardware_map_timer_start_phase_valid|readonly_fields|nvs_store_save_pump_settings" main/web_server.c`</verify>
  <acceptance_criteria>
    - PUMP-04 has an authenticated API path for setting Timer 1/Timer 2 start phases.
    - Invalid start phase values return HTTP 400 with stable error style.
    - Full replacement save semantics remain intact.
    - GPIO/hardware/cooling mutation fields remain read-only.
    - Existing duration, auto-start, and Relay 1 polarity behavior does not regress.
  </acceptance_criteria>
  <done>Pump config API can persist independent Timer 1/Timer 2 start phases.</done>
</task>

<task type="auto">
  <name>Task 4: Preserve safe runtime apply semantics for dual-relay config changes</name>
  <files>main/web_server.c</files>
  <read_first>main/web_server.c, components/pump_control/pump_control.h, components/nvs_store/nvs_store.h, main/app_main.c</read_first>
  <action>Update the web-server settings-to-runtime config helper to populate Relay 1/Relay 2 GPIOs, Relay 1/Relay 2 polarity, and Timer 1/Timer 2 start phases. When saving config while running, preserve the existing safe order: capture running status, validate payload, save NVS successfully, stop pump control to force both relays inactive, reinitialize with the new config, and restart only if it was running before save. Restart should naturally select the current confirmed/preview float state and configured start phase through `pump_control_start()`.</action>
  <verify>`rg "api_pump_settings_to_runtime_config|pump_control_stop|pump_control_init|pump_control_start|relay2|timer1_start_phase|timer2_start_phase" main/web_server.c`</verify>
  <acceptance_criteria>
    - Runtime config apply includes both relays and both start phases.
    - NVS save failure does not mutate runtime state.
    - Running save forces both relays safe before reinit.
    - Stopped save reinitializes stopped with both relays inactive.
    - Start/stop responses still include status snapshots.
  </acceptance_criteria>
  <done>Config saves apply safely to the dual-relay runtime.</done>
</task>

</tasks>

<verification>
- [ ] `rg "pump_relay1_gpio|pump_relay2_gpio|relay1_active_low|relay2_active_low|timer1_start_phase|timer2_start_phase" main/app_main.c main/web_server.c`
- [ ] `rg "active_relay|relay1_energized|relay2_energized|relay_energized|fault" main/web_server.c`
- [ ] `rg "readonly_fields|float_gpio|relay_gpio|pump_relay1_gpio|pump_relay2_gpio|cooling_relay_gpio|timer1_start_phase|timer2_start_phase" main/web_server.c`
- [ ] `rg "pump_control_stop|pump_control_init|pump_control_start|nvs_store_save_pump_settings" main/web_server.c`
- [ ] `.\scripts\build.ps1` after Plan 03 or as an early integration check if feasible.
</verification>

<success_criteria>
- Boot and API code consume the dual-relay runtime contract from Plan 01.
- PUMP-04 has a persisted authenticated config path for Timer 1/Timer 2 start phases.
- Status JSON exposes Relay 1/Relay 2 state and active relay without breaking compatibility fields.
- Safe save/reinit/restart semantics survive dual-relay integration.
- No GPIO mutation API, cooling API, or install UI is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/07-dual-timer-relay-pump-runtime/07-02-SUMMARY.md`.
</output>
