---
phase: 12-app-shell-login-and-owner-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - main/static/style.css
  - main/static/login.html
  - main/static/app.js
autonomous: false
requirements:
  - UI-13
must_haves:
  truths:
    - "A 2-4px blue `:focus-visible` ring is applied to all interactive elements to support keyboard navigation."
    - "Error messages and dynamic updates announce state changes via `aria-live` or `role='alert'`."
    - "The login page presents a clear, focused layout for credential entry and error recovery."
  artifacts: []
  key_links: []
---

<objective>
Implement accessibility primitives (focus rings and live regions) and apply them to polish the Login UI.
</objective>

<execution_context>
@C:/Users/Copter/.codex/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-baseline-ui-audit-and-state-language/11-STATE-LANGUAGE.md
@.planning/phases/12-app-shell-login-and-owner-dashboard/12-CONTEXT.md
@main/static/style.css
@main/static/login.html
@main/static/app.js
</context>

<threat_model>
### T-12-01: Broken focus visibility prevents keyboard navigation
- Severity: medium
- Surface: keyboard focus outline
- Risk: custom CSS resets often hide default focus outlines.
- Mitigation: verify `:focus-visible` ring applies to `a, button, input`.
- Verification: manually tab through the login page to confirm ring visibility.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add accessibility primitives to CSS and JS</name>
  <read_first>
    - .planning/phases/12-app-shell-login-and-owner-dashboard/12-CONTEXT.md
    - main/static/style.css
    - main/static/app.js
  </read_first>
  <files>
    - main/static/style.css
    - main/static/app.js
  </files>
  <action>
Modify `main/static/style.css` to add a `:focus-visible` outline for `a, button, input, select, .nav-item`. The outline should be 2-4px solid blue (`var(--primary)`).
Modify `main/static/app.js` login logic to ensure the `#login-error` container has `role="alert"` dynamically set when an error occurs, or that it is present in the HTML template. Add `aria-live="polite"` where relevant for loading states.
  </action>
  <verify>
    <automated>rg -n ":focus-visible" main/static/style.css</automated>
  </verify>
  <acceptance_criteria>
    - Interactive elements have a visible focus ring using `:focus-visible`.
    - Dynamic state changes utilize ARIA live regions for screen readers.
  </acceptance_criteria>
  <done>Accessibility primitives are implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Polish Login UI</name>
  <read_first>
    - .planning/phases/12-app-shell-login-and-owner-dashboard/12-CONTEXT.md
    - main/static/login.html
  </read_first>
  <files>
    - main/static/login.html
    - main/static/style.css
  </files>
  <action>
Update `main/static/login.html` to refine its layout. Ensure labels are cleanly associated with inputs, loading states are apparent, and error messages use the ARIA alert role. Align padding and margins to match the design tokens in `style.css`.
  </action>
  <verify>
    <automated>idf.py build</automated>
  </verify>
  <acceptance_criteria>
    - `login.html` is visually polished and matches Phase 12 design standards.
    - Error messages are accessible.
    - The firmware compiles without errors.
  </acceptance_criteria>
  <done>Login UI is updated and fully accessible.</done>
</task>

</tasks>

<verification>
- Focus rings are visible via keyboard navigation.
- Login screen compiles correctly into the firmware.
</verification>

<success_criteria>
- The login interface is clean and usable.
- Keyboard navigation works across all interactive elements.
</success_criteria>
