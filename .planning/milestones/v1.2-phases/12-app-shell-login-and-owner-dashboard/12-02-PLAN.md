---
phase: 12-app-shell-login-and-owner-dashboard
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01-PLAN.md
files_modified:
  - main/static/style.css
  - main/static/app.js
  - main/static/dashboard.html
  - main/static/status.html
  - main/static/hardware.html
  - main/static/wifi.html
autonomous: false
requirements:
  - UI-13
must_haves:
  truths:
    - "A mobile Slide-out Navigation Drawer replaces the horizontal icons on mobile viewports."
    - "A Hamburger menu (`&#9776;`) is used in the mobile topbar to toggle the drawer."
    - "The drawer automatically closes when tapping outside (overlay) or selecting a menu item."
  artifacts: []
  key_links: []
---

<objective>
Create a cohesive App Shell with a Slide-out Navigation Drawer for mobile devices.
</objective>

<execution_context>
@C:/Users/Copter/.codex/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-app-shell-login-and-owner-dashboard/12-CONTEXT.md
@main/static/style.css
@main/static/app.js
@main/static/dashboard.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Style the Mobile Navigation Drawer</name>
  <read_first>
    - .planning/phases/12-app-shell-login-and-owner-dashboard/12-CONTEXT.md
    - main/static/style.css
  </read_first>
  <files>
    - main/static/style.css
  </files>
  <action>
Add CSS classes for a slide-out drawer (e.g., `.app-sidebar.mobile-drawer`) and a background overlay. Ensure it uses a hardware-accelerated transition (`transform: translateX()`). Add styling for the hamburger button in the `.mobile-topbar`.
  </action>
  <verify>
    <automated>rg -n "translateX" main/static/style.css</automated>
  </verify>
  <acceptance_criteria>
    - Mobile drawer styles are implemented.
  </acceptance_criteria>
  <done>CSS for the mobile drawer is ready.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Drawer Logic in JS</name>
  <read_first>
    - main/static/app.js
  </read_first>
  <files>
    - main/static/app.js
  </files>
  <action>
Add a toggle function (e.g., `toggleMobileNav()`) in `app.js` to open and close the drawer by toggling an `.open` class. Attach click listeners to the overlay and menu items to close the drawer automatically upon selection or dismissal.
  </action>
  <verify>
    <automated>rg -n "toggleMobileNav" main/static/app.js</automated>
  </verify>
  <acceptance_criteria>
    - Toggle logic exists and handles the overlay correctly.
  </acceptance_criteria>
  <done>JavaScript logic for the drawer is implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Apply App Shell to HTML Templates</name>
  <read_first>
    - main/static/dashboard.html
  </read_first>
  <files>
    - main/static/dashboard.html
    - main/static/hardware.html
    - main/static/status.html
    - main/static/wifi.html
  </files>
  <action>
Modify the HTML structure of all authenticated pages. Replace the mobile topbar icon links with a single Hamburger Menu button `<button onclick="toggleMobileNav()">&#9776;</button>`. Ensure the sidebar `<nav>` structure matches the expected format for the mobile drawer CSS. Add the overlay `<div>`.
  </action>
  <verify>
    <automated>idf.py build</automated>
  </verify>
  <acceptance_criteria>
    - All pages share the unified App Shell structure.
    - Build succeeds.
  </acceptance_criteria>
  <done>HTML templates are updated with the App Shell.</done>
</task>

</tasks>

<verification>
- Hamburger button toggles the sidebar on mobile views.
- Clicking outside the sidebar closes it.
- Firmware compiles properly.
</verification>

<success_criteria>
- The user has a clean, uncrowded mobile navigation experience.
</success_criteria>
