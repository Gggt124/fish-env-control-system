---
phase: 18-tft-display-integration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - main/idf_component.yml
  - components/app_config/app_config.h
  - main/CMakeLists.txt
  - main/font8x16.h
  - main/tft_display.h
  - main/tft_display.c
  - main/app_main.c
autonomous: false
requirements:
  - TFT-01
  - TFT-02
user_setup: []
must_haves:
  truths:
    - "TFT display backlight turns on on boot"
    - "Serial logs show TFT display and SPI bus initialized successfully"
    - "Screen renders a 'Booting...' splash screen on startup"
  artifacts:
    - path: "main/font8x16.h"
      provides: "Embedded ASCII 8x16 font table"
      min_lines: 50
    - path: "main/tft_display.h"
      provides: "TFT public driver declarations"
      exports:
        - tft_display_init
    - path: "main/tft_display.c"
      provides: "TFT driver implementation and drawing primitives"
      contains: "esp_lcd_new_panel_ili9341"
  key_links:
    - from: "main/app_main.c"
      to: "main/tft_display.h"
      via: "include and initialization call"
      pattern: "#include \"tft_display\\.h\""
---

<objective>
Initialize the SPI bus (VSPI/SPI3_HOST) and native esp_lcd driver with the espressif/esp_lcd_ili9341 component, define custom font and shape drawing helper functions, and render a basic "Booting..." splash screen on device power-on.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/notes/tft-display-driver-choice.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TFT component dependency, pin configurations, and font/header contracts</name>
  <files>main/idf_component.yml, components/app_config/app_config.h, main/CMakeLists.txt, main/font8x16.h, main/tft_display.h</files>
  <action>
    - Add 'espressif/esp_lcd_ili9341: "^2.0.2"' under dependencies in main/idf_component.yml.
    - Append TFT DevKit V1 30-pin GPIO mappings to components/app_config/app_config.h (CS=5, RESET=22, DC=21, MOSI=23, SCK=18, LED=4).
    - Register 'tft_display.c' under SRCS in main/CMakeLists.txt.
    - Create main/font8x16.h containing the embedded 8x16 ASCII bitmap font table array (font8x16).
    - Create main/tft_display.h declaring 'esp_err_t tft_display_init(void);' and drawing primitive prototypes (e.g. char, string, x2 string, filled rect).
  </action>
  <verify>
    <automated>idf.py build</automated>
  </verify>
  <done>
    TFT package dependency is declared, pins are configured in app_config.h, build register is added, and font/interface headers are created.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement SPI/esp_lcd initialization, custom rendering wrappers, and boot splash screen</name>
  <files>main/tft_display.c, main/app_main.c</files>
  <action>
    - Implement tft_display_init() in main/tft_display.c using native esp_lcd APIs and esp_lcd_new_panel_ili9341.
    - Configure SPI bus on SPI3_HOST with standard VSPI pins from app_config.h, DC/CS panel IO, reset, and BGR color element order.
    - Swap coordinates (swap_xy=true) and mirror axes to set 320x240 landscape orientation.
    - Turn on LED backlight pin GPIO4.
    - Implement drawing primitives in main/tft_display.c: tft_draw_char, tft_draw_string, tft_draw_char_x2 (2x scaled), and tft_fill_rect. Maintain big-endian RGB565 conversion helper for LCD SPI.
    - In main/app_main.c, include 'tft_display.h' and call tft_display_init() immediately after NVS initialization (around line 477).
    - Clear display to black and render a high-contrast centered "Booting..." message with the product name and version.
  </action>
  <verify>
    <automated>idf.py build</automated>
  </verify>
  <done>
    TFT hardware drivers and drawing primitives are implemented, and the boot splash screen is hooked into app_main.c.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint - Human Verification of LCD boot screen</name>
  <files></files>
  <action>
    Confirm that espressif/esp_lcd_ili9341 is authentic and valid. Flash the built firmware, verify serial logs, and visually inspect the 2.4" TFT display on boot.
  </action>
  <verify>
    - Display backlight turns ON.
    - Screen clears to black.
    - Centered "Booting..." splash screen renders correctly.
  </verify>
  <done>
    User confirms the booting splash screen renders correctly on hardware boot.
  </done>
  <what-built>TFT driver initialization, basic rendering canvas wrappers, and booting splash screen.</what-built>
  <how-to-verify>
    1. Confirm that espressif/esp_lcd_ili9341 is authentic and valid at https://components.espressif.com/components/espressif/esp_lcd_ili9341 (ASVS legitimacy gate check).
    2. Flash the built firmware to the DevKit V1 board: idf.py -p COMx flash monitor.
    3. Verify in the serial monitor that "NVS initialized" is logged and no esp_lcd or SPI errors occur.
    4. Visually inspect the 2.4" TFT display on boot:
       - Screen backlight turns ON.
       - Screen clears to black.
       - A centered high-contrast "Booting..." screen renders displaying the product name/version in English without graphical glitches or upside-down mirroring.
  </how-to-verify>
  <resume-signal>Type "approved" once display backlight and "Booting..." splash screen render correctly on hardware boot.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| System Memory | Font arrays and string formatting must not exceed stack/heap allocations to prevent system crashes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | main/tft_display.c | mitigate | Enforce bound checking on string drawing logic (limit text to screen dimensions, 40 chars/line for 8px font) and use safe stack-allocated pixel buffers (limit size to <=1024 bytes). |
| T-18-02 | Denial of Service | main/app_main.c | mitigate | Run initialization on the main thread prior to scheduler start; ensure any testing loops or operations delay properly using vTaskDelay to prevent watchdog panic. |
| T-18-SC | Tampering | main/idf_component.yml | mitigate | Perform manual legitimacy verification on the Espressif Component Registry for espressif/esp_lcd_ili9341 in Wave 1 checkpoint. |
</threat_model>

<verification>
Perform compiler validation using `idf.py build`. Flash the device and verify boot log lines for successful SPI and LCD device registration.
</verification>

<success_criteria>
1. The firmware builds successfully without compilation errors.
2. The TFT display initializes on VSPI without crashes.
3. The display backlight lights up and renders the splash screen on power-on.
</success_criteria>

<output>
Create `.planning/phases/18-tft-display-integration/18-01-SUMMARY.md` when done
</output>
