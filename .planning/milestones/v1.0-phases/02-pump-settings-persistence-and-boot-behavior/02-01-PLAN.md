---
phase: 02-pump-settings-persistence-and-boot-behavior
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/app_config/app_config.h
  - components/nvs_store/nvs_store.h
  - components/nvs_store/nvs_store.c
autonomous: true
requirements:
  - TIME-08
  - RUN-04
  - RUN-05
user_setup: []
must_haves:
  truths:
    - "D-05 missing pump settings are a clean first-boot state and resolve to product defaults with auto-start enabled."
    - "D-06 invalid saved pump settings resolve to defaults but must report invalid status so boot can suppress auto-start."
    - "D-07 invalid saved pump settings are not automatically overwritten, cleared, or repaired."
    - "D-08 load status distinguishes loaded saved settings, defaults from missing settings, and defaults from invalid settings."
    - "D-09 Phase 2 persists only Timer 1/2 ON/OFF seconds, relay polarity, and auto-start."
    - "D-10 raw NVS access for pump settings belongs in nvs_store, not pump_control."
    - "D-11 pump settings use a separate pump_cfg namespace."
    - "D-12 persisted settings use a dedicated storage struct containing only persisted fields and load metadata."
    - "D-13 save validates and commits before any future runtime mutation."
    - "D-14 future running-state save behavior depends on this plan's save-before-apply persistence guarantee."
    - "D-15 future stopped-state save behavior depends on this plan's save-before-apply persistence guarantee."
    - "D-17 an optional clear helper may be added, but no HTTP route or UI for clearing pump settings is allowed in Phase 2."
  artifacts:
    - path: "components/app_config/app_config.h"
      provides: "Product default for pump auto-start"
      contains: "APP_TEMPLATE_PUMP_AUTO_START_DEFAULT"
    - path: "components/nvs_store/nvs_store.h"
      provides: "Pump settings storage struct, load status enum, and persistence API"
      contains: "nvs_store_pump_settings_t"
    - path: "components/nvs_store/nvs_store.c"
      provides: "pump_cfg namespace, validation, load, save, and optional clear helper"
      contains: "NVS_PUMP_NAMESPACE"
  key_links:
    - from: "components/nvs_store/nvs_store.c"
      to: "components/app_config/app_config.h"
      via: "pump settings defaults and timer bounds"
      pattern: "APP_TEMPLATE_PUMP_TIMER_MIN_SEC"
    - from: "components/nvs_store/nvs_store.h"
      to: "main/app_main.c"
      via: "boot reads persisted storage struct and load status"
      pattern: "nvs_store_load_pump_settings"
---

<objective>
Add the storage contract and NVS implementation for persisted pump settings.

Purpose: Make Timer 1/2 durations, relay polarity, and boot auto-start persistable without coupling `pump_control` to NVS or exposing any web/API/UI behavior.
Output: `nvs_store` can load, save, validate, and optionally clear pump settings from a separate `pump_cfg` namespace, with load status that preserves the missing-vs-invalid distinction.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-pump-settings-persistence-and-boot-behavior/02-CONTEXT.md
@.planning/phases/02-pump-settings-persistence-and-boot-behavior/02-RESEARCH.md
@.planning/phases/01-hardware-safe-pump-control-core/01-VERIFICATION.md
@components/app_config/app_config.h
@components/nvs_store/nvs_store.h
@components/nvs_store/nvs_store.c
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add auto-start default constant</name>
  <files>components/app_config/app_config.h</files>
  <read_first>components/app_config/app_config.h, .planning/phases/02-pump-settings-persistence-and-boot-behavior/02-CONTEXT.md</read_first>
  <action>Add `APP_TEMPLATE_PUMP_AUTO_START_DEFAULT true` near the existing `APP_TEMPLATE_PUMP_*` defaults. Keep GPIO pins, float polarity, and debounce as source defaults only; do not add persisted GPIO/debounce constants in this phase.</action>
  <verify>Run `rg "APP_TEMPLATE_PUMP_AUTO_START_DEFAULT" components/app_config/app_config.h`.</verify>
  <acceptance_criteria>
    - Auto-start default is centralized in `app_config.h`.
    - The default value is `true`.
    - No GPIO, float polarity, or debounce persistence scope is added.
  </acceptance_criteria>
  <done>Boot code and NVS defaults can share a single product auto-start default.</done>
</task>

<task type="auto">
  <name>Task 2: Add pump settings API types</name>
  <files>components/nvs_store/nvs_store.h</files>
  <read_first>components/nvs_store/nvs_store.h, components/app_config/app_config.h, components/pump_control/pump_control.h</read_first>
  <action>Extend `nvs_store.h` with a storage-only `nvs_store_pump_settings_t` containing `uint32_t timer1_on_sec`, `timer1_off_sec`, `timer2_on_sec`, `timer2_off_sec`, `bool relay_active_low`, and `bool auto_start`. Add `nvs_store_pump_settings_load_status_t` with statuses for loaded saved settings, defaults because missing, defaults because invalid, and an error/default fallback if useful. Expose `void nvs_store_pump_settings_defaults(nvs_store_pump_settings_t *out)`, `nvs_store_pump_settings_load_status_t nvs_store_load_pump_settings(nvs_store_pump_settings_t *out)`, `bool nvs_store_save_pump_settings(const nvs_store_pump_settings_t *settings)`, and optionally `bool nvs_store_clear_pump_settings(void)`. Do not include `pump_control.h` from `nvs_store.h`.</action>
  <verify>Run `rg "nvs_store_pump_settings|nvs_store_load_pump_settings|nvs_store_save_pump_settings" components/nvs_store/nvs_store.h`.</verify>
  <acceptance_criteria>
    - Public storage struct contains only the six persisted fields.
    - Load status can distinguish loaded, missing/defaulted, and invalid/defaulted.
    - API names follow the existing `nvs_store_*` prefix.
    - `nvs_store.h` remains independent of `pump_control.h`.
  </acceptance_criteria>
  <done>The persistence boundary is explicit and decoupled from runtime GPIO state.</done>
</task>

<task type="auto">
  <name>Task 3: Implement pump_cfg load/save helpers</name>
  <files>components/nvs_store/nvs_store.c</files>
  <read_first>components/nvs_store/nvs_store.c, components/app_config/app_config.h, .planning/phases/02-pump-settings-persistence-and-boot-behavior/02-CONTEXT.md</read_first>
  <action>Add a separate NVS namespace such as `pump_cfg` and keys for `t1_on`, `t1_off`, `t2_on`, `t2_off`, `relay_low`, and `auto_start`. Implement defaults from `APP_TEMPLATE_PUMP_TIMER*`, `APP_TEMPLATE_PUMP_RELAY_ACTIVE_LOW`, and `APP_TEMPLATE_PUMP_AUTO_START_DEFAULT`. Validate all timer durations against `APP_TEMPLATE_PUMP_TIMER_MIN_SEC` and `APP_TEMPLATE_PUMP_TIMER_MAX_SEC`, and validate stored booleans as `0` or `1`. Loading must return defaults with missing status when no pump settings exist. If some keys exist but any key is absent or invalid, return defaults with invalid status and do not erase or overwrite NVS. Saving must validate the whole struct before writing and must return false if any `nvs_set_*` or `nvs_commit()` fails.</action>
  <verify>Run `rg "pump_cfg|t1_on|auto_start|nvs_store_load_pump_settings|nvs_commit" components/nvs_store/nvs_store.c`.</verify>
  <acceptance_criteria>
    - Missing pump settings produce default values and a missing/default status.
    - Complete valid pump settings produce saved values and loaded status.
    - Partial or out-of-range settings produce default values and invalid/default status.
    - Invalid settings are not cleared, repaired, or overwritten automatically.
    - Save validates before commit and reports commit failure.
  </acceptance_criteria>
  <done>`nvs_store` owns durable pump settings and exposes clear load semantics for boot.</done>
</task>

</tasks>

<verification>
- [ ] `rg "APP_TEMPLATE_PUMP_AUTO_START_DEFAULT\\s+true" components/app_config/app_config.h`
- [ ] `rg "nvs_store_pump_settings_t|nvs_store_pump_settings_load_status_t" components/nvs_store/nvs_store.h`
- [ ] `rg "NVS_PUMP_NAMESPACE|pump_cfg" components/nvs_store/nvs_store.c`
- [ ] `rg "nvs_store_load_pump_settings|nvs_store_save_pump_settings" components/nvs_store`
- [ ] `rg "#include \"pump_control.h\"" components/nvs_store` returns no matches.
</verification>

<success_criteria>
- Timer settings, relay polarity, and auto-start are persistable in NVS.
- Persistence uses `pump_cfg`, not `wifi_cfg`.
- Missing settings default cleanly; invalid settings default safely and remain unrepaired.
- `pump_control` remains free of NVS dependencies.
- Requirements TIME-08, RUN-04, and RUN-05 are represented in the persistence contract.
</success_criteria>

<output>
After completion, create `.planning/phases/02-pump-settings-persistence-and-boot-behavior/02-01-SUMMARY.md`.
</output>
