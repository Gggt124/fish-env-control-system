---
phase: 04-web-pump-control-ui
plan: 03
type: execute
wave: 3
depends_on:
  - "04-02"
files_modified:
  - main/static/app.js
  - main/static/dashboard.html
  - main/static/style.css
  - build/
autonomous: true
requirements:
  - TIME-01
  - TIME-02
  - TIME-03
  - TIME-04
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - UI-05
  - UI-06
  - UI-07
user_setup: []
must_haves:
  truths:
    - "D-13 after Start/Stop returns, update from the API response status snapshot when available, then continue normal sync."
    - "D-14 runtime display priority is controller running state, countdown, active timer/phase, then relay and float state."
    - "D-15 if pump status is unavailable, show degraded state, disable Start/Stop temporarily, and keep config editing available when config can load."
    - "D-16 running state wording distinguishes controller state from relay state."
    - "D-17 float wording is binary and ties OFF to Timer 1 and ON to Timer 2."
    - "D-18 relay wording uses ON/OFF plus a short Thai explanation."
    - "D-19 timer phase wording uses `ช่วงเปิด` and `ช่วงปิด`."
    - "D-20 the browser ticks countdown locally every 1 second from the latest device snapshot."
    - "D-21 sync `/api/pump/status` every 2 seconds while the dashboard is visible; device status wins."
    - "D-22 trigger immediate status sync after Start, Stop, Save, and tab visibility restore."
    - "D-23 temporary sync failures continue local countdown from the latest snapshot and show stale/waiting state."
    - "D-24 after more than 5 seconds without successful status sync, treat pump status as stale and disable Start/Stop."
    - "D-25 stop local ticking while the tab is hidden and sync immediately when visible again."
  artifacts:
    - path: "main/static/app.js"
      provides: "Live pump status polling, countdown ticking, stale/degraded handling, and quick system status integration"
      contains: "syncPumpStatus"
    - path: "build/fish_pump_relay_timer_control.bin"
      provides: "ESP-IDF build output after embedded static frontend changes"
      contains: "firmware binary"
  key_links:
    - from: "main/static/app.js"
      to: "main/web_server.c"
      via: "machine-friendly status fields from `GET /api/pump/status`"
      pattern: "countdown_sec"
---

<objective>
Finish live pump status rendering, countdown behavior, degraded states, and validation for the embedded dashboard UI.

Purpose: Make the dashboard feel live while preserving device status as authoritative and proving the embedded frontend still builds into ESP-IDF firmware.
Output: The dashboard polls pump status every 2 seconds while visible, ticks the displayed countdown locally every second, handles stale/unavailable status safely, preserves compact system status, and passes static/build validation.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-web-pump-control-ui/04-CONTEXT.md
@.planning/phases/04-web-pump-control-ui/04-RESEARCH.md
@.planning/phases/04-web-pump-control-ui/04-01-SUMMARY.md
@.planning/phases/04-web-pump-control-ui/04-02-SUMMARY.md
@.planning/phases/03-authenticated-pump-control-api/03-VERIFICATION.md
@AGENTS.md
@scripts/build.ps1
@main/static/dashboard.html
@main/static/app.js
@main/static/style.css
@main/CMakeLists.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render machine-friendly pump status into Thai UI labels</name>
  <files>main/static/app.js, main/static/dashboard.html, main/static/style.css</files>
  <read_first>main/static/app.js, main/static/dashboard.html, .planning/phases/04-web-pump-control-ui/04-CONTEXT.md</read_first>
  <action>Add rendering helpers that map `running`, `float_state`, `active_timer`, `phase`, `countdown_sec`, `relay_energized`, `config_valid`, `initial_stabilizing`, `auto_start`, and `settings_status` into dashboard labels. Use Thai wording locked in context: `ระบบปั๊ม: กำลังทำงาน` / `ระบบปั๊ม: หยุดอยู่`, `ลูกลอย: OFF (ใช้ Timer 1)` / `ลูกลอย: ON (ใช้ Timer 2)`, `รีเลย์: ON (จ่ายปั๊ม)` / `รีเลย์: OFF (ตัดปั๊ม)`, and `Timer 1 - ช่วงเปิด` style phase labels. Format countdown as `MM:SS`, allowing hours to roll into minutes if needed.</action>
  <verify>Run `rg "ลูกลอย|รีเลย์|ช่วงเปิด|ช่วงปิด|format.*Countdown|countdown_sec" main/static/app.js main/static/dashboard.html`.</verify>
  <acceptance_criteria>
    - UI-04 displays active timer, phase, and countdown as MM:SS.
    - UI-05 displays float as binary ON/OFF and never as 0-100% water level.
    - UI-06 displays relay ON/OFF with a short operator explanation.
    - Controller running state is not conflated with relay energized state.
  </acceptance_criteria>
  <done>Pump status fields render with clear operational Thai labels.</done>
</task>

<task type="auto">
  <name>Task 2: Add polling, local countdown tick, and visibility handling</name>
  <files>main/static/app.js</files>
  <read_first>main/static/app.js, .planning/phases/04-web-pump-control-ui/04-CONTEXT.md</read_first>
  <action>Implement `syncPumpStatus()` using `apiGet('/api/pump/status')`, poll every 2 seconds while the dashboard is visible, and run a separate 1-second local tick that decrements the displayed countdown from the latest successful snapshot. Device snapshots always overwrite the local estimate. Stop both polling and ticking while the tab is hidden. On `visibilitychange` back to visible, trigger immediate pump status sync and resume timers.</action>
  <verify>Run `rg "syncPumpStatus|setInterval|visibilitychange|document.hidden|countdown" main/static/app.js`.</verify>
  <acceptance_criteria>
    - `/api/pump/status` sync runs every 2 seconds only while visible.
    - Local countdown ticks every second from the latest device snapshot.
    - Device status wins over local countdown estimates.
    - Hidden-tab timer behavior is not trusted; visible restore syncs immediately.
  </acceptance_criteria>
  <done>The dashboard has live countdown behavior bounded by authoritative device sync.</done>
</task>

<task type="auto">
  <name>Task 3: Handle degraded, stale, disabled, and quick-status states</name>
  <files>main/static/app.js, main/static/dashboard.html, main/static/style.css</files>
  <read_first>main/static/app.js, main/static/dashboard.html, main/static/style.css</read_first>
  <action>Add status freshness handling. If a pump status sync fails but a previous snapshot exists, continue local countdown and show a waiting/stale indicator. If no successful sync occurs for more than 5 seconds, mark pump status stale, disable Start/Stop, and show a pump-status error while keeping timer/config inputs usable. If config can still load, do not block editing. Preserve or adapt the existing compact `/api/status` quick-status update so Wi-Fi mode, AP clients, RSSI, heap, chip, and firmware fields still refresh without taking over the page.</action>
  <verify>Run `rg "stale|lastPumpStatus|pump-status-error|refreshStatus|dash-chip|dash-ap-clients" main/static/app.js main/static/dashboard.html main/static/style.css`.</verify>
  <acceptance_criteria>
    - Temporary pump status failures do not break the full dashboard.
    - Start/Stop are disabled after stale status exceeds 5 seconds.
    - Config editing remains available when only runtime status is unavailable.
    - Compact system/Wi-Fi status still updates from `/api/status`.
    - Buttons have clear disabled and pending visual states.
  </acceptance_criteria>
  <done>Dashboard fails visibly and safely when runtime status is stale or unavailable.</done>
</task>

<task type="auto">
  <name>Task 4: Run static checks and ESP-IDF build validation</name>
  <files>main/static/dashboard.html, main/static/app.js, main/static/style.css, build/</files>
  <read_first>AGENTS.md, scripts/build.ps1, main/CMakeLists.txt, .planning/phases/04-web-pump-control-ui/04-CONTEXT.md</read_first>
  <action>Run focused static checks for pump API routes, required DOM IDs, timer fields, no CDN/external links, no editable GPIO/debounce controls, no water-level percentage wording, and preserved `/status`/`/wifi` navigation. Then run the documented ESP-IDF build workflow. Prefer `.\scripts\build.ps1`; if unavailable, run `chcp 65001 > $null`, export ESP-IDF, then `idf.py build`. Do not flash hardware.</action>
  <verify>`idf.py build` succeeds and `build/fish_pump_relay_timer_control.bin` exists.</verify>
  <acceptance_criteria>
    - Static checks prove UI-01 through UI-07 and TIME-01 through TIME-04 are represented.
    - No CDN, Google Fonts, Tailwind, Material Symbols, package, or internet dependency is introduced.
    - Firmware build succeeds with embedded static files.
    - No hardware flashing or monitor session is performed.
  </acceptance_criteria>
  <done>Phase 4 frontend changes are statically checked and build-valid.</done>
</task>

<task type="auto">
  <name>Task 5: Summarize Phase 4 requirement coverage</name>
  <files>.planning/phases/04-web-pump-control-ui/04-03-SUMMARY.md</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/04-web-pump-control-ui/04-CONTEXT.md, previous Phase 4 summaries</read_first>
  <action>Create the plan summary using the established GSD summary format. Include route/API usage evidence, timer field mapping, validation evidence, countdown/stale behavior, no-CDN evidence, build result, and explicit requirement coverage for TIME-01 through TIME-04 and UI-01 through UI-07. Note that Phase 5 owns hardware validation.</action>
  <verify>Summary file exists and names all Phase 4 requirement IDs.</verify>
  <acceptance_criteria>
    - Summary records build command and result.
    - Summary records static checks for timer fields, Start/Stop, labels, and no external dependencies.
    - Summary records deviations, if any.
    - Summary explicitly notes hardware validation is deferred to Phase 5.
  </acceptance_criteria>
  <done>Phase 4 execution evidence is ready for verification.</done>
</task>

</tasks>

<verification>
- [ ] `rg "/api/pump/config|/api/pump/status|/api/pump/start|/api/pump/stop" main/static/app.js`
- [ ] `rg "timer1-on-min|timer1-on-sec|timer1-off-min|timer1-off-sec|timer2-on-min|timer2-on-sec|timer2-off-min|timer2-off-sec" main/static/dashboard.html`
- [ ] `rg "ลูกลอย|รีเลย์|ช่วงเปิด|ช่วงปิด|countdown_sec|MM:SS" main/static/app.js main/static/dashboard.html`
- [ ] `rg "http://|https://|cdn|Tailwind|Google Fonts|Material Symbols" main/static`
- [ ] `rg "float_gpio|relay_gpio|debounce_ms" main/static/dashboard.html` returns no editable control matches.
- [ ] `rg "0-100|water level|ระดับน้ำ.*%" main/static` returns no real-device UI wording.
- [ ] `.\scripts\build.ps1` or documented `idf.py build` succeeds.
- [ ] `Test-Path build\fish_pump_relay_timer_control.bin` returns `True`.
</verification>

<success_criteria>
- Dashboard displays active timer, phase, countdown, binary float state, relay state, and running state.
- Dashboard syncs from device status every 2 seconds while visible and ticks countdown locally every second.
- Dashboard handles stale/unavailable pump status without blocking config editing.
- Existing Wi-Fi setup, system status navigation, login/session, and local-only static frontend constraints remain intact.
- Firmware builds successfully with the Phase 4 embedded frontend.
</success_criteria>

<output>
After completion, create `.planning/phases/04-web-pump-control-ui/04-03-SUMMARY.md`.
</output>
