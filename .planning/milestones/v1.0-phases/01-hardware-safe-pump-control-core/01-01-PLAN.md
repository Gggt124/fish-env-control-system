---
phase: 01-hardware-safe-pump-control-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/app_config/app_config.h
autonomous: true
requirements:
  - HW-02
  - HW-03
  - HW-05
  - HW-06
  - TIME-05
  - TIME-06
  - TIME-07
user_setup: []
must_haves:
  truths:
    - "D-14 and D-15 are represented by explicit min/max timer duration constants."
    - "D-17 is represented by product timer defaults in app_config.h."
    - "Default float and relay GPIO values are centralized and source-configurable."
  artifacts:
    - path: "components/app_config/app_config.h"
      provides: "Pump GPIO, polarity, timer, debounce, and validation constants"
      contains: "APP_TEMPLATE_PUMP_TIMER_MIN_SEC"
  key_links:
    - from: "components/app_config/app_config.h"
      to: "components/pump_control/pump_control.c"
      via: "pump_control default config reads APP_TEMPLATE_PUMP_* constants"
      pattern: "APP_TEMPLATE_PUMP_TIMER1_ON_SEC"
---

<objective>
Add the source-level pump-control defaults that later Phase 1 component work will consume.

Purpose: Make hardware defaults, relay polarity, timer defaults, debounce, and anti-chatter bounds explicit in the existing project configuration boundary before implementing GPIO behavior.
Output: `components/app_config/app_config.h` contains pump-control constants derived from Phase 1 decisions and requirements.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-hardware-safe-pump-control-core/01-CONTEXT.md
@.planning/phases/01-hardware-safe-pump-control-core/01-RESEARCH.md
@components/app_config/app_config.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pump hardware constants</name>
  <files>components/app_config/app_config.h</files>
  <read_first>components/app_config/app_config.h, .planning/phases/01-hardware-safe-pump-control-core/01-CONTEXT.md</read_first>
  <action>Add pump hardware constants near the other `APP_TEMPLATE_*` limits: `APP_TEMPLATE_PUMP_FLOAT_GPIO` set to `32`, `APP_TEMPLATE_PUMP_FLOAT_ACTIVE_LOW` set to `true`, `APP_TEMPLATE_PUMP_RELAY_GPIO` set to `26`, and `APP_TEMPLATE_PUMP_RELAY_ACTIVE_LOW` set to `true`. Keep the existing `APP_TEMPLATE_` prefix and do not add board-specific code outside this config header.</action>
  <verify>Run `rg "APP_TEMPLATE_PUMP_(FLOAT_GPIO|RELAY_GPIO|RELAY_ACTIVE_LOW|FLOAT_ACTIVE_LOW)" components/app_config/app_config.h`.</verify>
  <acceptance_criteria>
    - `components/app_config/app_config.h` contains `#define APP_TEMPLATE_PUMP_FLOAT_GPIO 32`.
    - `components/app_config/app_config.h` contains `#define APP_TEMPLATE_PUMP_RELAY_GPIO 26`.
    - `components/app_config/app_config.h` contains `#define APP_TEMPLATE_PUMP_RELAY_ACTIVE_LOW true`.
    - `components/app_config/app_config.h` contains `#define APP_TEMPLATE_PUMP_FLOAT_ACTIVE_LOW true`.
  </acceptance_criteria>
  <done>Hardware defaults are centralized and match HW-02, HW-03, HW-05, and HW-06.</done>
</task>

<task type="auto">
  <name>Task 2: Add timer and debounce constants</name>
  <files>components/app_config/app_config.h</files>
  <read_first>components/app_config/app_config.h, .planning/REQUIREMENTS.md, .planning/phases/01-hardware-safe-pump-control-core/01-CONTEXT.md</read_first>
  <action>Add product timer constants: `APP_TEMPLATE_PUMP_TIMER1_ON_SEC 20`, `APP_TEMPLATE_PUMP_TIMER1_OFF_SEC 60`, `APP_TEMPLATE_PUMP_TIMER2_ON_SEC 10`, `APP_TEMPLATE_PUMP_TIMER2_OFF_SEC 180`, `APP_TEMPLATE_PUMP_TIMER_MIN_SEC 5`, `APP_TEMPLATE_PUMP_TIMER_MAX_SEC 86400`, and `APP_TEMPLATE_PUMP_FLOAT_DEBOUNCE_MS 100`. Do not use the older defaults from `ref-file/flow-document-v6.md`; `.planning/PROJECT.md` and requirements override them.</action>
  <verify>Run `rg "APP_TEMPLATE_PUMP_TIMER|APP_TEMPLATE_PUMP_FLOAT_DEBOUNCE_MS" components/app_config/app_config.h`.</verify>
  <acceptance_criteria>
    - Timer 1 defaults are exactly 20 seconds ON and 60 seconds OFF.
    - Timer 2 defaults are exactly 10 seconds ON and 180 seconds OFF.
    - Minimum timer duration is exactly 5 seconds.
    - Maximum timer duration is exactly 86400 seconds.
    - Float debounce duration is exactly 100 ms.
  </acceptance_criteria>
  <done>Timer defaults and validation bounds are available for the pump component.</done>
</task>

</tasks>

<verification>
- [ ] `rg "APP_TEMPLATE_PUMP_TIMER1_ON_SEC\\s+20" components/app_config/app_config.h`
- [ ] `rg "APP_TEMPLATE_PUMP_TIMER2_OFF_SEC\\s+180" components/app_config/app_config.h`
- [ ] `rg "APP_TEMPLATE_PUMP_TIMER_MIN_SEC\\s+5" components/app_config/app_config.h`
- [ ] `rg "APP_TEMPLATE_PUMP_FLOAT_DEBOUNCE_MS\\s+100" components/app_config/app_config.h`
</verification>

<success_criteria>
- Phase 1 pump defaults are explicit in `app_config.h`.
- No UI, API, NVS, relay runtime, or timer state-machine behavior is added by this plan.
- All verification checks pass.
</success_criteria>

<output>
After completion, create `.planning/phases/01-hardware-safe-pump-control-core/01-01-SUMMARY.md`.
</output>
