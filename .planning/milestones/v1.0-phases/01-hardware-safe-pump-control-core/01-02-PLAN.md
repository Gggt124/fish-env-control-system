---
phase: 01-hardware-safe-pump-control-core
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - components/pump_control/CMakeLists.txt
  - components/pump_control/pump_control.h
  - components/pump_control/pump_control.c
autonomous: true
requirements:
  - HW-02
  - HW-04
  - HW-06
  - TIME-05
  - TIME-06
  - TIME-07
  - RUN-03
  - RUN-06
  - RUN-07
  - RUN-08
  - RUN-09
  - RUN-10
  - RUN-11
user_setup: []
must_haves:
  truths:
    - "D-01 active-low relay default maps energized to GPIO LOW and inactive to GPIO HIGH."
    - "D-02 init leaves pump control stopped with relay inactive."
    - "D-03 invalid config prevents start and keeps relay inactive."
    - "D-04 relay GPIO is written inactive before and after output configuration."
    - "D-05 requires a 100 ms stable debounce before accepting a new raw float state."
    - "D-06 requires a debounced float-state change to switch immediately to the newly selected timer and reset into that timer's ON phase with its ON duration loaded."
    - "D-07 requires pending debounce to keep using the last confirmed float state while countdown continues and relay is not forced inactive solely because raw input is bouncing."
    - "D-08 requires start to keep relay inactive during initial stabilization before selecting the first timer."
    - "D-09 requires stop while running to immediately set relay inactive, running=false, and idle timer/phase/countdown."
    - "D-10 requires start from stopped to debounce float input first, select Timer 1 or Timer 2 from confirmed float state, and start that timer's ON phase."
    - "D-11 requires start while already running to be no-op success without resetting countdown or relay state."
    - "D-12 requires stop while already stopped to be no-op success while keeping relay inactive."
    - "D-13 requires repeated start to be handled safely in the core even before UI/API protection exists."
    - "D-14 through D-16 reject timer durations outside 5..86400 seconds without clamping."
    - "Float OFF selects Timer 1, Float ON selects Timer 2, and timer changes reset to ON phase."
    - "Relay energizes only during ON phase and de-energizes during OFF, stopped, invalid, and initialization states."
  artifacts:
    - path: "components/pump_control/CMakeLists.txt"
      provides: "ESP-IDF component registration for pump_control"
      contains: "idf_component_register"
    - path: "components/pump_control/pump_control.h"
      provides: "Public pump_control API and status/config types"
      contains: "pump_control_start"
    - path: "components/pump_control/pump_control.c"
      provides: "Safe GPIO initialization, debounce, timer state machine, start/stop, and status implementation"
      min_lines: 200
  key_links:
    - from: "components/pump_control/pump_control.c"
      to: "components/app_config/app_config.h"
      via: "default config derives from APP_TEMPLATE_PUMP_* constants"
      pattern: "APP_TEMPLATE_PUMP_TIMER1_ON_SEC"
    - from: "components/pump_control/pump_control.c"
      to: "ESP-IDF GPIO driver"
      via: "gpio_config and gpio_set_level"
      pattern: "gpio_config"
    - from: "components/pump_control/pump_control.c"
      to: "ESP-IDF esp_timer"
      via: "periodic timer callback drives debounce and phase checks"
      pattern: "esp_timer_start_periodic"
---

<objective>
Create the reusable pump-control firmware component.

Purpose: Deliver the core hardware-safe relay/timer/float behavior before adding persistence, API routes, or UI.
Output: `components/pump_control/` component with a small public API, fail-safe GPIO initialization, config validation, debounced float selection, timer phase transitions, relay control, and status reporting.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-hardware-safe-pump-control-core/01-CONTEXT.md
@.planning/phases/01-hardware-safe-pump-control-core/01-RESEARCH.md
@.planning/phases/01-hardware-safe-pump-control-core/01-01-SUMMARY.md
@components/app_config/app_config.h
@components/session/session.c
@components/wifi_manager/wifi_manager.c
@components/wifi_manager/CMakeLists.txt
@ref-file/flow-document-v6.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add component registration and public API</name>
  <files>components/pump_control/CMakeLists.txt, components/pump_control/pump_control.h</files>
  <read_first>components/wifi_manager/CMakeLists.txt, components/session/session.h, .planning/phases/01-hardware-safe-pump-control-core/01-CONTEXT.md</read_first>
  <action>Create `components/pump_control/CMakeLists.txt` registering `pump_control.c` with `INCLUDE_DIRS "."` and `REQUIRES app_config driver esp_timer freertos`. Create `pump_control.h` with `#pragma once`, C++ guards, and prefix-scoped types/APIs. Define config/status enums for relay polarity, float state, active timer, and timer phase. Define config structs for two timers, float GPIO, float active-low setting, relay GPIO, relay polarity, and debounce ms. Expose exactly these core functions: `pump_control_config_t pump_control_default_config(void)`, `bool pump_control_init(const pump_control_config_t *config)`, `bool pump_control_start(void)`, `bool pump_control_stop(void)`, and `bool pump_control_get_status(pump_control_status_t *out)`. Do not expose raw ESP-IDF internals beyond `gpio_num_t` in config.</action>
  <verify>Run `rg "pump_control_(default_config|init|start|stop|get_status)" components/pump_control/pump_control.h` and `rg "REQUIRES app_config driver esp_timer freertos" components/pump_control/CMakeLists.txt`.</verify>
  <acceptance_criteria>
    - `components/pump_control/CMakeLists.txt` exists and registers `pump_control.c`.
    - `pump_control.h` exposes the five required public functions.
    - `pump_control.h` has C++ guards and includes only necessary standard/ESP-IDF headers.
    - API types can represent invalid/stopped state, active Timer 1/2, ON/OFF phase, float ON/OFF, relay energized/inactive, and countdown seconds.
  </acceptance_criteria>
  <done>The component contract is ready for implementation and future API/UI phases.</done>
</task>

<task type="auto">
  <name>Task 2: Implement safe GPIO init and config validation</name>
  <files>components/pump_control/pump_control.c</files>
  <read_first>components/app_config/app_config.h, components/session/session.c, components/wifi_manager/wifi_manager.c, .planning/phases/01-hardware-safe-pump-control-core/01-CONTEXT.md</read_first>
  <action>Implement file-local static state guarded by a FreeRTOS mutex, using the existing `s_` prefix style. `pump_control_default_config()` must fill defaults from `APP_TEMPLATE_PUMP_*` constants. `pump_control_init()` must validate GPIOs, polarity, debounce, and every timer duration. Reject the entire config if any timer duration is below `APP_TEMPLATE_PUMP_TIMER_MIN_SEC` or above `APP_TEMPLATE_PUMP_TIMER_MAX_SEC`; do not clamp. Reject relay GPIO values that are invalid output pins, including ESP32 input-only pins 34-39. Configure the float GPIO as input with pull-up enabled when active-low. For relay safety, calculate inactive level from polarity, call `gpio_set_level(relay_gpio, inactive_level)` before output config, configure output mode, then call `gpio_set_level(relay_gpio, inactive_level)` again. Invalid init must leave status invalid/stopped and relay inactive if the relay GPIO can be safely addressed.</action>
  <verify>Run `rg "gpio_set_level|gpio_config|APP_TEMPLATE_PUMP_TIMER_MIN_SEC|APP_TEMPLATE_PUMP_TIMER_MAX_SEC" components/pump_control/pump_control.c`.</verify>
  <acceptance_criteria>
    - Default config uses `APP_TEMPLATE_PUMP_FLOAT_GPIO`, `APP_TEMPLATE_PUMP_RELAY_GPIO`, timer default constants, polarity default, and debounce default.
    - Config validation returns false for zero duration, duration below 5 seconds, duration above 86400 seconds, same float/relay pin, invalid GPIO, or relay input-only GPIO.
    - Relay inactive helper derives inactive level from polarity.
    - Relay inactive level is written before and after `gpio_config` for the relay output.
  </acceptance_criteria>
  <done>Safe initialization and validation satisfy HW-02, HW-04, HW-06, TIME-05, TIME-06, TIME-07, and RUN-11 foundations.</done>
</task>

<task type="auto">
  <name>Task 3: Implement debounce, timer state machine, start/stop, and status</name>
  <files>components/pump_control/pump_control.c, components/pump_control/pump_control.h</files>
  <read_first>ref-file/flow-document-v6.md, .planning/phases/01-hardware-safe-pump-control-core/01-CONTEXT.md, components/pump_control/pump_control.h</read_first>
  <action>Implement a component-owned periodic `esp_timer` running every 100 ms after successful init. In the timer callback, sample raw float state, require `debounce_ms` of stable raw state before updating the confirmed debounced float state, and keep using the last confirmed state while debounce is pending. `pump_control_start()` from stopped must be no-op success when already running; otherwise set running true, relay inactive, clear active timer/phase/countdown, and enter initial stabilization until a debounced float state is confirmed. Once confirmed, select Timer 1 for float OFF and Timer 2 for float ON, enter ON phase, load the selected ON duration, and energize relay only if phase is ON. A confirmed float change while running must immediately switch active timer and reset into that timer's ON phase. Phase expiry alternates ON/OFF and reloads the matching duration. `pump_control_stop()` must be no-op success when already stopped; when running it immediately sets relay inactive, sets running false, and clears active timer, phase, and countdown to idle. `pump_control_get_status()` must copy state into the caller buffer without exposing internal pointers.</action>
  <verify>Run `rg "esp_timer_start_periodic|PUMP_CONTROL_TIMER_1|PUMP_CONTROL_PHASE_ON|pump_control_stop|pump_control_get_status" components/pump_control`.</verify>
  <acceptance_criteria>
    - Initial start keeps relay inactive until float state is stable for 100 ms.
    - Float OFF selects Timer 1 and Float ON selects Timer 2.
    - Debounced float change resets the selected timer into ON phase.
    - ON phase energizes relay and OFF phase de-energizes relay.
    - Stop forces relay inactive and clears active timer/phase/countdown.
    - Repeated start while running does not reset countdown or relay state.
    - Repeated stop while stopped remains successful and relay inactive.
  </acceptance_criteria>
  <done>The pump-control component implements the core Phase 1 state machine safely.</done>
</task>

</tasks>

<verification>
- [ ] `rg "idf_component_register" components/pump_control/CMakeLists.txt`
- [ ] `rg "pump_control_default_config" components/pump_control/pump_control.h components/pump_control/pump_control.c`
- [ ] `rg "gpio_set_level" components/pump_control/pump_control.c`
- [ ] `rg "esp_timer_start_periodic" components/pump_control/pump_control.c`
- [ ] `idf.py build` succeeds after this plan or after Plan 03 integration, depending on ESP-IDF component inclusion timing.
</verification>

<success_criteria>
- `pump_control` component exists and follows local component conventions.
- Config validation rejects unsafe timer and GPIO configs instead of clamping.
- Relay is inactive during init, invalid config, stopped state, initial stabilization, and OFF phase.
- Debounced binary float state selects Timer 1/Timer 2 exactly as specified.
- No web API, UI, NVS persistence, auto-start, or hardware test scope is added.
</success_criteria>

<output>
After completion, create `.planning/phases/01-hardware-safe-pump-control-core/01-02-SUMMARY.md`.
</output>
