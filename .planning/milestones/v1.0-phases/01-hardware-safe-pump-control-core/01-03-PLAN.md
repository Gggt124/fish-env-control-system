---
phase: 01-hardware-safe-pump-control-core
plan: 03
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - main/CMakeLists.txt
  - main/app_main.c
autonomous: true
requirements:
  - HW-01
  - HW-03
  - HW-04
  - HW-05
  - RUN-03
  - RUN-11
  - VAL-01
user_setup: []
must_haves:
  truths:
    - "Firmware still targets classic ESP32 through existing sdkconfig defaults."
    - "Boot initializes pump_control with default config but does not auto-start pump control in Phase 1."
    - "Relay GPIO26 is driven inactive during boot initialization."
    - "Existing Wi-Fi, web server, mDNS, captive DNS, and watchdog startup flow remains intact."
    - "`idf.py build` succeeds and produces the firmware binary."
  artifacts:
    - path: "main/CMakeLists.txt"
      provides: "main component dependency on pump_control"
      contains: "pump_control"
    - path: "main/app_main.c"
      provides: "Boot-time pump_control initialization without auto-start"
      contains: "pump_control_init"
  key_links:
    - from: "main/app_main.c"
      to: "components/pump_control/pump_control.h"
      via: "include and init call"
      pattern: "#include \"pump_control.h\""
    - from: "main/CMakeLists.txt"
      to: "components/pump_control"
      via: "REQUIRES pump_control"
      pattern: "pump_control"
---

<objective>
Wire the pump-control component into firmware boot without starting the pump, then validate the build.

Purpose: Make the safe relay initialization part of the actual firmware while preserving existing Wi-Fi/web behavior and deferring auto-start to the persistence phase.
Output: `app_main.c` initializes `pump_control` from default config; `main/CMakeLists.txt` depends on `pump_control`; `idf.py build` succeeds.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-hardware-safe-pump-control-core/01-CONTEXT.md
@.planning/phases/01-hardware-safe-pump-control-core/01-RESEARCH.md
@.planning/phases/01-hardware-safe-pump-control-core/01-02-SUMMARY.md
@main/app_main.c
@main/CMakeLists.txt
@components/pump_control/pump_control.h
@scripts/build.ps1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pump_control as a main dependency</name>
  <files>main/CMakeLists.txt</files>
  <read_first>main/CMakeLists.txt, components/pump_control/CMakeLists.txt</read_first>
  <action>Add `pump_control` to the `REQUIRES` list in `main/CMakeLists.txt`. Keep the existing embedded static files and existing component dependencies unchanged. Do not modify `EMBED_FILES` symbol paths.</action>
  <verify>Run `rg "pump_control" main/CMakeLists.txt`.</verify>
  <acceptance_criteria>
    - `main/CMakeLists.txt` includes `pump_control` under `REQUIRES`.
    - Existing dependencies `app_config`, `nvs_store`, `session`, `wifi_manager`, `esp_http_server`, `esp_wifi`, `cjson`, and `mdns` remain present.
  </acceptance_criteria>
  <done>The main component links against the new pump-control component.</done>
</task>

<task type="auto">
  <name>Task 2: Initialize pump_control during boot without auto-start</name>
  <files>main/app_main.c</files>
  <read_first>main/app_main.c, components/pump_control/pump_control.h, .planning/phases/01-hardware-safe-pump-control-core/01-CONTEXT.md</read_first>
  <action>Include `pump_control.h` in `main/app_main.c`. After the startup banner and before services that may continue running for a long time, create `pump_control_config_t pump_config = pump_control_default_config();` and call `pump_control_init(&pump_config)`. Log success with relay GPIO, float GPIO, and that pump control is stopped. On failure, log an error and continue booting with existing Wi-Fi/web services so the setup UI remains reachable. Do not call `pump_control_start()` in Phase 1; auto-start belongs to Phase 2 after persisted settings exist.</action>
  <verify>Run `rg "pump_control_(default_config|init|start)" main/app_main.c` and confirm `pump_control_start` is absent.</verify>
  <acceptance_criteria>
    - `main/app_main.c` includes `pump_control.h`.
    - `pump_control_default_config()` and `pump_control_init()` are called during boot.
    - `pump_control_start()` is not called anywhere in `main/app_main.c`.
    - Existing NVS, session, Wi-Fi, HTTP, mDNS, DNS, watchdog, and status loop logic remains in place.
  </acceptance_criteria>
  <done>Firmware initializes relay-safe pump hardware while staying stopped by default in Phase 1.</done>
</task>

<task type="auto">
  <name>Task 3: Build validate Phase 1</name>
  <files>sdkconfig.defaults, scripts/build.ps1, build/</files>
  <read_first>AGENTS.md, scripts/build.ps1, sdkconfig.defaults</read_first>
  <action>Run the project build using the documented Windows ESP-IDF workflow. Prefer `.\scripts\build.ps1` because it sets code page 65001, `PYTHONUTF8=1`, exports ESP-IDF, prints `idf.py --version`, and runs `idf.py build`. If the script is unavailable, run `chcp 65001 > $null`, export ESP-IDF from `$env:IDF_PATH` or `C:\esp-idf`, then run `idf.py build`. Do not flash hardware in this phase plan.</action>
  <verify>`idf.py build` succeeds and `build/fish_pump_relay_timer_control.bin` exists.</verify>
  <acceptance_criteria>
    - Build completes without C compiler, linker, or CMake errors.
    - `build/fish_pump_relay_timer_control.bin` is generated.
    - No relay/timer UI/API/NVS persistence behavior is added during build validation.
  </acceptance_criteria>
  <done>Phase 1 automated validation gate passes.</done>
</task>

</tasks>

<verification>
- [ ] `rg "pump_control" main/CMakeLists.txt`
- [ ] `rg "#include \"pump_control.h\"" main/app_main.c`
- [ ] `rg "pump_control_init" main/app_main.c`
- [ ] `rg "pump_control_start" main/app_main.c` returns no matches.
- [ ] `.\scripts\build.ps1` or documented `idf.py build` succeeds.
- [ ] `Test-Path build\fish_pump_relay_timer_control.bin` returns `True`.
</verification>

<success_criteria>
- Firmware initializes `pump_control` with safe defaults and leaves pump stopped.
- Relay output defaults to inactive during boot through the component init path.
- Existing Wi-Fi/setup/web-server foundation remains wired.
- `idf.py build` succeeds.
</success_criteria>

<output>
After completion, create `.planning/phases/01-hardware-safe-pump-control-core/01-03-SUMMARY.md`.
</output>
