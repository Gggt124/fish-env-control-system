---
phase: 16
plan: "16-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_ui_phase16.py
  - main/static/wifi.html
  - main/static/style.css
  - main/static/app.js
autonomous: true
requirements:
  - WIFI-UI-01
  - WIFI-UI-02
  - WIFI-UI-03
  - WIFI-UI-04
must_haves:
  truths:
    - "When opening the Wi-Fi page, the user sees a styled Empty State Card in place of the input form instructing them to select a network from the list on the left."
    - "Clicking a network in the scan list smoothly fades out the Empty State Card and fades in the input panel over 0.2s."
    - "Clicking the 'Cancel' (ยกเลิก) button clears the selected network, hides the input form, and restores the Empty State Card."
    - "Users with reduced motion enabled experience an instant (0s transition) swap between the Empty State Card and the input panel."
    - "The Empty State Card and the input panel stack normally on mobile layouts below the desktop breakpoint."
    - "No backdrop-filter (blur) GPU styles are used anywhere in the Wi-Fi setup page layout."
  artifacts:
    - path: "tests/test_ui_phase16.py"
      provides: "Automated assertions for UI empty state, CSS transitions, media queries, and style check"
    - path: "main/static/wifi.html"
      provides: "Structural HTML for Empty State Card, modified input panel without overlay-hint"
    - path: "main/static/style.css"
      provides: "CSS styling for Empty State Card, fade transitions, prefers-reduced-motion override, and mobile responsive stack"
    - path: "main/static/app.js"
      provides: "JS transition orchestration logic, fadeSwap helper, updated selectNetwork and clearSelection logic"
  key_links:
    - from: "main/static/app.js"
      to: "main/static/wifi.html"
      via: "Class addition and selection event listeners"
    - from: "main/static/style.css"
      to: "main/static/wifi.html"
      via: "Flex styling, responsive layout breakpoints, and transition durations"
---

<objective>
Polish the Wi-Fi setup configuration UI. Replace the CPU-heavy backdrop blur disabled panel overlay with a clean Empty State Card (Option 1) that instructs the user to choose a Wi-Fi from the left list. Implement smooth CSS opacity transitions (0.2s) when swapping between the Empty State Card and the credentials form, honoring prefers-reduced-motion media query. The layout must stack normally on mobile.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@main/static/wifi.html
@main/static/style.css
@main/static/app.js
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Create automated verification tests for Phase 16</name>
  <files>tests/test_ui_phase16.py</files>
  <action>
Create tests/test_ui_phase16.py as a Python unittest that loads wifi.html, style.css, and app.js. Assert the following behaviors:
- wifi.html must contain an Empty State Card element with id 'empty-state-card' containing the required icon '📶' or '👈', Thai heading 'โปรดเลือกเครือข่าย', and description explaining to select a network.
- wifi.html must not contain the old overlay-hint elements or inline backdrop-filter attributes.
- style.css must define styles for '.empty-state-card' using the design system variables: background 'var(--surface-container-low)', border '1px dashed var(--outline-variant)', and border-radius 'var(--radius-lg)'.
- style.css must define transitions for opacity on both '.empty-state-card' and '.input-panel' (0.2s ease-out).
- style.css must not use 'backdrop-filter: blur' or '-webkit-backdrop-filter: blur' anywhere inside the Wi-Fi setup rules.
- style.css must contain a media query for '@media (prefers-reduced-motion: reduce)' that overrides standard transitions to 0s.
- app.js must reference 'empty-state-card' or 'emptyCard' and implement 'fadeSwap' sequential transition logic.
Ensure you run and verify that the tests fail initially due to missing implementation details.
  </action>
  <verify>
Run the test command:
python -m unittest tests/test_ui_phase16.py
Verify that the test suite is discovered and fails with clear assertion errors on the missing empty state elements and style configurations.
  </verify>
  <done>
tests/test_ui_phase16.py exists, compiles cleanly, and fails on empty state and transition checks.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Empty State Card, CSS transitions, and JS fade-swap logic</name>
  <files>main/static/wifi.html, main/static/style.css, main/static/app.js</files>
  <action>
Implement the frontend layout, styles, and triggers to fulfill all locked decisions:
1. In main/static/wifi.html:
   - Insert the Empty State Card (id='empty-state-card') inside the '.wifi-grid' column alongside the input panel. Include icon '📶' (or '👈'), heading 'โปรดเลือกเครือข่าย', and description 'เลือกเครือข่าย Wi-Fi ที่พบในตารางด้านซ้ายเพื่อกรอกข้อมูลการตั้งค่า' (per D-01, D-02).
   - Remove the old overlay-hint div from '#input-panel'.
   - Add the class 'hidden' to the '#input-panel' so it starts hidden on page load.
2. In main/static/style.css:
   - Remove the '.overlay-hint' and '.overlay-hint-box' rules to clean up unused code and avoid backdrop-blur styles (per D-04).
   - Add '.empty-state-card' styles using var(--surface-container-low), var(--outline-variant), and var(--radius-lg) (per D-03).
   - Configure opacity transition class '.fade' and base rules on both cards: '.empty-state-card, .input-panel' with 'transition: opacity 0.2s ease-out' (per D-05).
   - Implement '@media (prefers-reduced-motion: reduce)' block setting transition duration to 0s to support accessibility (per D-06).
   - Ensure the layout stacks normally below the desktop breakpoint (using grid-template-columns: 1fr on mobile and 8fr 4fr on desktop) (per D-07).
3. In main/static/app.js:
   - Define a helper function 'fadeSwap(toHide, toShow)' to sequentially transition opacity (fade out first, wait 200ms, hide, show second, reflow, fade in) (per D-05). Check 'prefers-reduced-motion: reduce' media query in JS to perform an instant swap if user has motion reduction enabled (per D-06).
   - Update 'selectNetwork(ssid)' to trigger 'fadeSwap(emptyCard, panel)' when a network is selected.
   - Update 'clearSelection()' to trigger 'fadeSwap(panel, emptyCard)' when selection is cancelled.
  </action>
  <verify>
1. Run the test command:
   python -m unittest tests/test_ui_phase16.py
   Confirm that all assertions pass successfully.
2. Build the ESP-IDF project:
   idf.py build (or .\scripts\build.ps1)
   Verify the build completes with 0 errors and generates the updated firmware binary containing the embedded asset changes.
  </verify>
  <done>
The Empty State Card is styled and markup is added, the sequential opacity transition and prefers-reduced-motion rules are defined, the JS uses fadeSwap to transition panels, the unit tests pass, and the project builds successfully.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run full verification suite and final build check</name>
  <files>tests/test_ui_phase16.py, main/static/wifi.html, main/static/style.css, main/static/app.js</files>
  <action>
Verify that all UI changes do not cause regressions across the rest of the application.
1. Clean the build directories.
2. Run the entire Python test suite to verify both Phase 13 and Phase 16 UI requirements.
3. Perform a full compile check of the ESP-IDF project.
  </action>
  <verify>
1. Clean compile build artifacts:
   idf.py fullclean
2. Execute all python unittests:
   python -m unittest discover tests
   Ensure all tests execute and pass cleanly.
3. Compile the ESP-IDF project:
   idf.py build
   Ensure compilation succeeds with no warnings or errors.
  </verify>
  <done>
All tests discoverable under the tests/ directory pass, and the firmware build compiles successfully.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → HTTP API | Settings configuration requests are sent from the browser to the ESP32 web server. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | main/static/app.js | mitigate | Client-side changes are UX improvements only. Dynamic network details (SSID) are sanitized using escHtml and escJs helpers before UI insertion to prevent XSS / HTML injection. |
| T-16-02 | Information Disclosure | main/static/wifi.html | mitigate | The password field uses type='password' by default. The password character visibility is only exposed via user checking the visibility checkbox. |
| T-16-03 | Denial of Service | main/static/style.css | mitigate | Replacing backdrop-filter with plain opacity transitions prevents heavy GPU compositing repaints, resolving rendering issues on low-power devices. |
</threat_model>

<verification>
Ensure all files compile cleanly inside the ESP-IDF environment using the local compiler and script:
- Run `idf.py build` (or `.\scripts\build.ps1`) and verify the build finishes successfully with code 0.
- Verify through git that all code edits are clean and no compiler warnings exist.
</verification>

<success_criteria>
- Build compiles successfully with 0 errors.
- Unittests in `tests/test_ui_phase16.py` pass.
- Empty State Card (โปรดเลือกเครือข่าย) with icon 📶 and description displayed when no network is selected.
- Selecting a network triggers sequential fade swap to the credentials input form over 0.2s.
- Clicking Cancel clears selected network and transitions back to Empty State Card.
- Backdrop-filter GPU-heavy styles are completely removed.
- Media query prefers-reduced-motion forces 0s transition.
</success_criteria>

<output>
Create `.planning/phases/16-wi-fi-setup-ui-polish/16-01-SUMMARY.md` when done
</output>
