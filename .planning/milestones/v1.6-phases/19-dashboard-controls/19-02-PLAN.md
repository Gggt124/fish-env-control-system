---
phase: 19-dashboard-controls
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - main/static/index.html
  - main/static/style.css
  - main/static/app.js
autonomous: true
requirements:
  - DASH-UI-02
must_haves:
  truths:
    - User sees time remaining inside a circular progress ring on the dashboard.
    - The circular progress ring animates smoothly as time counts down.
  artifacts:
    - path: main/static/index.html
      provides: SVG elements for the progress ring inside `.pump-countdown-wrap`
    - path: main/static/style.css
      provides: CSS classes for SVG ring styling and transitions
    - path: main/static/app.js
      provides: Logic to dynamically update the ring's `stroke-dashoffset`
  key_links:
    - from: main/static/app.js
      to: main/static/index.html
      via: Modifying `#pump-progress-ring-circle` inline styles
---

<objective>
Implement Circular Progress Rings for Timer Countdown to visualize time remaining.
Purpose: Satisfy the data visualization redesign requirements of DASH-UI-02.
Output: Updated UI with animated SVG circular progress for the pump countdown.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@main/static/index.html
@main/static/style.css
@main/static/app.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SVG Progress Ring HTML</name>
  <files>main/static/index.html</files>
  <action>
In `#view-dashboard`, locate the `.pump-countdown-wrap` div.
Add an `<svg class="progress-ring">` containing two `<circle>` elements: a background track circle and a foreground `#pump-progress-ring-circle` element.
Ensure the existing `.pump-countdown` text element is positioned so that it renders inside the center of the SVG ring.
  </action>
  <verify>
    <automated>grep -c "pump-progress-ring-circle" main/static/index.html</automated>
  </verify>
  <done>SVG progress ring markup is correctly inserted around the countdown text.</done>
</task>

<task type="auto">
  <name>Task 2: Style Progress Ring in CSS</name>
  <files>main/static/style.css</files>
  <action>
Add styles for `.progress-ring` and `.progress-ring-circle`.
Set `.pump-countdown-wrap` to `position: relative` and align items centrally so the SVG ring and the text overlap correctly.
Style the `.progress-ring` SVG with appropriate width and height (e.g., 120x120).
Style the circles with no fill and a thick stroke (e.g., 8px).
Set the foreground circle's stroke color to `var(--primary)` and add a `transition: stroke-dashoffset 0.25s linear;` for smooth animation.
Transform the foreground circle `transform: rotate(-90deg); transform-origin: 50% 50%;` to start the progress at the top.
  </action>
  <verify>
    <automated>grep -c "progress-ring" main/static/style.css</automated>
  </verify>
  <done>CSS properly aligns the progress ring and prepares it for stroke-dashoffset animations.</done>
</task>

<task type="auto">
  <name>Task 3: Animate Progress Ring dynamically</name>
  <files>main/static/app.js</files>
  <action>
Modify `renderPumpCountdown()` in `app.js` to animate the progress ring.
Retrieve the active timer and phase from `pumpLastStatus`, and calculate the total duration using the existing `getPumpDurationSec(timer, phase)` function.
If the pump is running and the total duration is valid, calculate the percentage `(pumpDisplayedCountdownSec / totalSec) * 100`.
Calculate the SVG circle's circumference (based on the radius defined in HTML) and set the `strokeDasharray` to the circumference.
Set the `strokeDashoffset` dynamically based on the percentage to visually animate the countdown.
If the pump is stopped or data is missing, reset the ring to 0 or 100% full.
  </action>
  <verify>
    <automated>grep -c "pump-progress-ring-circle" main/static/app.js</automated>
  </verify>
  <done>The ring dynamically animates down as time counts down.</done>
</task>

</tasks>

<verification>
Run `idf.py build` to ensure embedded files compile properly.
</verification>

<success_criteria>
Pump countdown timer displays a circular progress ring that animates down accurately as the phase progresses.
</success_criteria>

<output>
Create `.planning/phases/19-dashboard-controls/19-02-SUMMARY.md` when done
</output>
