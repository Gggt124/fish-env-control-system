---
phase: 19-dashboard-controls
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - main/static/index.html
  - main/static/style.css
autonomous: true
requirements:
  - DASH-UI-01
  - DASH-UI-02
must_haves:
  truths:
    - User sees pump, timer, and cooling controls separated visually into modern cards.
    - User sees a 2-column layout on desktop and single-column stack on mobile.
    - User interacts with iOS-style pill-shaped toggle switches instead of standard checkboxes.
  artifacts:
    - path: main/static/index.html
      provides: Dashboard grid layout structure and toggle switch markup
    - path: main/static/style.css
      provides: CSS classes for dashboard-grid, dashboard-col, and toggle-switch
  key_links:
    - from: main/static/index.html
      to: main/static/style.css
      via: CSS grid classes and toggle classes
---

<objective>
Restructure the Dashboard view into a card-based 2-column grid and implement modern iOS-style toggle switches.
Purpose: Satisfy the layout redesign requirements of DASH-UI-01 and DASH-UI-02.
Output: Updated CSS layout rules and restructured HTML inside `#view-dashboard`.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@main/static/index.html
@main/static/style.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement CSS Grid and Toggle Switch Styles</name>
  <files>main/static/style.css</files>
  <action>
Add `.dashboard-grid` class to establish a responsive layout: `display: grid; grid-template-columns: 1fr; gap: var(--sp-lg);` and for `@media (min-width: 1024px)` use `grid-template-columns: 1fr 1fr;`.
Add `.dashboard-col` to group cards: `display: flex; flex-direction: column; gap: var(--sp-lg);`.
Add `.toggle-switch` styles for iOS-style pill toggles. The `.toggle-switch` container should be inline-block, relative, sized ~44x24px. Hide the actual input. Style `.toggle-slider` as the pill track with a white circular `:before` pseudo-element for the knob. Animate `transform` and `background-color` on `:checked`.
Refine `.toggle-row` to flex align the new `.toggle-switch` alongside its text content smoothly.
  </action>
  <verify>
    <automated>grep -c "toggle-switch" main/static/style.css</automated>
  </verify>
  <done>CSS contains new grid and toggle switch rules without breaking existing CSS variables.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure Dashboard HTML Layout</name>
  <files>main/static/index.html</files>
  <action>
Inside `#view-dashboard`, wrap the existing cards (pump runtime, pump config, cooling runtime) into the new `.dashboard-grid`.
Create two `.dashboard-col` containers.
In the first column, place `#pump-runtime-panel` and the `<section class="pump-config">` containing `#pump-config-form`.
In the second column, place `#cooling-runtime-panel`. Ensure the cards flow neatly.
Update the checkboxes (`#pump-auto-start`, `#cooling-auto-enable`) to use the new `.toggle-switch` HTML structure: wrap the input in `<div class="toggle-switch">` and add `<span class="toggle-slider"></span>` immediately after the input.
  </action>
  <verify>
    <automated>grep -c "dashboard-grid" main/static/index.html</automated>
  </verify>
  <done>Dashboard view sections are wrapped in 2 columns and toggle switches use the new HTML markup.</done>
</task>

</tasks>

<verification>
Run `idf.py build` to ensure embedded files compile properly.
</verification>

<success_criteria>
Dashboard is separated into responsive columns, and toggle inputs use the pill-shaped design.
</success_criteria>

<output>
Create `.planning/phases/19-dashboard-controls/19-01-SUMMARY.md` when done
</output>
