---
phase: 18-modern-app-shell
plan: 03
type: execute
wave: 2
depends_on:
  - 18-02
files_modified:
  - main/static/index.html
  - main/static/login.html
  - main/static/dashboard.html
  - main/static/status.html
  - main/static/wifi.html
  - main/static/hardware.html
  - main/CMakeLists.txt
  - main/web_server.c
autonomous: true
requirements:
  - ARCH-UI-01
must_haves:
  truths:
    - All legacy HTML content is successfully relocated into the unified SPA shell.
    - C server serves the SPA shell for all view endpoints.
  artifacts:
    - path: "main/web_server.c"
      provides: "HTTP route handler for the SPA"
  key_links:
    - from: "main/web_server.c"
      to: "main/static/index.html"
      via: "serve_static returning _binary_index_html_start"
---

<objective>
Migrate the existing page content into the new unified `index.html` view containers, remove the obsolete multi-page HTML files, and update the ESP32 C server to serve `index.html` for all front-end routes.

Purpose: Finalize the SPA architecture implementation to meet ARCH-UI-01 and ensure the firmware correctly serves the new shell.
Output: Unified frontend and updated C code serving it.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-modern-app-shell/18-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Content to SPA Shell</name>
  <files>main/static/index.html, main/static/login.html, main/static/dashboard.html, main/static/status.html, main/static/wifi.html, main/static/hardware.html</files>
  <action>Open `index.html`. Take the core `<body>` or `<main>` content from `login.html`, `dashboard.html`, `status.html`, `wifi.html`, and `hardware.html` and move it into the corresponding `<section id="view-...">` containers in `index.html`. Ensure IDs don't conflict, and avoid duplicating the sidebar or headers. Once the unique content is safely ported into the views, delete the old `login.html`, `dashboard.html`, `status.html`, `wifi.html`, and `hardware.html` files from the filesystem using standard commands.</action>
  <verify>
    <automated>test ! -f main/static/login.html && grep -c "view-dashboard" main/static/index.html > /dev/null</automated>
  </verify>
  <done>All distinct page content resides inside index.html and obsolete files are deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Wire SPA to C Web Server</name>
  <files>main/CMakeLists.txt, main/web_server.c</files>
  <action>In `main/CMakeLists.txt`, replace the entries for the deleted HTML files in `EMBED_FILES` with `static/index.html`. Keep `style.css` and `app.js`. In `main/web_server.c`, update the HTTP GET route handlers for `/login`, `/dashboard`, `/status`, `/wifi`, and `/hardware`. Replace their respective `_binary_*_html_start` and `_binary_*_html_end` external declarations and `serve_static()` calls to uniformly point to `_binary_index_html_start` and `_binary_index_html_end`. Keep existing auth-check and redirect logic intact in those handlers (e.g., if unauthenticated on /dashboard, still return a 302 to /login, where the browser will fetch /login and receive index.html).</action>
  <verify>
    <automated>grep -c "_binary_index_html_start" main/web_server.c > /dev/null</automated>
  </verify>
  <done>C server compiles and returns index.html for all page routes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Network | Server-side routing correctly enforces HTTP 302 redirects for unauthenticated requests before serving SPA shell. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03-AUTH | Information Disclosure | web_server.c | mitigate | Ensure existing `is_authenticated()` checks remain in the route handlers prior to serving index.html. |
</threat_model>

<verification>
- `idf.py build` succeeds.
- Web server handles routes and embeds index.html correctly.
</verification>

<success_criteria>
- Multi-page setup replaced by unified SPA.
- C backend builds without linker errors regarding deleted files.
</success_criteria>

<output>
Create `.planning/phases/18-modern-app-shell/18-03-SUMMARY.md` when done
</output>
