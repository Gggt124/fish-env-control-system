---
phase: 22-uat-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - main/static/style.css
  - main/static/app.js
autonomous: true
requirements:
  - UAT-UI-01
must_haves:
  truths:
    - "Automatic dark mode override is disabled so the UI uses light mode consistently"
    - "Sidebar is completely hidden when a user views the login page"
  artifacts:
    - path: "main/static/style.css"
      provides: "Sidebar hidden rules for login state"
    - path: "main/static/app.js"
      provides: "Routing state injection for login view"
  key_links:
    - from: "main/static/app.js"
      to: "document.body.classList"
      via: "handleRoute()"
      pattern: "login-active"
---

<objective>
Fix UI styling bugs reported during UAT, specifically removing the forced automatic dark mode and hiding the sidebar on the desktop login page. Modifying `style.css` and `app.js` will also trigger a save in pure UTF-8, resolving their encoding issues.
Purpose: Polish the UI to conform exactly to UAT expectations.
Output: Updated `style.css` without dark mode media queries, and updated `app.js` routing logic.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@main/static/style.css
@main/static/app.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Automatic Dark Mode</name>
  <files>main/static/style.css</files>
  <action>Find the `@media (prefers-color-scheme: dark)` block in `main/static/style.css` and completely remove it. The UI should only use the standard light theme rules. Ensure that saving this file is done in standard UTF-8 (which the file edit tool does automatically) to fix the encoding bug.</action>
  <verify>
    <automated>grep -v '^#' main/static/style.css | grep -c "prefers-color-scheme: dark" == 0</automated>
  </verify>
  <done>Dark mode media query is completely removed from style.css</done>
</task>

<task type="auto">
  <name>Task 2: Hide Sidebar on Login Page</name>
  <files>main/static/app.js, main/static/style.css</files>
  <action>In `main/static/app.js`, within the `handleRoute()` function, check if `viewId === 'view-login'`. If true, add the class `login-active` to `document.body.classList`. If false, remove the `login-active` class.

Then, in `main/static/style.css`, append the following CSS rules to properly hide the navigation elements and expand the content when on the login page:
```css
body.login-active #app-sidebar, body.login-active .mobile-topbar {
    display: none !important;
}
body.login-active #app-content {
    margin-left: 0 !important;
    margin-top: 0 !important;
}
```
This ensures the login page is full-screen without navigation elements on both mobile and desktop.</action>
  <verify>
    <automated>grep -c "login-active" main/static/app.js</automated>
  </verify>
  <done>Login page accurately hides the sidebar and expands to full width</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client Browser | CSS and basic JS UI logic manipulation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering | Static UI assets | accept | Static assets are just view layer, server-side auth remains intact on API. |
</threat_model>

<verification>
1. `style.css` no longer contains the `prefers-color-scheme: dark` media query.
2. `app.js` `handleRoute()` correctly toggles the `login-active` class on the body.
3. `style.css` has rules for `body.login-active` that hide the sidebar and topbar.
</verification>

<success_criteria>
- The UI stays in light mode regardless of OS settings.
- The login page renders without the sidebar or mobile topbar.
- Both modified files are saved with UTF-8 encoding.
</success_criteria>

<output>
Create `.planning/phases/22-uat-bug-fixes/22-01-SUMMARY.md` when done
</output>
